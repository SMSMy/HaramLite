# HANDOFF.md — تسليم جلسة إصلاح جودة الفصل (MDX)

> **اقرأ هذا الملف كاملاً قبل أي عمل.** ثم `fixplan.md` (الخطة التفصيلية ✓) ثم `docs/AUDIT.md` (ذاكرة المشروع ✓).
> **اللغة**: تكلّم مع المالك بالعربية ✓ (بأدلة مقيسة ✓) · واكتب الالتزامات بالإنجليزية ✓.

---

## 1. المشروع

`C:\Code-backup\HaramMute Desktop III\haramlite-rs` — **HaramLite**: تطبيق ويندوز (Tauri v2 + Rust) يزيل الموسيقى من الفيديو والصوت **محلياً على الجهاز** (بلا رفع، بلا حساب). الموقع: `https://haramlite.com` (من `docs/` عبر GitHub Pages ✓).

**محرّك الفصل**: منفذ Rust لمسار MDX-Net من `audio-separator` (`src-tauri/src/separator.rs` + `stft.rs`) على نموذج `models/UVR-MDX-NET-Voc_FT.onnx` (64MB ✓) عبر ONNX Runtime.

**الوضعان**: «أغنية» (Song ✓ — سلسلة تحسين + خريطة مقاطع ✓) · «مقطع عادي» (Clip ✓ — كتم/خفض بلا سلسلة ✓).

---

## 2. مهمتك

المستخدم قارن مخرجات المحرّك بـ **UVR5** على **نفس الملف ونفس النموذج** ✗ وقال: **عزلنا أسوأ** ✗. بعد تحقيق طويل مع أربعة مراجعين، **حُسم السبب بقراءة المصدرين الأصليين** ✓ ⇒ **انحرافان** عن UVR5 ⇒ **طبّق إصلاحهما حسب `fixplan.md`** ✓.

---

## 3. المرجعان — **نزّلهما من الرقم المجمَّد** ✓ (لا من `main` ✗)

```bash
# audio-separator — ما نسخه كودنا  (463 سطراً · SHA-256 = F1A647D5D94F938F…)
curl -sSL https://raw.githubusercontent.com/nomadkaraoke/python-audio-separator/7a1c06f42fb5c0a7dd5dcdf45bead5bcababdbae/audio_separator/separator/architectures/mdx_separator.py -o mdx_ref.py

# UVR5 — ما يقارن به المستخدم  (1459 سطراً · SHA-256 = D3D1FD3288491895… · لم يتغيّر منذ 2023-10-18)
curl -sSL https://raw.githubusercontent.com/Anjok07/ultimatevocalremovergui/b20ab2508066b0372631a1b3b5d6b0e04b45d7eb/separate.py -o uvr5.py
```
> ⚠ **نزّل من الرقم أعلاه بالضبط** ✓✓ — من `main`/`master` قد تنزاح الأسطر ✗ فتبطل كل الأدلة في هذا الملف ✓.
> وبعد التنزيل: **تحقّق من البصمة** ✓ (`Get-FileHash … -Algorithm SHA256` ✓) — ثم **أعد ترقيم الأسطر** ✓ وتأكّد أن `UVR5:499` = `source = self.demix(mix)` ✓.

- **`audio-separator`** = ما نسخه كودنا ✓
- **UVR5 `separate.py`** = **ما يقارن به المستخدم** ✓✓ (كتلة MDX في `seperate()` ~474-525)

> **قاعدة مقدّسة**: لا «إصلاح مطابقة» بلا **سطر** من هذين الملفين ✓✓ — أربعة أطراف قالت «تحقّقتُ» ✗ وكانت مخطئة في **ثلاث** مرات ✓، والمصدر حسم كل خلاف في دقائق ✓.

---

## 4. الانحرافان — بالدليل

### A — بوابة المقاطع المشتبهة (الأعلى أثراً)
```python
# UVR5 separate.py
497:  mix = prepare_mix(self.audio_file)      # float32 · 44100 · بلا تطبيع
499:  source = self.demix(mix)                # ← الملف كله، بلا أي بوابة محتوى
```
```rust
// عندنا separator.rs
568:  let kept = silence::compute_kept_ranges(...)     // مصنّف كثافة ✗
675:  let mut vocals_src = [zeros, zeros];            // تصفير افتراضي ✗
687:  for (a,b) in &analysis.suspect { … }            // النموذج على المقاطع فقط ✗
684:  let fade = sr/20;                               // crossfade خطي 50ms ✗
677:  if suspect.is_empty() { vocals_src = mix.clone(); }   // الحالة القصوى: 0% ⇒ لا فصل ✗
```
**الأثر — يعتمد على الكاشف، لا بالافتراض** ✗ (تصحيح مهم بعد مراجعة):
```rust
// silence.rs
 96:  if cuts.is_empty() { return vec![]; }        // ← لا مقاطع صامتة ⇒ [] ⇒ suspect=[] ⇒ لا فصل ✗
100:  // build kept ranges = inverse of cuts       // ← وإلا: kept = مكمّل المقاطع ⇒ المناطق العالية
```
| حالة الملف | `cuts` | `kept` | النتيجة |
|---|---|---|---|
| لا يوجد أي مقطع صامت ≥٨٠٠ms (تحت `p90×0.06` أو −55dBFS) | `[]` | `[]` | **لا استدلال إطلاقاً** ✗ — المخرج = الخليط |
| **الشائع في الأغاني** (مقدمة/خاتمة/فراغات بين الجُمل) | ≠`[]` | مكمّلها | **تغطية جزئية** ⇒ حذف الغناء خارج المقاطع ✗ + حدود بلا سياق `trim` ✗ |
⇒ **الحالة تُقرأ من السجلّ** ✓ (`coverage` / `suspect_spans` ✓) — **ولا يُفترض أحدهما** ✗.
⇒ ولذلك: **إن كان ملفك صاخباً متصلاً فقد لا تُغلق الخطوة ١ الفجوة وحدها** ✓ — **وهذا ليس سبباً لتأخيرها** ✓ (هي مطابقة أرضية صحيحة ✓).

**الحدود لا تُمحى بالتلاشي**: حقل MDX الزمني ≈ **5.92s** (261120/44100 ✓) — وحدّ المقطع عندنا **محتوى-تابع** لا محاذٍ لـ`hop_length`/`gen_size` ✗ ⇒ النموذج بلا سياق `trim` حقيقي هناك ✗ (المقطع العادي يملك سياقاً حقيقياً يُسقطه ✓).

### B/E — مسار المستوى (Level path)
```python
# UVR5
499:  source = self.demix(mix)                  # على المستوى الأصلي ✓
615:  source = source * self.compensate        # التعويض على الغناء نفسه ✓
514:  secondary = mix.T − source.T             # الطرح من mix الأصلي ✓
389:  spec_utils.normalize(source, …)          # التطبيع عند الكتابة ✗
```
```rust
// عندنا
151:  if peak > 0.9 { g = 0.9/peak; mix *= g }   // تطبيع الدخل ✗
714:  *v = s * peak;                             // ثم ضرب الغناء في peak ✗
720:  mix[j] − vocals[j] * COMPENSATE            // طرح من المُطبَّع ✗
```
**الأثر**: عند `peak = 0.5` (الشائع في الفيديو ✓) ⇒ الغناء **−6 dB** ✗ ⇒ **يبقى نصف الغناء في المسار الآلي** ✓✓ = «فصل أقل نظافة» ✓.
> كودنا **منفذ أمين لـ`audio-separator`** ✓ — لكن **مرجع المستخدم UVR5** ✗ ⇒ **القرار: نطابق UVR5** ✓ — **وثبّت هذا القرار في `AUDIT.md`** ✓ (قرار مقصود لا سهو ✓).

---

## 5. الإجراء — خطوة خطوة، وقياس بين كل خطوتين

### قياس قبل البدء — **ثلاثة أسطر من السجلّ، والصقها قبل أي `commit`** ✓
على **نفس ملف المستخدم**:
```
1. sep: mix analysis: suspect_spans=… coverage=… dense=…      ← الحالة الفاصلة
2. peak = …                                                    ← يكشف B/E
3. هل ظهر SEPARATE-AB-REPORT أصلاً؟                            ← إن غاب في clip+dense=false فـMDX لم يعمل (مسار آخر ✗ ليس A)
```
| ماذا ترى | ماذا يعني | ماذا تفعل |
|---|---|---|
| `coverage` منخفضة/صفر أو `suspect_spans=0` | **A** هو المرشّح | نفّذ الخطوة ١ ✓ |
| `coverage` عالية و`peak < 0.9` | A ضعيف على هذا الملف، **B/E** أقوى | نفّذ ١ رغم ذلك ✓ ثم قِس ✓ — **ولا تبدأ ٢ بلا موافقة** ✗ |
| `coverage` عالية و`peak ≥ 0.9` والفجوة في **وسط** الكورس | لا A ولا B يفسّران نزفاً منتظماً | ١ تبقى صحيحة ✓ — وبعدها **ذهبي/STFT** لا تخمين ✗ |

**وبقية القياسات**:
1. **`ق٤`**: أدخل **ثانيتين صمت رقمي** في نسخة من المقطع ⇒ إن عاد الفصل ⇒ **A مؤكد** ✓
2. **`ق٥`** (الأقوى ✓): خذ **نفس WAV** الذي يخرجه `normalize_for_engine` وافتحه **في UVR5** ⇒ اختفى الفرق ⇒ السبب المدخل ✓ / بقي ⇒ المحرك ✓
3. **`ق٦`**: `x̂ = ISTFT(STFT(x))` ⇒ `err = x − x̂` ⇒ ≈0 ⇒ STFT سليم ✓ (يستبعد فرضيته ✓)

### الخطوة ١ — إزالة البوابة (A)
```rust
// separator.rs داخل separate()
let vocals_src = demix(&mut session, &mix, &|p| progress(p))?;   // الملف كله — كما :499
```
+ استعمل حشو UVR5: `pad = gen_size + trim − (L % gen_size)` ✓ (لا `2*TRIM` وحده ✓)
+ أبقِ `analyze_mix` **للتشخيص** فقط ✓ (تسجيل `dense`/`coverage` ✓)
**تحقّق**: `coverage=1.00` ✓ + `ق٤` يصير بلا معنى ✓ — **التزام مستقل** ✓

### الخطوة ٢ — مسار المستوى (B/E) — **مشروطة بموافقة المالك** ⚖️
> **لا تنفّذها تلقائياً** ✗: هي **تترك `audio-separator` عمداً** ✗ لتطابق UVR5 ✓ ⇒ **تغيير مرجع لا إصلاح محايد** ✓ ⇒ **نفّذها فقط إن بقي فرق بعد الخطوة ١، وبعد موافقة صريحة من المالك** ✓ — ووثّقها في `AUDIT.md` كقرار مقصود ✓.

```rust
// احذف تطبيع الدخل من مسار الاستدلال ✗
let mut vocals = demix(&mut session, &mix, …)?;                  // المستوى الأصلي ✓
for c in vocals.iter_mut() { for v in c.iter_mut() { *v *= COMPENSATE; } }   // :615 ✓
let instrumental = [ mix[0] − vocals[0], mix[1] − vocals[1] ];   // :514 ✓
```
**تحقّق**: `vox + inst ≈ mix` ✓ (بفارق `0.021·vox` ✓ = سلوك UVR5 ✓) + `peak` لم يعد يؤثر على مستوى الغناء ✓ — **التزام مستقل** ✓

### الخطوة ٣ — الذهبي (المتبقّي غير المحسوم)
شغّل Python **مرة** (`audio-separator` + نفس الملف) واحفظ: `window` · `STFT` · `model_out` · `result` · `divider` · `tar_waves` ⇒ وقارن Rust بها بحدّ **< −60 dB**.
**والبنود المفتوحة**: **النافذة** (`stft.rs` periodic ✓ مقابل `separator.rs:495` symmetric ✓) · **`overlap`** (عندنا 0.25 ✗ وUVR5 Default = `chunk − n_fft` = 253440 ⇒ ≈2.9% ✓ — تداخلنا أكبر ✓ يُفترض أن يساعد ✓) · **تطبيع المخرج** (UVR5 يفعله `:389` ✓ ولا نفعله ✗).

### الخطوة ٤ — تحسينات مجانية
`pcm_s16le` ← `pcm_f32le` ✓ (الأهمّ ليس الكمّ −96 dBFS ✗ بل **القصّ** بعد الـresample ✗) · `-af aresample=resampler=soxr` ✓ · توضيح تعليق `separator.rs:673-674` ✓.

---

## 6. القواعد الملزمة

1. **متغيّر واحد لكل تجربة** ✗ — لا تجمع خطوتين في تشغيل.
2. **قِس قبل/بعد** ✓ — لا تحكم بالسمع وحده.
3. **لا «إصلاح» بلا سطر من المرجع** ✗✗ (§3).
4. **اختبار سلبي لكل تغيير** ✓ — أفسد عمداً ⇒ يجب أن يفشل الفحص ⇒ استعادة.
5. **اقرأ الناتج لا نيّة الأمر** ✗ — لا تُعلن نجاحاً قبل `Read` للملف بعد الكتابة ✓ (تكرّر هذا الخطأ **أربع مرات** ✓).
6. **لا تثق بتعليق** ✗ — اقرأ التنفيذ ✓.
7. **سجّل في `docs/AUDIT.md`** ✓: الموضع · الأثر · الدليل · ما استُثني ولماذا.

---

## 7. ممنوع ✗

- **لا تلمس** ما ثبتت مطابقته للمرجع (جدول في `fixplan.md` §القسم الأخير ✓): `normalize` على الدخل (لـ`audio-separator` ✓) · الـ`demix` (‏Hann + `result`/`divider` ✓) · `dim_f` ✓ · `compensate` قبل الطرح ✓.
- **إصلاحات مرفوضة** (مُثبتة بالحساب ✓): `(mix[j]*peak) − vocals[j]*COMPENSATE` ✗ (−0.92/−6.02 dB) · `vocals *= peak/0.9` ✗ · تغيير `silence.rs:96` إلى `vec![(0,n)]` ✗ (مشتركة مع `effects.rs:105` و`pipeline.rs:363` ⇒ تُفقد مسار `remux` ✓ `media.rs:527`).
- **لا تعدّل HTML بمطابقة نصّية** ✗ — استعمل مطابقة الأقواس بالعدّ ✓ (ثلاثة أخطاء من هذا النوع ✓: ١٠ وسوم `</a>` ✗ · فقرة محذوفة ✗ · `class="class="…""` أفقد البطاقات شكلها ✗).
- **لا `git push` قبل تشغيل الحراس الخمسة** ✓: `pnpm site:check` (أو `node scripts/check-site-*.cjs` ✓).

---

## 8. الحالة الحالية (سجّلها في بداية جلستك وتحقق منها)

```
آخر التزام: 7b883d3 (إصلاح إعادة إرساء الصوت عند قفزات الصمت في الإضافة)
الشجرة: نظيفة ✓ (عدا HANDOFF.md و fixplan.md غير المتعقّبين ✓)
غير مدفوع: 0 ✓
الحراس الخمسة: كلها exit=0 ✓
حزمة الإضافة: src-tauri/target/store/HaramLite-Bridge-1.1.2-chrome.zip ✓ (1.1.2 · 337KB)
```

**وحالتان لا تنساهما**:
- **إضافة المتجر**: المرفوع `1.1.0` ✓ (بانتظار اعتماد Google ✗) — و`1.1.2` جاهزة محلياً ✓ **لم تُرفع** ✓ ⇒ **إصلاحات الإضافة لم تصل للمستخدمين بعد** ✓ — أخبر المالك بذلك عند أي حديث عن الإضافة ✓.
- **الموقع**: الحيّ يطابق `main` ✓ (بعد دقائق من كل `push` ✓ بسبب ذاكرة CDN ✓).

---

## 9. الأسلوب

- **التزام مستقل لكل خطوة** ✓ (يمكن عزل أي انحدار ✓) · رسالة الالتزام بالإنجليزية ✓ بشرح **السبب والدليل** ✓.
- **ألصق للمالك**: الأمر + **اسم الأداة** + **رمز الخروج** ✓ — لا كلمة «تمّ» ✗.
- **عند أي خلاف تقني**: اذهب إلى **المصدر** ✓✓ — ولا تُحكّم بين آراء ✓.
