# HaramLite 0.2.9 — «الحوافّ المكشوفة»

> **حالة هذه الوثيقة: مُعدَّة لا منشورة.** لا وسم، ولا دفع، ولا بناء. الأرقام أدناه
> **مقيسة محلياً على التزام ساكن** (`git status` فارغ قبل القياس وبعده) ولا تُنسب إلى CI.
>
> **المصدر**: `git log main..0db0ade` — **٥٥ التزاماً**، والدمج من `main = ada7b2c`
> (مقيس: `git rev-list --count main..0db0ade` = **55**). كل بند أدناه مسنود ببصمة التزامه،
> لا بذاكرة كاتب.

| | |
|---|---|
| الإصدار | **0.2.9** — موحَّد في `package.json` · `src-tauri/tauri.conf.json` · `src-tauri/Cargo.toml` · `src-tauri/Cargo.lock` (`pnpm versions:check` ⇒ ✓ · exit 0) |
| إضافة المتصفّح | **1.1.9** — دورة نشر مستقلّة، **لا تُوحَّد مع التطبيق** (`browser-extension/manifest.json`) |
| رأس فرع التكامل | `0db0ade` |
| قاعدة النطاق | `main = ada7b2c` (‏`git merge-base main rehearsal/owner-test` = `ada7b2c`) |

---

## ١) ما أُصلح

### ١-١ · الإضافة: الوضع واللغة واللغتان معاً
**المشكلة المقيسة**: زرّ المشاهدة كان يرسل الطلب **بلا وضع**، فيقع التطبيق على افتراضيّه
(`song`) — أي أن الوضع **المعروض** في الواجهة لم يكن الوضع **المُرسَل**. وزرّ اللغة لم يكن
موجوداً أصلاً، وكانت اللغة تتبع `navigator.languages` وحدها.

- **`9d1c1e4`** — `feat(ext): 1.1.9 - the mode choice reaches the player, named bridge errors stop reading as empty replies, and the language has a visible switch`
- **`d2e3bd3`** — `fix(ext): the displayed mode is the mode that gets sent - the watch request carried none, so the app used its own default (song)`
- **`2fabfba`** — زرّ المشاهدة يعود إلى المسار المؤقّت، والأوضاع الصريحة تُحفَظ، **وقائمة سماح تمنع الإفراط في الإسقاط**
- **`a2d1656`** — جواب جاسوس مستقلّ: الاتجاه عند التبديل الحيّ · مزلاج البدء · **ومسربا HTML كانا يعيدان فتح Trusted Types**
- **`c800cff`** — انتظار القراءة الأولى قبل إرسال الوضع · متابعة تغييرات التبويب الآخر · بناء قائمة السياق مرّة واحدة · وطباعة الوضع المُرسَل
- **`0521402`** — `i18n(ext)`: **لغة واحدة لكل نصّ** — الجرد أولاً، ثم إعادة كتابة الإنجليزية، **وقائمة سماح بالأسماء** تمنع الرجوع
- **`2b1d8c2`** — إعادة قياس صفوف المتصفّح بعد 1.1.9 (إبقاء المصفوفة صادقة)

**القياس**: `pnpm ext:guard` أخضر · `pnpm ext:mutants` (بوّابة سالبة) بلا ثقوب · صفوف المتصفّح الثلاثة بمكافآتها الرقمية وآثارها الملتزَمة في `qa/browser-rows/`.

### ١-٢ · البوت بلغتين + `/lang`
النصوص كانت **عربية مكتوبة في `telegram.rs`** بلا جدول ترجمة. الآن للبوت لغة، و`/lang` يبدّلها فعلاً.

- **`c11fe88`** — `feat(telegram): the bot speaks English too, and /lang switches for real`
- **`5d42327`** — `fix(telegram)`: «نصّا المالك الحيّان كانا نصوصي المرحليّة المتوازية» (تصحيح ذاتي مُعلَن)
- **`756d07a`** — قياس الحدّ الذي سمّاه الجاسوس على النصّ الخام: **ليس موجوداً** (نفي بالقياس لا بالرأي)

### ١-٣ · نافذة/شاشة الإعدادات
**العطل المقيس**: شاشة الإعدادات كانت **شريطاً بطول ٦٧ بكسل داخل ترويسة مموّهة**
(`backdrop-filter` ينشئ containing block فيحبس العنصر `fixed`)، ثم صارت **نافذة ثانية**، ثم
أُلغيت النافذة الثانية لصالح **شاشة داخل النافذة في سبعة تبويبات**.

- **`db4e686`** — `feat(ui): an independent settings window opened from Rust, covered by the existing capability`
- **`2085794`** — `feat(ui): the settings screen moves into the window, in seven tabs`
- **`a5fcb1e`** — `refactor(ui): drop the second window, settings become an in-window screen`
- **`03bb637`** — الشاشة تأخذ الوضع **من الرست**، و CSS‑ي يكفّ عن إبطال `hidden`
- **`5e5ce0f`** — `fix(ui): the settings screen was a 67px strip inside the blurred header` (علّة `backdrop-filter`)
- **`10d8dfa`** — `fix(settings): unnest the seven tab panels so every tab shows its own group`
- **`5190f22`** — `test(settings)`: حكم على **خريطة `id → تبويب` المعلَنة**، لا على حضور DOM
- **`9a49379`** — `ci(layout)`: قياس **شاشة الإعدادات** لا القائمة المنسدلة وحدها
- **`a00c8d5` / `ba916f5` / `3f4e823` / `73d2b6b`** — انظر ١-٤

### ١-٤ · بوّابتا الحرّاس: الأسماء + مُفسَدات رست
**الفجوة المقيسة**: خطّ الأساس كان يقارن **العدد** وحده ⇒ **اختبار يُحذف ويُوضع مكانه اختبار
تفاهي يمرّ**. والحارس الذي **لا يسقط قط ليس حارساً**.

- **`a00c8d5`** — `feat(gate): compare the rust test *names*, not just the counts` — **جردُ أسماء** في `qa/rust-baselines.json`؛ اسم غاب ⇒ **يسقط البناء**
- **`ba916f5`** — `feat(qa): a baseline of test *names*, and the two measured holes as mutants` — الثقبان المقيسان (نزع حارس واختبار تافه مكانه · اختبار صار `#[ignore]`) صارا **مُفسَدَين**
- **`3f4e823`** — `feat(qa): a negative gate for rust guards` — نظير `ext:mutants` للجانب الرست
- **`73d2b6b`** — `ci(gate): run the rust mutant gate in the existing job` — **موصولة بـCI** (وإلا فهي نصيحة، قاعدة المشروع §١٠)
- **`8252a9d`** — تثبيت خطّ الأساس بعد إصلاح الجسر

**القياس (على `2da7093`)**: `pnpm rust:gates` ⇒ **clippy ١٤/١٤ موضعاً فريداً · ٤١٨ ناجح (الأساس ٤١٨) · ٠ فاشل · ٥ مُهمَل · الجرد ٤١٨/٤١٨ اسماً والغائب صفر** · exit 0 — **قيس مرّتين بنفس النتيجة**.

### ١-٥ · جرد التبويبات
الشاشة سبعة تبويبات، ولكل معرّف تبويبٌ **معلَن** في خريطة، والحارس يحكم على **الخريطة** لا على
حضور العنصر في DOM (وهو ما كان يمرّ على عنصر مخفيّ).

- **`f2114a5`**, **`3ce2baf`**, **`9fa296c`**, **`2471286`**, **`96ab31e`** — فرع `agent/settings2` (**غير مدموج بعد**؛ انظر §٣): حذف مسار `#advanced-panel` الميت · تثبيت الحارس على العنوان · حذف توصيل «المعاينة السريعة» ومستهلكيه · توسيع حارس المسارات الميتة إلى **كل `src`**
- **`5190f22`** — التبويبات و`id` المعلَنة (مدمج)

### ١-٦ · حذف المسارات الميتة
- **`2471286`** — `refactor(queue): delete the dead quality-select wiring, and widen the dead-path guard to all of src`
- **`9fa296c`** — `refactor(main): delete the dead quick-preview DOM wiring, and drop its allowlist`
- **`f2114a5`** — `refactor(settings): delete the dead #advanced-panel path and guard against its return`
- **`96ab31e`** — `refactor(preview): delete the quick-preview setting and all of its consumers`

> **حدّ أمانة**: هذه الأربعة **على `agent/settings2` ولم تُدمج في فرع التكامل بعد** (مقيس:
> `git rev-list --count 0db0ade..agent/settings2` = **5**). فحذف المسارات الميتة **ليس في 0.2.9
> حتى تُدمج** — أُثبته هنا ولا أدّعي خلافه.

### ١-٧ · `subcode` وأعطال المحرّك
**المشكلة**: ردّ فشل بلا `code` يُقرأ في الإضافة «ردّ فارغ» فيضيع السبب.

- **`e5ee662`** — `fix(slots,telegram,bridge): an occupancy connector, a t3 that measures its own resource, and engine subcodes`
- **`02ef68a`** — `test(bridge): a raw engine message is never a valid subcode`
- **`824e211`** — `fix(bridge): carry watch and mode together so an explicit clip survives the request file`

**القياس**: `pnpm bridge:codes` ⇒ **٧ رموز مُصدَرة في ١٧ موضع خطأ**، ولكلٍّ مدخل وحالة ترجمة في
ملفَّي الإضافة (**٢١ فحصاً · ٠ فاشل**) · exit 0.

### ١-٨ · ترجمة رسائل الواجهة
**العطل المقيس**: رسائل يصوغها **التطبيق** (لا الإضافة) تُعرض عربية في واجهة إنجليزية،
و`direction="ltr"` — وزيادة مفاتيح ترجمة **لا تُصلحه** لأن النصّ لا يأتي من الإضافة.

- **`3beea55`** — `fix(i18n): read the backend's error through its stable code, not the raw text`
- **`58845e2`** — `fix(ui): every displayed backend error goes through errText`
- **`a273f08`** — `test(ui): no raw .error read escapes errText, and the codes match Rust both ways`
- **`34e2dfa`** — `test(ui)`: عدّ `String(<catch binding>)` عند مصرف العرض أيضاً — **مُفسَد هو الذي وجد الثقب**
- **`3ceb424`** — ربط عيّنة الإلغاء بالنصّ الذي يبنيه `separator.rs` **اليوم** (وكان التعليق تاريخياً)

> **حدّ أمانة**: هذه الخمسة **على `agent/texts2`/`agent/ui-texts` ولم تُدمج** (مقيس:
> `0db0ade..agent/texts2` = **6**). ويبقى **`ط-١٢`** مفتوحاً في `docs/BACKLOG-0.3.md:353`:
> تفصيل المحرّك `{e}` يبقى عربياً داخل جملة إنجليزية.

### ١-٩ · إصلاح `stderr` والتنزيل
**العطل المقيس (بلاغ المالك)**: تنزيل يفشل بعد ١٠٠٪ والرسالة والسجلّ يحملان **تقدّماً فقط بلا
سطر الخطأ**. السبب الجذري بالقراءة: `yt_dlp.rs` كان يصرّف `stderr` **إلى بالوعة تُرمى**،
ويبني الذيل من `stdout` وحده — و`yt-dlp` يكتب `ERROR:` على `stderr`. ثم **أُعيد إنتاجه بنصّه**:
`ERROR: unable to download video data: HTTP Error 403: Forbidden`.

- **`68b7f5e`** — `fix(ytdlp): stop discarding stderr — a failed download now carries its cause`
- **`b0d8f2d`** — `feat(ytdlp): retry a transient download failure once — measured, logged, bounded`
- **`ba5a852`** — `fix(ytdlp): a download failure is no longer labelled "ملفات:" (file error)`
- **`0e9da97`** — `fix(ytdlp): never retry a permanent failure, never spend a valid result`
- **`36f2e62`** — `fix(qa): the rust baseline gate now compares tests_ignored too`
- **`124ee13`** — `fix(ytdlp): judge the failure from its ERROR line, and rescue a valid slot on stall`
- **`6e07746`** — `style(ytdlp)`: إزاحة توثيق لا تغيير كود

> **حدّ أمانة**: السبعة **على `agent/x2-dl` ولم تُدمج** (مقيس: `0db0ade..agent/x2-dl` = **13**).
> أي أن **إصلاح `stderr` والتنزيل ليس في 0.2.9 حتى تُدمج**.

### ١-١٠ · الإلغاء
**العطل المقيس**: الإلغاء من الواجهة كان يُسجَّل `ERROR pipe: خطأ استدلال النموذج: تم إلغاء
المعالجة من قبل المستخدم` — أي **إلغاء المستخدم يُقرأ عطلاً**.

- **`705ae1b`** — `fix(pipe): a user's cancel is not a failure — no ERROR line on the engine path`
- **`1ca3b0d`** — `fix(sep): type the stop in demix, and guard that the type is the one production builds`
- **`1661a70`** — `test(pipe)`: إغلاق تفادي الحارس بالتعليق/التجزئة، وحراسة مساري المحرّك والأداة
- **`438ac78`** — `test(sep)`: حصر الحارس النصّي على **متن `demix`**، وجعل الحارس الحيّ **غير قابل للتخطّي الصامت**
- **`9375383`** — إيقاف الحارس عن رفض الكود الصحيح، ومنع الحارس الحيّ من المرور بلا تنفيذ
- **`fb9f250`** — علم المشاهدة يعود إلى المسار المؤقّت
- **`b348c38`** — `test(layout)`: الحارس يقيس **الحالة التي يفتحها المستخدم فعلاً** (`agent/x4-cancel`، **غير مدموج**: `0db0ade..agent/x4-cancel` = **1**)

**الحدّ المعلَن للإلغاء**: نداء الاستدلال **حاجب ولا يُقطع** داخل العملية ⇒ نقلُه إلى عملية
عاملة **بندٌ مسجَّل في 0.3** (`ARCHIVE/0.2.9PLAN.md:333`، القرار ٢).

### ١-١١ · SSRF — ⚠️ **ليس من عمل 0.2.9**
طلب المشرف إدراج SSRF في «ما أُصلح»، و**القياس ينقض ذلك**:

```
$ git diff --stat main..0db0ade -- src-tauri/src/yt_dlp.rs
(فارغ — 0 بايت)
$ git merge-base --is-ancestor 011c5eb main   ⇒  True
```

حارس المضيفات المحلية/الخاصة (`validate_download_url` · `private_scope`، وفيه التغطية
المُعلَنة `127.0.0.0/8` · `10/8` · `172.16/12` · `192.168/16` · `0.0.0.0` · `::1` · `fc00::/7`
· `fe80::/10` · `localhost`، **والصور المتكافئة** `[::ffff:127.0.0.1]` و`[::ffff:7f00:1]`
و`[::ffff:c0a8:105]`) قائم في `src-tauri/src/yt_dlp.rs:51` و`:125`، **وأُدخل في `011c5eb`
وهو سلفٌ لـ`main`** (`main..011c5eb` = **0**). ⇒ **صفر تغيير على `yt_dlp.rs` في نطاق 0.2.9**،
و**لا يجوز أن يُدرَج بنداً في نشرة 0.2.9**. الموجود فيه اليوم اختباران: **٩ تأكيدات**
(`local_and_private_targets_are_rejected`، `:1537`) و**٢ تأكيدات**
(`malformed_urls_are_rejected_before_any_process_runs`، `:1639`).

---

## ٢) ما لم يُقَس (حدود معلَنة — لا تُقرأ كإنجاز)

> منقولة **بالاسم** من تقارير `ARCHIVE/*-report.md`؛ والقاعدة قاعدة المشروع §٤.

| ما لم يُقَس | التقرير · القسم · السطر |
|---|---|
| **عرض تلغرام في عميل حقيقي** — لا توكن بوت ولا شبكة خارجية؛ كل قياس «ما يصل السلك» على خادم Bot API **وهمي محلّي** عبر `TcpListener` حقيقي. ومحرفا الشريط `▰`/`▱` **غير مُختبَرين على عميل حقيقي** (احتمال تباعد غير أحاديّ **لم يُقَس**) | `x3-tg-report.md` §٨ · سطر 269 |
| **التزامن الحيّ داخل الصفحة (المشغّل)** — لا متصفّح ولا GPU ⇒ كل أحكام التزامن والتسريع **بنيوية من قراءة الكود**؛ التأكيد النهائي بجدول حكم بيد المالك | `review-0.2.9-cline.md` §٦ · سطر 241 |
| **مقطع حقيقي فيه صمت** — مسار clip نُفِّذ فعلاً على يوتيوب حقيقي وأنتج ملفاً **كامل الطول** و`kept=[]` لأن المقطع **بلا صمت ≥٨٠٠ مللي**؛ فالقصّ نفسه مقيس بـffmpeg (`full=6.000s kept_sum=4.300s served=4.300s diff=0.0000s`) **ولم يُقَس مقطع حيّ فيه صمت** | `docs/BACKLOG-0.3.md:349` (ط-٧) |
| **VRAM بثلاث مهامّ متزامنة** — «ثالثٌ يُتوقَّع أن يتجاوز البطاقة **ولم يُقَس عمداً**»؛ والقياسان المتزامنان 4734 MiB (12 ث · CUDA) و**7943 MiB من 8192 (180 ث · DirectML · هامش 249 MiB)** | `docs/BACKLOG-0.3.md:314` و`:319-325` |
| **تثبيت MSI على جهاز نظيف** — يحتاج Windows Sandbox/آلة المالك؛ ولا صفّ مصفوفة مملوء لـ«تشغيل فصل بـCUDA على تثبيت NSIS وحده» مقابل MSI | `docs/BACKLOG-0.3.md:143` (ن-٣) · `0.2.9PLAN.md:212` (الطبقة ب) |
| **سلوك الإضافة محمَّلة من المانيفست** — كروم ١٥٣ لا يحترم `--load-extension`؛ فكل قياس حيّ **حقنٌ عبر CDP** للملف المشحون. **غير المقيس: تسجيل الخدمة الخلفية وربط المانيفست، ونقرٌ حقيقيّ على مدخل قائمة السياق، و`chrome.storage` الحقيقي** | `x1-ext-report.md` §٧ · سطر 545 (بنود ١–٣) |
| **جودة الترجمة الإنجليزية تحتاج مراجعة بشرية** — «الحارس يشترط **الوجود والتكافؤ** لا المعنى»، و«الترجمة الإنجليزية صحيحة لغوياً» **لم يُتحقَّق منها** | `ui-texts-report.md` §٧ · سطر 147 (الفقرة ٤) |
| **ما لم يُقَس في آخر جولات التنزيل** — (أ) **هل الـ403 على المقطع الأخير أم على الطلب الكامل؟** (ب) **هل استُنفدت `--retries`/`--fragment-retries`؟** (ج) **أن إعادةً واحدة كانت ستُنجح فشل المالك بعينه** (المقيس: نفس الأمر نجح **18 من 19**) (د) **زمن انتظار التصريف (1000ms)** على شبكة/قرص بطيء (هـ) أن الواجهة/تلغرام تتصرف صحيحاً بعد التغيير (لم يُشغَّل التطبيق) | `x2-dl-report.md` §٥ · سطر 177 (بنود ٢–٦) · و§٨-٦ سطر 310 · و§٩-٤ سطر 429 |
| **لا جلسة WebView2 ولا لقطة للتطبيق العامل** — حارس الهندسة يقود **كروم بلا واجهة** على `dist/` عبر CDP: نفس DOM ونفس CSS، **لا داخل WebView2**؛ وعدد النوافذ=١ **مقيس بنيوياً لا بتشغيل** | `settings2-report.md` §٦ · سطر 244 (بنود ١–٢) |
| **حارس الهندسة يقرأ نصّاً عربياً مشحوناً لا الجدول** (`i18n table NOT readable from src/i18n.ts — measuring GEOMETRY ONLY`) ⇒ أثر تبديل اللغة على **عرض** التسميات **غير مقيس** | `settings2-report.md` §٦ · سطر 250 (بند ٣) |
| **لا عدّاء CI** — كل الأزمنة محلية، وكلفة البوّابة على `windows-latest` (بناء بارد) **لم تُقَس** | `gates-report.md` §٥ · سطر 138 |
| **`G4` لم يُغلق** — الاختبار المعطَّل بـ`return` داخل جسمه **لا يراه الجرد**، و`rust:mutants` يقيس **ما يغطّيه سجلّ التحويلات وحده** | `gates-report.md` §٥ · سطر 138 · قاعدة المشروع:٣٤٣ |
| **`ci.yml` لم يُشغَّل على GitHub** (لا دفع) ⇒ الخطوات الجديدة مقيسة **محلياً فقط** | `gates-report.md` §٥ · سطر 138 |
| **مصفوفة الاختبار اليدوي لم تُمَلأ** (تحتاج ويندوز + بوتاً حقيقيّاً + هاتفاً) | `x3-tg-report.md` §٨ · سطر 269 |
| **تذبذب مقيس في اختبار قائم** — `telegram::tests::t3_two_users_run_in_parallel_up_to_the_global_cap`: **سببُه التنازع على الجهاز لا المنتج** (٤٢ تشغيلاً ساكناً ⇒ صفر فشل؛ وتحت تنازع حقيقي **١ فشل من ٣٦**) ⇒ «صفر فاشل» **ليس قابلاً لإعادة الإنتاج بثقة**، و**لم يُغلق** | `x4-cancel-report.md` §٧ · سطر 603 (بند ٨) · `docs/BACKLOG-0.3.md:359` (ك-١) |

---

## ٣) مؤجَّل إلى 0.3 — **مُعلَن برقم سطره وسبب تأجيله**

| البند | رقم السطر | سبب التأجيل |
|---|---|---|
| **سقف طوابير تلغرام لكل مستخدم** — الافتراضيّ **١** والسقف المسموح **٢** | `ARCHIVE/0.2.9PLAN.md:341` (القرار ١١) · `docs/BACKLOG-0.3.md:281` (ز-٩/٩) و`:299` (ز-١٠/٢) | يحتاج إعادة هيكلة (مُوزِّع FIFO لكل `user_id` بدل مسار عالمي واحد)، **والسقف يُقاس قبل تثبيته**: أسوأ حالة معروفة **7943 MiB من 8192** (DirectML · ملف طويل) ⇒ الهامش **249 MiB**، والثالث **لم يُقَس**. فالتثبيت على رقم غير مقيس ممنوع |
| **توسيع `pace_group_send` للوسائط** | `src-tauri/src/telegram.rs:797` (والتعليل في `:788-796`) | الكلفة **زمن انتظار لا يُقاس بلا مجموعة حقيقية** (يحتاج بوتاً وchat حقيقيّاً = `x3-tg-report.md` §٨)، والتصميم الحالي يضع الانتظار على **خيط الكاتب وحده** فلا يحجب `/kill` — **وتوسيعه للوسائط يغيّر ترتيب ما يصل السلك** (وهو سلوك مُختبَر) |
| **تكرار `stopPoll`** (`content.js:306-307`) | `ARCHIVE/review-0.2.9-cline.md:259` و`:233` (البند ١١) | «نظافة تحرير» بلا أثر سلوكي مقيس؛ ويحتاج **مُفسَداً** («حذف النداء الصحيح») حتى لا يكون الحذف تخميناً — والملف **محروس** فتغييره يستلزم تحديث الحارس معه |
| **توافق قديم/جديد في الإضافة** (إضافة قديمة + تطبيق جديد · وتطبيق قديم + إضافة جديدة) | `ARCHIVE/0.2.9PLAN.md:178-181` و`:197-198` (صفّا المصفوفة) | الإضافة **تراكمية** في 0.2.9 (`page_kept` و`mode` حقول **جديدة**، و`kept` **يحفظ معناه**) ⇒ التقادم **آمن الآن**. وتغيير **معنى** حقل مستقبلاً يلزمه `state_version` في حالة الجسر — و**صفّا المصفوفة يحتاجان جهازاً ويد المالك**، فلم يُملآ |
| **نقل الاستدلال إلى عملية عاملة** (ليُقطع نداء ONNX الجاري) | `ARCHIVE/0.2.9PLAN.md:333` (القرار ٢) | قطع نداء ONNX **غير ممكن داخل العملية**؛ والإلغاء اليوم **تعاوني عند حدود المراحل** مع قتل فوري لـ`ffmpeg`/`yt-dlp`. ويستلزم عملية عاملة + سِجلّ PIDs + إثبات «صفر عمليات يتيمة» |

---

## ٤) ما لا تقوله هذه الوثيقة (تحديثات صريحة)

1. **لا وسم ولا نشر ولا دفع** في هذه الجولة — كل ما هنا **محلّي**، و`main` **لم يُلمس**.
2. **`main` المحلّي انزاح عن البعيد**: `git rev-parse main` = `ada7b2c` · `git rev-parse origin/main`
   = `0a6f4e6` ⇒ **«محلّي وغير مدفوع»**، ولا يُبلَّغ عنه كحالة المنشور (قاعدة المشروع §١٣).
3. **بنود §١-٥ · §١-٦ · §١-٨ · §١-٩ · §١-١٠ (جزء `b348c38`) ليست في فرع التكامل بعد**،
   وقد ذُكرت بأرقام فروعها صراحةً. ومعنى ذلك أن **0.2.9 كما هو اليوم لا يحمل** حذف المسارات
   الميتة ولا إصلاح `stderr` ولا ترجمة رسائل الواجهة.
4. **SSRF ليس من عمل 0.2.9** (§١-١١) — أُثبت بفرق فارغ على `yt_dlp.rs` وبأن مُدخِله سلفٌ لـ`main`.
5. **الأرقام محلية على هذا الجهاز** (‏`cargo 1.95.0` · `clippy 0.1.95`) ولا تُنسب إلى CI.
