# خطة بناء HaramLite-RS — برنامج إزالة الموسيقى بخفة وسرعة

> **الحالة: معتمَدة ✓** — قرارات المالك: واجهة **Tauri v2**، المشروع في `haramlite-rs` بجوار الحالي، الواجهة **ثنائية عربية/إنجليزية**
> التاريخ: 2026-08-23

---

## 1) الرؤية

برنامج سطح مكتب جديد **بلغة Rust** يجمع بين:
- **خفة وسرعة `harammute lite`** (نموذج ONNX بحجم 63MB بدلاً من 639MB، تشغيل فوري، حجم تثبيت صغير)
- **ميزات `HaramMute Desktop`** (وضعان + سلسلة مؤثرات + yt-dlp بتحديث آمن + معالجة كل الصيغ)

النتيجة المستهدفة: ملف تنفيذي واحد صغير (~15-25MB) بدون بايثون على الإطلاق، استهلاك ذاكرة أقل بعشر مرات، ومعالجة أسرع على CPU.

---

## 2) الدروس المستخلصة من تحليل `harammute lite`

| الجانب | ما في haramlite | القرار في HaramLite-RS |
|---|---|---|
| النموذج | `UVR-MDX-NET-Voc_FT.onnx` — **63.7MB** | ✅ نفس النموذج (جودة مجربة وخفة عشر أضعاف BS-RoFormer) |
| التشغيل | خادم FastAPI محلي + متصفح | 🔄 ناقش أدناه (سؤال A) |
| التبعيات | بايثون كامل + deno + ffmpeg (~500MB) | ❌ لا بايثون نهائياً — Rust أصلي + ffmpeg.exe فقط |
| stderr=None في النافذة | حلّه مسبقاً بكتابة log file | ✅ نبني عليه: كل المخرجات إلى Log Window دائماً |
| التحديث | ⚠️ **GitHub PAT مكشوف داخل updater.py** | ❌ صفر توكنات مضمّنة — تحديث عام بلا مصادقة + SHA-256 |

> ⚠️ **إجراء أمني عاجل خارج المشروع:** التوكن في `harammute lite/updater.py` سايب —
> ألغِه من GitHub → Settings → Developer settings → Fine-grained tokens.

---

## 3) المكدس التقني (كلها Rust أصلية)

### المحرك الصوتي
| المهمة | الأداة |
|---|---|
| تشغيل نموذج ONNX | `ort` = ربط رسمي لـ ONNX Runtime (يدعم DirectML لأي GPU + CUDA تلقائياً) |
| STFT/ISTFT وتقطيع الصوت | `rustfft` + `realfft` |
| قراءة/كتابة WAV/FLAC | `hound` (WAV) + `claxon` (FLAC) |
| معالجة PCM | `dasp` أو معالجة يدوية مباشرة (f32 buffers) |

### سلسلة المؤثرات (DSP نقي في Rust — بدون مكتبات خارجية)
| المؤثر | التنفيذ |
|---|---|
| Reverb | Schroeder/Freeverb (comb + allpass filters) |
| Delay | تأخير بخطوط تغذية راجعة (feedback taps) pingpong |
| Highpass + EQ | biquad filters (RBJ cookbook) |
| Compressor | RMS envelope + gain computer |
| Loudness normalize | LUFS (K-weighting + gated measurement) هدف -14 LUFS |
| Limiter | look-ahead brickwall -1 dBTP |
| قص الصمت | نوافذ RMS + عتبة تكيّفية (للأغاني فقط) |
| De-muffle | highpass لطيف + exciter توافقي خفيف |

### البنية التحتية
| المهمة | الأداة |
|---|---|
| ffprobe/ffmpeg (فحص، تحويل، استخراج) | subprocess لملفات ffmpeg.exe المرفقة |
| تنزيل YouTube | yt-dlp.exe مرفق (نسخة احتياطية محلية دائمة) |
| تحديث yt-dlp الآمن | GitHub Releases → SHA-256 → تنزيل إلى `.new` → تبديل ذرّي → نسخة `.previous` للتراجع (تصحيح أخطاء BUG-02 السابقة بالتصميم) |
| HTTP | `reqwest` (blocking في خيوط العمل) |
| السجلات | `tracing` + طبقة مخصصة تبثّ مباشرة إلى نافذة Log + ملف دوّار |
| أخطاء Rust الصامتة | `std::panic::set_hook` يلتقط أي panic ويكتبه في السجل والواجهة مع موقع الملف/السطر |
| الواجهة | 🔄 سؤال A أدناه |

---

## 4) معمارية المعالجة (Pipeline)

```
إدخال (أي صيغة)
   │
   ▼
[ffprobe] فحص الحقيقة ──► إصلاح تلقائي: mp4 صوتي؟ mkv بصوت فقط؟ ──► توحيد WAV 44.1k stereo
   │
   ▼
┌─ وضع «الأغاني» ─────────────────────────────────────────────┐
│ 1. فصل: UVR-MDX-NET-Voc (ort/GPU إن وجد) ← غناء + مرافق     │
│ 2. المؤثرات على الغناء: de-muffle → highpass → exciter      │
│    → reverb → delay pingpong → compressor                  │
│ 3. قص مقاطع الصمت (نوافذ RMS)                              │
│ 4. Mastering: LUFS -14 → limiter -1dBTP                    │
└──────────────────────────────────────────────────────────────┘
┌─ وضع «المقطع العادي» ───────────────────────────────────────┐
│  فصل فقط: إزالة الموسيقى وإخراج الغناء النظيف               │
└──────────────────────────────────────────────────────────────┘
   │
   ▼
[ترميز حسب الطلب] MP4 (فيديو أصلي محفوظ) / MP3 320k / WAV / FLAC
```

- **تقطيع متوازٍ** مثل HaramMute (chunks على خيوط rayon) مع دمج تداخلي (crossfade) — سرعة أعلى من التشغيل التسلسلي.
- شريط تقدم حقيقي محسوب من عدد الإطارات المنجزة داخل النموذج (لا مؤشرات وهمية).

---

## 5) المواصفات الوظيفية (حسب طلبك حرفياً)

1. **تحميل yt-dlp**: لصق رابط → جودة تلقائية → مع تحديث ذاتي آمن + نسخة محلية احتياطية تعمل حتى لو فشل التحديث أو الإنترنت.
2. **وضعان فقط في الواجهة**: «🎵 أغنية» و«🎬 مقطع عادي» — بلا قوائم معقدة.
3. **مؤثرات الأغاني**: reverb/delay/exciter/EQ/compressor لإعادة الحياة بعد سحب الموسيقى وإزالة الكتمة + **قص الصمت تلقائياً**.
4. **المقطع العادي**: إزالة موسيقى فقط، بلا مؤثرات ولا قص.
5. **الاستخراج**: فيديو → MP4 (صوت معالج + فيديو أصلي) أو MP3؛ صوت → MP3/WAV/FLAC.
6. **قبول كل الصيغ** + **الإصلاح الذكي**: ffprobe يحدد الطبيعة الفعلية للملف (صوت مقنّع بامتداد فيديو والعكس) ويعالجه بالمسار الصحيح دون أخطاء.
7. **خروج أخف/أسرع**: ترميز فعّال + خيارات عمق/bitrate.
8. **نافذة Log دائمة**: مستويات (INFO/WARN/ERROR) + تصفية + زر «فتح مجلد السجل» + التقاط panics مع file:line — لأن أخطاء Rust كثيراً ما تكون صامتة.

---

## 6) مراحل التنفيذ (مع معيار قبول لكل مرحلة)

| المرحلة | المحتوى | معيار القبول |
|---|---|---|
| **M0** هيكل المشروع + Logging | Cargo workspace، tracing→UI+file، panic hook | نافذة Log تعرض سجل حي وpanics الاختبارية |
| **M1** خط أنابيب الوسائط | ffprobe/ffmpeg wrapper + إصلاح الملفات المغلوطة + استخراج MP4/MP3 | ملف mp4 صوتي يُكتشف ويُعالج كصوت ✓ |
| **M2** محرك الفصل | ort + UVR-MDX-NET-Voc + STFT/تقطيع/دمج | موجة 440Hz تنفصل بنجاح، خرج غير فارغ ✓ |
| **M3** المؤثرات | سلسلة DSP كاملة + قص الصمت + LUFS | A/B مسموع + قياسات LUFS مطابقة ✓ |
| **M4** الواجهة | وضعان + اختيار صيغة + شريط تقدم حقيقي + نافذة Log | دورة كاملة بالماوس بلا أوامر نصية ✓ |
| **M5** yt-dlp + التحديث الآمن | تنزيل + تحقق SHA-256 + تبديل ذرّي + fallback | قطع الإنترنت أثناء التحديث لا يكسر النسخة العاملة ✓ |
| **M6** التغليف | بناء exe واحد + ffmpeg + yt-dlp + نموذج ONNX (63MB)، مثبت Inno Setup | تثبيت نظيف على جهاز بلا أي متطلبات مسبقة ✓ |

---

## 7) المخاطر والبدائل

| الخطر | الاحتمال | البديل الجاهز |
|---|---|---|
| جودة UVR-MDX-NET-Voc أقل من BS-RoFormer | مؤكدة نسبياً | خيار «جودة قصوى» لاحقاً بنموذج MDXC آخر أكبر عبر نفس محرك ort |
| تعقيد STFT المطابق لمعالجة النموذج | متوسط | الالتزام بأرقام UVR الرسمية (chunk_size=262144, margin=...) كما في audio-separator |
| عرض العربية في الواجهة | حسب قرار A | الحلول مدرجة في الخيارات أدناه |
| بطء CPU | مقبول (نموذج 63MB) | DirectML تلقائي عند وجود أي DX12 GPU |

---

## 8) قرارات بانتظار موافقتك

- **A) نوع الواجهة**: (1) Tauri v2 — واجهة ويب حديثة، عربية RTL مثالية، لكن يتطلب أدوات Node للأمامية؛ (2) egui — Rust صافٍ، ملف واحد خفيف جداً، عربية جيدة عبر cosmic-text، أبسط في البناء.
- **B) اسم ومكان المشروع**: `C:\Code-backup\HaramMute Desktop III\haramlite-rs` ؟
- **C) اللغة في الواجهة**: عربية فقط / ثنائية عربية-إنجليزية؟
