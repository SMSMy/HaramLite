# سجل التغييرات

كل التغييرات الملحوظة في هذا المشروع. التنسيق مستوحى من
[Keep a Changelog](https://keepachangelog.com/)، والترقيم دلالي (SemVer).

> - تفاصيل **0.2.1** و**0.2.2** و**0.2.3** هي نصوص نشراتها الفعلية في
>   [`docs/RELEASE-0.2.1.md`](docs/RELEASE-0.2.1.md) و[`-0.2.2`](docs/RELEASE-0.2.2.md)
>   و[`-0.2.3`](docs/RELEASE-0.2.3.md)، وكذلك **0.2.4** و**0.2.5** و**0.2.6** في
>   [`-0.2.4`](docs/RELEASE-0.2.4.md) و[`-0.2.5`](docs/RELEASE-0.2.5.md) و[`-0.2.6`](docs/RELEASE-0.2.6.md)
>   و**0.2.7** في [`-0.2.7`](docs/RELEASE-0.2.7.md).
> - لنسختَي **0.1.x** و**0.2.0** لا توجد ملاحظات إصدار مفصّلة في المستودع،
>   والمدخلات أدناه مأخوذة من وسم الإصدار نفسه — فلا تُقرأ كقائمة ميزات كاملة.
> - التحديث الذاتي **موقوف** في الإصدارات 0.2.1–0.2.4 (مفتاح توقيع الإصدارات غير
>   مضبوط في المستودع)، فالترقية بتنزيل المثبّت الجديد وتثبيته فوق الحالي. وفي
>   **0.2.5** يعمل **فحص** الإصدار وفتح صفحة التنزيل من داخل الإعدادات، أما
>   التحديث بنقرة واحدة (الموقّع) فمؤجَّل.

## [0.2.7] — 2026-09-17

**إصدار بوّابات لا ميزات**: لم تتغيّر طريقة الفصل ولا الواجهة — ما تغيّر أن كل ادّعاء صار خلف
بوّابة تعمل آلياً أو قياس مسجّل بتاريخ وجهاز. والمُخرَج المرئي للمستخدم واحد: إصلاح `--probe`.

### أُصلح
- **`haramlite --probe` كان يخرج 0 على ملف تالف** (مفقود أو صفر بايت) ويطبع تصنيفاً فارغاً:
  `ffprobe` يطبع `{}` على stdout عند الفشل فيُحلَّل بنجاح ويُقرأ الفراغ «وسائط فارغة». صار
  **الفراغ فشلاً**، وبرسالة تسمّي الملف. **والاختبار كُتب قبل الإصلاح وسقط عليه** — والمصفوفة
  اليدوية هي التي كشفته (صفّ 3.1).

### أُضيف
- **البوّابات من تسع إلى ثلاث عشرة** (تفاصيلها في `docs/CONTRIBUTING.md`): `cargo fmt --check` ·
  حارس الهندسة (`18 صندوقاً × 6 حالات نافذة` بمتصفّح بلا واجهة) · **65 اختبار واجهة** ·
  `build-info.json` + **SBOM بصيغة CycloneDX** (804 مكوّنات) · `cargo-deny` + CodeQL أسبوعياً ·
  و**`matrix:check`**.
- **مصفوفة الاختبار اليدوي**: 55 صفاً و20 إلزامية، **وللعشرين كلها سجلّ** (تاريخ + جهاز + نتيجة)،
  ونُقلت إلى `qa/` **خارج مجلد النشر** — لأن `docs/` جذر موقع GitHub Pages وكان الملف يُنشر فعلاً.
- **`scripts/fetch-resources.cjs`**: تنزيل الأصول المرفقة بتحقّق بصمة (`--verify-only` · `--list` ·
  `--cuda`)، و`docs/STATUS.md` و`docs/WHY-WINDOWS-WARNS.md`.

### تغيّر
- **`rustfmt` على الشجرة كلها** (36 ملفاً · 410 كتل ⟶ 0) قبل أي تغيير منطقي، مع بوّابة تمنع الرجوع.
- **`rustls` 0.23.43 ⟶ 0.23.45**: ثغرة حقيقية (‏RUSTSEC-2026-0285) بمسار مُثبَت في بناء ويندوز.
- **`src-tauri/Cargo.toml` له حقل رخصة** (`license = "MIT"`) بدل عملaround في `deny.toml`.
- **سياسة ج-٢ نصّاً**: «كل عطل ميداني ⇒ اختبار يفشل على الكود القديم ← إصلاح ← يبقى للأبد».

### ملاحظة على الأصول
- **بناءان تحت الرقم نفسه** (الأول لم يُنشر): الثاني يحوي إصلاح `--probe`. **والأول مذكور ببصمته
  ومحفوظ** — والبصمات كلها في [`docs/RELEASE-0.2.7.md`](docs/RELEASE-0.2.7.md).
- **الترقية تنزيلٌ وتثبيت**: التحديث الذاتي موقوف (`active: false`)؛ وما يعمل هو **فحص** الإصدار.

## [0.2.6] — 2026-09-16

### تغيّر
- **تحصين أمني بأربعة بنود** (كل بند مغلق باختبار **سلبي** يُثبت أن الحماية ترى الخطأ):
  نطاق ملفات الواجهة لم يبقَ مفتوحاً على **كل القرص** (صار السماح **زمنياً** للملف الواحد
  الذي يقرؤه الخلف فعلاً) · **١٦ بصمة لمكتبات CUDA مثبَّتة في التنفيذي** بدل مقارنتها
  بمانيفست يُنزَّل من الشبكة · طلب إظهار النافذة على المنفذ المحلي صار **مصادَقاً برمز
  ٣٢ بايتاً** من عشوائية النظام · ورمز اقتران تيليجرام من **عشوائية نظام** بدل مولّد غير سرّي.
- **مفتاح حقيقي لتحديث `yt-dlp` تلقائياً** (الإعدادات ← الأدوات والمساعدة) بمفتاح افتراضه
  التشغيل، و**تلميح** يشرح ما هي `yt-dlp`، و**سطر تحذير عند الإطفاء** يعرض إصدار نسختك
  المحلية ويشرح الثمن. وإطفاؤه يوقف الفحص **قبل أي نداء شبكي**.
- **زرّ تحديث `yt-dlp` صار زراً حقيقياً** (كان نصّاً يُقرأ كشارة حالة) و**متاحاً دائماً**:
  ضغطه يتجاوز كاش الـ٢٤ ساعة فيجلب الأحدث في الحال.
- **حالات CUDA لكل واحدة رسالتها**: لم تُطلب · لا كرت · كرت بلا مكتبات · مكتبات ناقصة
  (تسمّي الناقص بالاسم) · جاهزة. وسلسلة المحاولات (‏CUDA ← DirectML ← CPU) لم تتغيّر.
  وأُضيف عمود `cuda` إلى `--check` (لا يُحمّر الفحص) وعلم `--cuda` لسطر الأوامر.
- **`src/main.ts`: ٣٣٣٢ ⟶ ٢٧٩ سطراً** في ١٦ وحدة، بتكافؤ مُثبَت (مجموعة السلاسل النصية في
  الحزمة المبنية مطابقة تماماً: ‏1,124 = 1,124 · وملف الأنماط بنفس البصمة · و٣٤ نداء أمر
  و١٥ حدثاً و١١٤ معرّف واجهة و٢١ مفتاح تخزين بعددها قبل التقسيم).
- **بوابة بناء في CI** كانت غائبة تماماً: خط التكامل كان يفحص الأنواع **ولا يبني الحزمة**.
  أُضيفت `pnpm build:web` بزمن مقيس **2.5s** ومُفسَدها يُسقطها.

### أُصلح
- **التحقّق من رابط التنزيل**: أُضيف فحص مخطّط ورفض كل عنوان محلي/خاص، **وشمل الصيغ
  المتكافئة** لـIPv6 (‏`[0:0:0:0:0:0:0:1]` · `[::]` · `[::ffff:7f00:1]`) و**6to4** و
  **NAT64** و**multicast** — وقد كشف المشرف ثلاث صور كانت تمرّ لأن الفحص كان يقارن
  **بادئة النصّ** لا قيمة العنوان، فأُصلح بمحلّل حقيقي.
- **`fsync` قبل كل إعادة تسمية** في مواضع الكتابة الأربعة (كان انقطاع الكهرباء في اللحظة
  الحرجة يُفقد آخر كتابة).
- **زرّ التحديث لم يبقَ ميتاً**: كان معطَّلاً ما دام التحديث التلقائي مشتعلاً، بينما سطر
  بطاقة التنزيل يوجّه المستخدم إليه عند الفشل.
- **تلميح `yt-dlp`** لم يبقَ يدّعي حالة يمكن للمفتاح أن يُبطلها.
- **الخروج الصامت حين تعمل نسخة أقدم أُزيل** (تكملة لبند التحصين ٣): كان من شغّل النسخة
  الجديدة وهناك نسخة أقدم في الشريط **لا يرى شيئاً أبداً** — القديمة تتلقّى طلب الإظهار
  بصيغة لا تعرفها فتتجاهله، والجديدة تقرأ ذلك ثم تخرج بلا رسالة. الآن **الإقرار وحده**
  يعني أن نافذة ستظهر (فيُخرج صامتاً برمز 0)، وأي جواب آخر — رفض · إغلاق بلا بايت · مهلة ·
  **أو غياب ملف الرمز** (حالة النسخة الأقدم بعينها) — يُعرض فيه **صندوق نظام أصلي** برسالة
  عربية تسمّي السبب والحلّ، ويُخرج التطبيق برمز **1**. وسبعة اختبارات على مقابس حقيقية
  بمنافذ عابرة، ومُفسِدها يُسقط أربعة منها.
- **لوحة الإعدادات كانت تخرج من النافذة في الواجهة الإنجليزية**: كانت مثبَّتة بـ`left-0`
  **فيزيائية**، وحاويتها في الشريط تقع عند **الحافة اليمنى** في الإنجليزية (‏LTR) ⇒ **٢٠٨px**
  من اللوحة خارج النافذة (مقيس على نافذة عرضها ١٠٨٤px)، فيُرى شريط مقطوع لا لوحة. والعربية
  (‏RTL) تنقلب فيها الحافة فيقع الامتداد إلى داخل النافذة — **ولهذا لم يُرَ العطل في العربية
  قطّ**. والإصلاح **خاصية منطقية واحدة**: `left-0` ⟶ `end-0` (‏`inset-inline-end: 0`) ⇒
  **صفر تجاوز في الاتجاهين**، بلا تغيير في العربية.

### ملاحظات معروفة (معلنة قبل أن تسأل)
- **سقف تيليجرام على مقاطع 720p الطويلة**: مُرمِّز Media Foundation لا ينزل تحت أرضية
  مرتبطة بالدقّة (‏≈1285 kbps)، فالمقطع الطويل قد يُرسَل **صوتاً فقط**. والعمل حوله يدوياً:
  دقّة **480p**. **والاختيار التلقائي للدقّة انتقل إلى 0.2.7.**
- **مقاطع HDR**: `-pix_fmt yuv420p` لا يزيل وسوم HDR ⇒ قد تُعرض باهتة في مشغّلات تحترم
  الوسوم. و`tonemap` مؤجَّل بوعي إلى إصدار يُقاس فيه.
- **مستخدمو CUDA**: بصمات الملفات الستة عشر صارت مثبَّتة، فمن عنده نسخ من مانيفست أقدم
  **ستُعاد تنزيلها** تلقائياً (إصلاح ذاتي مقصود) — عمل لمرة واحدة بعد التحديث.

### مؤجَّل بوعي
التوقيع الرقمي للحزم · `tonemap` لمقاطع HDR · الاختيار التلقائي للدقّة من الميزانية ·
مُحدِّث النقرة الواحدة الموقَّع · مُرمِّزات AMD/Intel · بناء ZIP محمول · إعادة هيكلة
`process_file` · الخطوط الثلاثة الجديدة.

## [0.2.5] — 2026-09-15

### تغيّر
- **المحرّك صار بناء FFmpeg بصيغة LGPLv3** بدل GPLv3: أُزيل `libx264` من الكود
  كاملاً. التصدير العادي يستعمل `h264_mf -rate_control quality -quality 85`
  (وNVENC أولاً على أجهزة NVIDIA مع تراجع تلقائي)، ومسار سقف الحجم يستعمل
  `h264_mf` بوضع مقيَّد بالمعدّل. و`-pix_fmt yuv420p` مثبَّت صراحةً على المسارين.
- **المثبّت أصغر**: `setup.exe` **220.2 ⟶ 188.0 MB** (‏−14.6٪) و`msi`
  **431.2 ⟶ 371.4 MB** (‏−13.9٪).
- **الجودة أولاً — والملف قد يكبر**: مقابل `libx264 crf18` قِيس تكافؤ أو أفضل
  قليلاً في VMAF (‏+0.06…+0.56) مع فرق حجم **−3.6٪ / +37.3٪ / +83.5٪** على ثلاثة
  محتويات (مراجع مضغوطة ⇒ النسب نسبية لا مطلقة).
- **زرّ «التحقق من التحديثات» عاد ويعمل**: يقرأ آخر إصدار منشور من واجهة GitHub
  ويقارن الإصدارات دلالياً، وإصدار التطبيق من التطبيق نفسه. والفحص الصامت عند
  الإقلاع خلف كاش ٢٤ ساعة (حدّ GitHub ٦٠ طلباً/الساعة لكل IP).
- **قائمة الإعدادات صارت معتمة** بدل الزجاجية (تباين التسميات ‎10.83:1، وسدّ
  النفاذ: على خلفية فاتحة نظرية ‎1.69 ⟶ 5.24)، **وأُزيلت الرموز التعبيرية** من
  نصوصها وحلّت محلها أيقونات Material Symbols.
- **سياسة التبعيات**: تجميع الدنيا والرقعة في طلب واحد لكل منظومة، وتجاهل
  الترقيات الكبرى، وتجاهل صريح لمحرّك الفصل `ort` (نسخة المزوّد يجب أن تطابق بناء
  ORT المضمَّن بدقّة).

### أُصلح
- **`+faststart` في مسار التصدير**: كان `moov` في آخر الملف فيبدو أول تشغيل
  «متجمّداً» حتى يكتمل التنزيل (‏`moov@40` قبل `mdat@8234` الآن، بنفس حجم الملف).
- **تنظيف تحذيرات `clippy` الميكانيكية** بلا تغيير سلوك، والاستثناءات موثَّقة
  بأسبابها في `docs/AUDIT.md` — منها شرط مقصود لـNaN أُعيد بعد أن «نظّفه» تحويل
  ميكانيكي كان سيغيّر الفرع.

### ملاحظات معروفة (معلنة قبل أن تسأل)
- **سقف تيليجرام على المقاطع الطويلة**: مُرمِّز Media Foundation لا ينزل تحت
  أرضية مرتبطة بالدقّة (≈1.3 Mbps على 720p)، فمقاطع أطول من نحو ٤ دقائق قد
  تُرسَل **صوتاً فقط** بدل فيديو. والأرضية تتبع الدقّة (‏480p ⟶ ≈0.44 Mbps)،
  فاختيار الدقّة من الميزانية خيار قيد الدراسة.
- **مقاطع HDR**: تُحوَّل إلى 8-بت مع **إبقاء وسوم HDR** ⇒ عرض باهت في بعض
  المشغّلات. ومعالجة النطاق الديناميكي (`tonemap`) مؤجَّلة.

### مؤجَّل بوعي
مُحدِّث النقرة الواحدة الموقّع · `tonemap` · مُرمِّزات AMD/Intel (`qsv`/`amf`)
بانتظار جدول قياس · بناء ZIP محمول · حارس أيقونات التطبيق · إعادة هيكلة
`process_file` · الخطوط الثلاثة الجديدة.

## [0.2.4] — 2026-09-15

### أُصلح
- **قصّ الصمت كان يُتلف الملف** عند وجود قيم غير منتهية (NaN/Inf) في نحو 10%
  من نوافذ القياس: صارت العتبة لا نهائية فاعتُبر كل نافذة سليمة «صامتة».
  الآن العتبة من النوافذ السليمة وحدها، وإن لم توجد فلا قصّ.
- **زرّ الإلغاء لم يكن يوقف الترميز**: كان التطبيق يتجاهل نتيجة الفحص الأخير
  فيُعلن النجاح ثم يمضي في مسار حذف المصدر المنزَّل. الآن يُلغي ويحذف ما كُتب.
- **ترتيب حفظ الإعدادات**: كان يُنشر في الذاكرة قبل الكتابة على القرص، فيتخلّف
  التطبيق عن الملف عند فشل الكتابة.
- **أُزيل زرّ «التحقق من التحديثات»** لأنه لا يمكن أن ينجح (لا ملف تحديثات منشور).
- **تسرّب المجلدات المؤقتة** وملفات التنزيل الجزئية عند أي مسار فشل مبكر.
- **أقفال قابلة للتسمّم** كانت تحوّل عطلاً في خيط إلى انهيار متكرر، مع توحيد
  ترتيب القفلين المتشابكين.
- **مسح صوت الصفحة** لم يعد يمسّ شيئاً خارج مجلده، والاستخراج قبل المسح.
- **اختبار ختم الرموز** كان يمنح ثقة زائفة (يؤكّد على نصّ داخل الاختبار وحده).

### تغيّر
- **الإعدادات في خمس مجموعات معنونة** بدل كتلة واحدة.
- **تعريب كامل**: 192 مفتاحاً لكل لغة (كانت 128)، وصفر نصّ عربي في الواجهة
  الإنجليزية، وأسماء مفهومة للأزرار الأيقونية.
- **إتاحة**: التباين يمرّ AA على 129 عنصراً (كان يفشل على 12)، ولوحة المفاتيح
  تعمل على منطقة الإفلات، والحوارات بأدوارها وحصر التركيز و`Esc`.
- **الموقع**: شارة المستودع في الشريط الأعلى، وتذييل منظَّم في ثلاثة أعمدة،
  وبطاقة UVR، وتصحيح ادعاءَي المساحة وزمن القراءة، وتصحيح نسبة المشروع الأصلي
  إلى «الملهِم الأول» (المشترك فكرة ومجال وأدوات، لا كود).

### الأداء
- قراءة الملف الصوتي: الذروة **213.5 ⟶ 112.5 MB** على ملف 300 ثانية (−47%)،
  والبيانات بتّية مطابقة.
- نافذة الفصل تُبنى مرة واحدة بدل كل مقطع.

### حُذف
- **`cause_test_panic`**: أمر اختبار كان **يُشحن في كل نسخة إصدار** بلا مستهلك.

### البنية
- **حارس تطابق الإصدار** (`pnpm versions:check`) ورافع إصدار للأربعة ملفات.
- **أُزيل لغم كان يُسقط `cargo test` كلياً**: مانيفست comctl32 صار يُربط بثنائي
  اختبار المكتبة وحده (`0xc0000139` كان يمنع إقلاعه).
- حلقة النسخة الواحدة لم تعد تدور بلا نوم عند فشل دائم.
- `SECURITY.md` · `.gitattributes` · `dependabot.yml`، ورفع أصول النشرة يستبدل.

## [0.2.3] — 2026-09-14

### تغيّر
- الفصل صار على **الملف كله** كما في UVR5 (`source = demix(mix)`) بدل المقاطع
  المشتبهة وحدها — فالملف الذي بلا مقاطع صمت كان يخرج **بلا أي فصل**، وصار
  يُفصل كاملاً؛ وتحليل المقاطع بقي **للتشخيص في السجل** فقط.
- القياس على المقطع نفسه والنموذج نفسه: ارتباط الغلافين **0.9999** مع UVR5،
  وصفر نوافذ غناء مفقودة.
- ملاحظة مقصودة: وضع «أغنية» ما زال يطبّق سلسلة التحسين بعده (قصّ الصمت
  والتطبيع إلى ‏−14 LUFS).

### أُضيف
- سطر تشخيص لمستوى الدخل مع كل فصل:
  `level: peak=… (… dBFS) attenuated=…` — يفصل بين شكوى المدخل وشكوى المحرّك.

### أُصلح
- حُذف كود اللصق الميت (`splice_span` واختباراه) وقارئ عنوان ميت (`job_url_of`
  واختباره) واستيرادان غير مستعملين — البناء الآن بلا تحذيرات.

## [0.2.2] — 2026-09-12

### أُضيف
- **التشغيل مع بدء تشغيل ويندوز في الخلفية**: سؤال مرة واحدة بعد التثبيت، ويُكتب
  في `HKCU\...\Run` سطر `"<مسار البرنامج>" --hidden-start` — والريجستري هو
  المصدر الوحيد للحقيقة (فمسار قديم بعد نقل أو ترقية لا يُعدّ مفعّلاً)، وبلا
  صلاحيات مسؤول.
- **البرنامج يعرف إن كانت الإضافة مثبَّتة**: مضيف الاتصال يسجّل أصل كل إضافة
  تتصل به، وإن لم يتصل متصفح قط يعرض الإعدادات رابط صفحة الإضافة.
- **اختيار الوضع من نافذة الإضافة**: «مقطع عادي» أو «وضع أغنية» لكل طلب على حدة،
  ومتوافق للخلف (الطلب بلا وضع يسلك السلوك السابق).

### أُصلح
- قائمة الإعدادات صارت **قابلة للتمرير** بحدّ ارتفاع، و«حول البرنامج» انتقل
  ليكون آخر عنصر فيها.
- سلوك أنظف عند إيقاف الدفعات وإلغاء المعالجة.

## [0.2.1] — 2026-09-11

### أُضيف
- **بوت تيليجرام (اختياري)**: أرسل رابطاً أو ارفع ملفاً ← معالجة على جهازك ←
  إعادة الناتج. اقتران برمز من ٦ أرقام صالح ١٠ دقائق (٥ محاولات ثم يُبطَل)،
  وأي حساب آخر يُتجاهل. وحدود تيليجرام معلنة: إرسال حتى **50MB** ولا تنزيل أكثر
  من **20MB** من ملف مُرسَل (الروابط بلا حد)، وخادم Bot API محلي يرفع السقف إلى
  **2GB**. وخيار «إرسال الصوت فقط (MP3) دائماً».
- **أيقونة الشريط**: زر الإغلاق يُخفي إلى الشريط بدل إنهاء العملية، والإغلاق
  الكامل من قائمة الأيقونة، والنقر عليها يعيد النافذة.
- **تكامل المتصفح مكتملاً**: مشاهدة مفلترة تشغّل الصوت المعالَج داخل الصفحة
  متزامناً مع الفيديو، وتتخطّى مقاطع الصمت التي يخلّفها كتم الموسيقى.

### الأمن
- توكن البوت و`api_hash` **مشفّران بـDPAPI** (مرتبطان بالحساب والجهاز) — لا نصّ
  صريح على القرص، ولا نسخة في متصفح الواجهة.
- مجلد بيانات البرنامج انتقل من `%APPDATA%` (المتجوّل) إلى `%LOCALAPPDATA%`.
- تنقيح التوكن من كل رسالة خطأ، فلا يظهر في السجل ولا في محادثة تيليجرام.

### أُصلح
- حذف المصادر المؤقتة بعد المعالجة، وتنظيف مجلد تيليجرام المؤقت تلقائياً (كان
  يتراكم إلى مئات الميغابايت).
- إزالة تحذير مكرر في السجل عند كل وظيفة مشاهدة.
- نتائج تيليجرام تُحفظ في مجلد نتائجك (`Videos\HaramLite`) بدل مجلد مؤقت.

## [0.2.0] — 2026-09-05

- إصدار إنتاجي (`v0.2.0: production release`). لا ملاحظات إصدار مفصّلة في
  المستودع لهذه النسخة.

## [0.1.1] — 2026-08-24

- إخفاء نوافذ cmd عند التشغيل، وتمرير ذكي (smart autoscroll)، ورفع الإصدار إلى
  0.1.1 — من رسالة وسم الإصدار.

## [0.1.0] — 2026-08-24

- أول وسم منشور في المستودع (`fix: hide cmd windows on windows`). لا ملاحظات
  إصدار مفصّلة.

[0.2.3]: https://github.com/SMSMy/HaramLite/releases/tag/v0.2.3
[0.2.2]: https://github.com/SMSMy/HaramLite/releases/tag/v0.2.2
[0.2.1]: https://github.com/SMSMy/HaramLite/releases/tag/v0.2.1
[0.2.0]: https://github.com/SMSMy/HaramLite/releases/tag/v0.2.0
[0.1.1]: https://github.com/SMSMy/HaramLite/releases/tag/v0.1.1
[0.1.0]: https://github.com/SMSMy/HaramLite/releases/tag/v0.1.0
