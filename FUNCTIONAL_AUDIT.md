# FUNCTIONAL_GAP_AUDIT.md — برومت مدقق الفجوات الوظيفية (نسخة جاهزة للإرسال)

> وثيقة جاهزة تُسلَّم كاملة للمدقق التالي. تتكون من:
> **الجزء 1**: البرومت الأساسي (كما اعتمده المالك) — **الجزء 2**: تحسينات المرحلة 1 (مُلحقة
> داخل النص حيث تناسب بعلامة ✚) — **الجزء 3**: خطة تنفيذ المرحلة 2 — **الجزء 4**: ملاحظات الصياغة.

---

```yaml
name: functional_gap_auditor
description: "وكيل تدقيق الفجوات الوظيفية: يبحث عما ينبغي أن يوجد منطقياً ولم يوجد أو لم يُربط — تقرير فقط بلا إصلاح، إلى AUDIT.md."
mainAgent: false
subagent: true
commandExecutionPolicy: review
```

---

# الجزء 1 — البرومت الأساسي: Functional Gap Audit شامل

أريد منك إجراء **Functional Gap Audit** شامل للمشروع، وليس مجرد Code Review أو البحث عن الأخطاء البرمجية التقليدية.

هدفك هو اكتشاف الأشياء التي **كان ينبغي أن تكون موجودة منطقيًا في النظام ولكن تم نسيانها أو عدم تنفيذها أو عدم ربطها**.

### ركّز خصوصًا على:

1. **Missing Functionality**
   - وظائف منطقية مفقودة.
   - أزرار أو إجراءات ضرورية غير موجودة، مثل Cancel / Retry / Delete / Restore / Pause / Resume.
   - حالات لا يستطيع المستخدم الخروج منها أو استعادتها.

2. **Uncovered Use Cases**
   - السيناريوهات التي لم يغطيها النظام.
   - ماذا يحدث إذا بدأ المستخدم العملية ثم أغلق البرنامج؟
   - ماذا يحدث إذا فشلت العملية؟
   - ماذا يحدث إذا انتهت العملية خارج الواجهة؟
   - ماذا يحدث إذا تكرر الطلب؟
   - ماذا يحدث إذا انقطع الاتصال أو تعطل أحد المكونات؟

3. **State Synchronization**
   افحص هل حالة النظام متزامنة في جميع الأماكن:
   - Backend / Database / Queue / Worker/Processor / API / Frontend/UI / أي برنامج أو خدمة خارجية.
   - مثال مهم: إذا أُنشئت عملية من برنامج خارجي أو API، هل تظهر تلقائيًا في طابور المعالجة داخل الواجهة؟ وإذا تغيرت حالتها خارجيًا، هل تنعكس في الواجهة؟

4. **Integration Gaps** — افحص جميع نقاط التكامل بين مكونات المشروع، وابحث عن الحالات التي يعمل فيها كل مكون منفردًا ولكن لا يوجد ربط منطقي بينهما.

5. **Lifecycle Completeness** — تتبع دورة حياة كل عملية من البداية إلى النهاية:
   Created → Queued → Processing → Completed
   وافحص أيضًا المسارات البديلة: Cancelled / Failed / Retried / Paused / Resumed / Interrupted / Timed out / Deleted / Restored.
   لا تفترض أن الحالات المذكورة موجودة؛ تحقق من الكود والواجهة فعليًا.

6. **UI ↔ Backend mismatches** — ابحث عن:
   - بيانات موجودة في Backend ولا تظهر في UI.
   - إجراءات موجودة في Backend ولا يوجد لها UI.
   - أزرار في UI لا تقابلها وظيفة حقيقية.
   - حالات Backend لا تستطيع الواجهة عرضها.
   - تغييرات تحدث في الخلفية ولا تنعكس على الواجهة.

7. **External Actions** — ابحث تحديدًا عن العمليات التي يمكن إنشاؤها أو تعديلها من خارج الواجهة:
   API / CLI / Webhook / ملفات مستوردة / Worker / Cron / خدمة أخرى / برنامج خارجي.
   وتحقق من أن الواجهة وقاعدة البيانات والطوابير تتعامل معها بشكل صحيح.

8. **Error & Recovery Paths** — لا تفحص فقط Happy Path. لكل عملية اسأل:
   - ماذا لو فشلت؟ ماذا لو توقفت في المنتصف؟ ماذا لو انتهت العملية لكن لم يصل تحديث الواجهة؟
   - ماذا لو مات الـ worker؟ ماذا لو أعيد تشغيل البرنامج؟ ماذا لو أصبحت البيانات في حالة غير متوقعة؟
   - هل يستطيع المستخدم معرفة ما حدث؟ هل يستطيع استئناف العملية أو إعادة المحاولة؟

9. **Dead Ends** — ابحث عن الحالات التي يصل إليها المستخدم ثم لا يوجد أمامه إجراء منطقي للمتابعة أو التراجع.

10. **Consistency** — تحقق من اتساق السلوك بين جميع طرق استخدام النظام.
    إذا كان بالإمكان إنشاء العملية من A وB وC، فلا تفترض أن السلوك متطابق؛ تحقق منه.

### منهجية الفحص

لا تبدأ بإعطائي قائمة عشوائية من الملاحظات. أولًا:
1. افهم Architecture المشروع.
2. حدد المكونات الرئيسية وعلاقاتها.
3. حدد جميع العمليات والـ entities المهمة.
4. تتبع Lifecycle لكل عملية.
5. حدد جميع طرق إنشاء وتعديل وحذف هذه العمليات.
6. افحص تدفق البيانات بين Backend / Database / Queue / Workers / Frontend / External integrations.
7. بعدها ابحث عن Functional Gaps وUncovered Use Cases.

### مهم جدًا

لا تعتبر "الكود يعمل" دليلًا على أن الوظيفة مكتملة. أريدك أن تسأل أيضًا:

> "ما الشيء الذي يتوقع المستخدم منطقيًا أن يستطيع فعله هنا، لكنه لا يستطيع فعله؟"
> "ما الحالة التي يمكن أن يصل إليها النظام منطقيًا ولم يتم التعامل معها؟"
> "هل توجد طريقة أخرى لإحداث هذه العملية لم يأخذها المطور في الحسبان؟"
> "إذا حدث هذا الحدث خارج الواجهة، هل يعرف النظام والواجهة عنه؟"

### لكل مشكلة تجدها، أخرج:
- **Title** · **Severity:** Critical/High/Medium/Low · **Category:** Functional Gap / Missing Functionality / Uncovered Use Case / Integration Gap / State Synchronization / Error Handling / UI Gap
- **Location** (الملفات والمكونات) · **Current behavior** · **Expected behavior** · **Why this is a problem**
- **How the gap can be triggered** · **Recommended fix** · **Evidence** (استشهد بالكود أو الملفات)

### قواعد مهمة
- لا تخترع وظائف لمجرد أنها قد تكون مفيدة.
- فرّق بين "ميزة إضافية اختيارية" وبين "وظيفة مفقودة يحتاجها النظام منطقيًا".
- لا تعتبر كل Edge Case مشكلة؛ قيّمها بناءً على طبيعة النظام.
- لا تصلح المشاكل مباشرة في المرحلة الأولى — أريد أولًا **Audit فقط** وتقريرًا شاملًا.
- إذا وجدت مشكلة محتملة لكن الأدلة غير كافية، ضعها تحت **Needs Verification**.
- افحص المشروع كاملًا، وليس الملفات التي تبدو مرتبطة بالميزة فقط.

وفي النهاية أعطني:
1. ملخصًا تنفيذيًا. 2. قائمة المشاكل مرتبة حسب الخطورة. 3. أهم Functional Gaps التي يجب إصلاحها أولًا.
4. قائمة Uncovered Use Cases. 5. قائمة Integration/Synchronization Gaps. 6. قائمة Needs Verification.
7. تقييمًا تقريبيًا لـ **Functional Completeness** للمشروع مع تفسير التقييم.

---

# الجزء 2 — تحسينات المرحلة 1 (✚ ملحقات مدمجة في البرومت)

## ✚ أ. خريطة المصطلحات — استبدل المصطلحات العامة بما يقابلها فعلياً (Tauri محلي بلا سيرفر)

| مصطلح النموذج | ما يقابله في HaramLite |
|---|---|
| Database | `settings.json` + `bridge_state.json` + مجلد `requests/*.json` + localStorage للواجهة |
| Queue | طابور الدفعة في ذاكرة GUI + قناة `bridge-worker` + صف `watch_service` |
| Worker | خيط `bridge-worker` · خيط `watch_service` · حوض خيوط Tauri للمعالجة · عملية yt-dlp/ffmpeg ابنة |
| API | أوامر Tauri (`invoke`) · أحداث `emit` (`sep-progress`, `log-line`, `cuda-install`…) · بروتوكول Native Messaging (stdio) |
| External | الإضافة في المتصفح · مجلد المراقبة · CLI · الإطلاق المخفي `--hidden-start` · التحديث الذاتي لـ yt-dlp · GitHub (أصول CUDA) |
| Cron | المسح الدوري لمراقب المجلد (60ث) + مسح مجلد `requests` كل 5ث |

## ✚ ب. مسارات إنشاء العملية — افحص كل مسار على حدة ولا تفترض تطابق السلوك:

1) سحب ملف في GUI (مفرد) · 2) دُفعة GUI · 3) مجلد المراقبة (watch) · 4) طلب المتصفح (bridge) · 5) CLI · 6) المعاينة السريعة (preview)

لكل مسار اسأل: هل تصل الحالة للواجهة؟ هل يعمل الإلغاء؟ هل الفشل قابل لإعادة المحاولة في نفس الجلسة؟ هل يُمنع التكرار؟ هل تتزامن «آخر نتيجة» مع ما تراه الواجهة؟ — واختبر **التقاطع**: هل يمكن أن يعالج مساران نفس الملف في اللحظة نفسها (مثال: ملف يصل لمجلد المراقبة والمستخدم يسحبه في GUI معاً)؟ هل توجد قفلات؟

## ✚ ج. البقاء بعد الانهيار — مطلوب صراحةً (انهيارات صامتة موثقة على هذا الجهاز)

- ملفات `_haramlite_work/` و `*.download` و `*.part` بعد قتل العملية: من ينظفها؟ هل تتراكم؟
- ملف بدأ فصله ثم مات التطبيق: هل تبقى علامة؟ هل يعرف المستخدم أنه نصف معالج؟ هل يمكنه إعادة المحاولة دون التباس؟
- بعد إعادة التشغيل: هل حالة الواجهة (طابور الدفعة مثلاً) تتطابق مع الواقع؟ (طابور GUI في الذاكرة يضيع — ماذا يرى المستخدم؟)
- رسالة «الجلسة السابقة انتهت فجأة» في السجل + ملف `haramlite-crash.log` — استخدمهما دليلاً عند الحاجة.

## ✚ د. Dead Ends المرجّح وجودها — تحقق ولا تفترض

- بند فشل في الدفعة: هل يوجد Retry؟ إن لم يوجد، هل هو Gap أم تصميم مقبول؟ اذكر السبب.
- ملف واحد: هل يوجد إلغاء أثناء العمل؟ (يوجد — تحقق أنه يعمل من كل المسارات).
- رابط فشل من المتصفح: هل إعادة الإرسال ممكنة بلا إعادة تشغيل التطبيق؟ (كانت Gap — أُصلحت مؤخراً؛ تحقق من الحالة الحالية).

## ✚ هـ. قواعد التسليم (متوافقة مع مدققينا السابقين)

- التقرير فقط، **بلا أي إصلاح**، إلى `AUDIT.md` في جذر المشروع — **قسم جديد بتاريخ اليوم بعد الأقسام الموجودة، لا استبدال**.
- الأدلة: ملف + سطر دائماً. لا تقل «ربما» داخل المشاكل المؤكدة.
- قبل أي أمر طرفية: اشرح السبب وانتظر موافقة المستخدم. (لاحظ: cargo في `$env:USERPROFILE\.cargo\bin` وليس في PATH.)
- لا تعيد فتح ما وثّقه `surgical_audit_report.md` أو `CUDA_RUNTIME_PLAN.md` كمُعالَج — إلا بمسار إثبات جديد.

---

# الجزء 3 — خطة تنفيذ المرحلة 2 (ترتيب الفحص)

| الخطوة | المحتوى | المخرجات |
|---|---|---|
| **1** | اقرأ: NEXT_STEPS.md · surgical_audit_report.md · CUDA_RUNTIME_PLAN.md · AUDIT.md (اقرأ README إن احتجت) | خريطة معمارية + قائمة الكيانات (مهمة معالجة/تنزيل/إعداد/طلب bridge) |
| **2** | تتبع دورة الحياة الكاملة لكيان «مهمة معالجة» عبر مساراته الستة (الجزء 2-ب): من الإنشاء حتى UI + إشعار + state.json | جدول مسارات × حالات (موجود/غائب/غير متزامن) |
| **3** | الفحص الموجّه بالملحقات: مسارات الإنشاء (ب) · البقاء بعد الانهيار (ج) · Dead Ends (د) | قائمة مشاكل مبدئية بالأدلة |
| **4** | تدقيق تقني تقليدي سريع (منطق/حدّيات/معالجة أخطاء) على الوحدات الحساسة فقط بعد الفحص الوظيفي: bridge.rs · watch_service.rs · pipeline.rs · yt_dlp.rs · main.ts | ملاحظات إضافية (لا تكرر ما سبق) |
| **5** | اكتب قسم AUDIT.md الجديد: ملخص تنفيذي ← مشاكل بالخطورة ← أهم الفجوات ← Uncovered Use Cases ← تكامل/مزامنة ← Needs Verification ← **نسبة الاكتمال الوظيفي بجدول مبرر** | التقرير النهائي |

> قاعدة حاكمة: الوظيفة «تعمل» لا تعني «مكتملة». والسؤال المتكرر: «أين يعلق المستخدم ولا يستطيع المتابعة أو التراجع؟»

---

# الجزء 4 — ملاحظات صياغة على النموذج الأساسي (للمالك)

1. طلب «تقييم Functional Completeness» كنسبة — اطلب **النسبة + جدولاً مبرراً لكل مكوّن** (نسبة بلا تفصيل لا قيمة لها). أُدرج ذلك في خطة المرحلة 2 (الخطوة 5).
2. أضف صراحةً: «لا تنظر للواجهة وحدها — افتح مسارات الإنشاء الستة واختبر ترابطها» — أكبر قيمة لهذا النموذج هي اكتشاف أن watch/bridge/GUI قد لا يعرف بعضها بعضاً. (مدمج في الملحق ب).
3. النموذج يتضمن قواعد «لا تصلح في المرحلة الأولى» ✓ — حافظ عليها عند أي متابعة لاحقة.
