# خطة التنفيذ المتكاملة — تطورات HaramLite المستقبلية

> **الحالة: معتمَدة 100% من المالك ✓** (التعديلات الأربعة + إجابات الأسئلة المفتوحة)
> **المصدر:** `FUTURE_PLAN.md` (مؤرشفة)
> **المستودع:** `SMSMy/HaramLite`
> **الإصدار المستهدف:** 0.2.0

---

## أولاً: خلاصة الوضع الراهن (ما اكتشف من الكود)

| الملاحظة | الأثر على الخطة |
|---|---|
| `bin/` (ffmpeg/ffprobe/yt-dlp) و`models/` (63MB) **مضمّنة فعلاً** في NSIS عبر `tauri.conf.json` resources | بند "أول تشغيل" يتحول من "تثبيت" إلى **معالج إصلاح ذاتي** بدل إعادة اختراع العجلة |
| `index.html` يحمّل Tailwind من CDN (`cdn.tailwindcss.com`) + خط Material Symbols من Google Fonts + **سكربتات AdGuard دخيلة** (من حفظ المتصفح) | بند الـ Offline يجب أن يشمل تنظيف هذه الشوائب أيضاً |
| منطق التحميل الآمن (SHA-256 + تبديل ذرّي + تراجع `.previous`) موجود وجاهز في `yt_dlp.rs` | سنعيد استخدامه **كما هو** لتنزيل النموذج في معالج الإصلاح |
| الإعدادات كلها في `localStorage` فقط (cuda، اللغة، keep_inst) | مجلد المراقبة يتطلب إعدادات على مستوى Rust — سنبني طبقة إعدادات موحدة أولاً |
| `tauri-plugin-opener` و`dialog` مثبتان فعلاً | زر "الإبلاغ عن مشكلة" لا يحتاج أي تبعية جديدة |
| نسخة التطبيق `0.1.1` لكن شارة الواجهة تقول `v0.8.2-beta` | تضمين التصحيح في الدفعة |
| مجلد `Extension/` هو إضافة HaramMute القديمة (تخاطب بايثون على localhost:8765) — **منتج مغلق ومجمّع** وليس نواة قابلة للبناء | سنبني إضافة MV3 جديدة من الصفر وتستعير منها الفكرة فقط، لا الكود |

---

## ثانياً: الخطة على شكل 6 مراحل (Sprints)

### Sprint A — البناء الكامل دون إنترنت (الأساس لكل شيء)

**A1. الاستغناء عن Tailwind CDN**
- إضافة `tailwindcss@3.4.x` + `@tailwindcss/forms` + `@tailwindcss/container-queries` (نفس بلجنات الـ Play CDN الحالية) كـ devDependencies.
- نقل `tailwind.config` المضمّن في `index.html` إلى `tailwind.config.js` مع `content: ["./index.html", "./src/**/*.{ts,html}"]`.
- إنشاء `src/input.css` بأوامر `@tailwind` + دمج `styles.css`، وبناء `tailwind.css` مبنية ومضغوطة عبر سكربت في `package.json`.
- حذف سكربت CDN والإعدادات المضمّنة من `index.html`.

**A2. استضافة خط Material Symbols محلياً**
- تنزيل ملف woff2 المتغير + `@font-face` محلي (الخطوط العربية Thmanyah محلية أصلاً).

**A3. تنظيف وتحصين**
- إزالة سكربتات AdGuard الدخيلة من `index.html`.
- تفعيل CSP صارم (`default-src 'self'`) بعد زوال كل المصادر الخارجية.
- توحيد رقم الإصدار في الشارة مع `CARGO_PKG_VERSION`.

> **معيار القبول:** لقطات شاشة قبل/بعد متطابقة بصرياً 100%، وفتح التطبيق مع قطع الإنترنت يعرض الواجهة كاملة.

---

### Sprint B — مكاسب سريعة في تجربة الاستخدام

**B1. المعاينة السريعة (Quick Preview)**
- **Rust:** معامل `preview_seconds` جديد يمر من `separate_file` إلى `pipeline::process_file`، ويقتطع WAV المطبّع إلى N ثانية قبل الفصل (عبر `-t` في مرحلة التطبيع).
- **الواجهة:** مفتاح "معاينة سريعة" مع اختيار المدة (10/15/30 ثانية) بجانب زر المعالجة؛ المخرج يحمل لاحقة `_preview` ويُعلَّم في الطابور.
- **⚠ دلالات المعاينة (تعديل 4):** قصّ الصمت على شريحة 15 ثانية لا يمثّل سلوك الملف الكامل. لذلك:
  - في **وضع الأغنية**: يُكتب في الواجهة صراحةً أن النتيجة «عينة جودة الفصل فقط — لا تمثل قصّ الصمت النهائي» (تسمية الزر + تلميح Tooltip + وسم على عنصر الطابور `_preview`).
  - في **وضع المقطع العادي**: المعاينة ممثلة حقيقية للمخرج النهائي، ويُكتب ذلك أيضاً.

**B2. إشعارات الانتهاء**
- `tauri-plugin-notification` (تبعية Rust + JS + إذن في capabilities).
- إشعار نظام عند اكتمال كل ملف واكتمال الطابور (ومع الفشل).
- صوت خفيف عبر Web Audio API بصوت مضمّن base64 (بلا أي تبعية Rust جديدة) — يمكن استعارة `ding.wav` من مجلد Extension.
- مفتاح تفعيل في الإعدادات.
- **⚠ قيد ويندوز (تعديل 1):** إشعارات النظام على ويندوز تتطلب **AUMID مسجلاً + اختصار قائمة ابدأ** — المثبت NSIS ينشئهما تلقائياً فينجح؛ **النسخة المحمولة تفشل بصمت**. المعالجة:
  - عند أول تشغيل محمول: محاولة إنشاء اختصار قائمة ابدأ + تسجيل AUMID برمجياً (IShellLink)؛ إن تعذّر → تخفيض رشيق: إشعار داخلي في الواجهة + الصوت فقط، مع ملاحظة في صفحة الإعدادات توضح السبب وطريقة الإصلاح.

**B3. زر "حول البرنامج"**
- نافذة Modal: الإصدار (من أمر `ping`)، المطورون، **حقوق النماذج والأدوات**: UVR-MDX-NET-Voc_FT، ONNX Runtime، ffmpeg، yt-dlp، خط Thmanyah، والرخصة.

**B4. زر "الإبلاغ عن مشكلة"**
- رابط مباشر إلى `https://github.com/SMSMy/HaramLite/issues/new` عبر opener المثبت (مع قالب مشكلة معبّأ مسبقاً بالإصدار ونظام التشغيل).

> **معيار القبول:** دورة معاينة كاملة بوضعيها وبتسمياتها الصحيحة، إشعار + صوت يصلان على النسخة المثبتة، ورسالة التخفيض الرشيق تظهر على المحمولة عند فشل AUMID، والنافذتان (حول/مشكلة) تفتحان بلا أخطاء.

---

### Sprint C — التوزيع: إعداد أول تشغيل + التحديث الذاتي

**C1. معالج الإصلاح الذاتي عند أول تشغيل**
- بما أن المثبت يضمّن كل شيء فعلاً، نضيف فحص سلامة عند الإقلاع: تحقق من `ffmpeg.exe / ffprobe.exe / yt-dlp.exe / النموذج`.
- عند نقص أي مكوّن (نسخة محمولة، حذف يدوي): **نافذة إعداد تلقائية** بشريط تقدم تنزّل المفقود من GitHub Releases مع تحقق SHA-256 — بإعادة استخدام نمط `download_verified` الموجود في `yt_dlp.rs` حرفياً.
- توحيد منطق الفحص مع أمر CLI الموجود `--check`.

**C2. التحديث الذاتي (Auto-updater)**
- تفعيل `tauri-plugin-updater`: توليد زوج مفاتيح توقيع (`tauri signer generate`)، `createUpdaterArtifacts: true`، و`endpoint` يشير إلى `latest.json` في GitHub Releases.
- فحص صامت عند الإقلاع + زر "التحقق من التحديثات" في الإعدادات + شريط تقدم + إعادة تشغيل.

**⚠ قيود النسخة المحمولة (مجتمعة — تعديل 1):**

| الميزة | النسخة المثبتة (NSIS) | النسخة المحمولة |
|---|---|---|
| التحديث الذاتي | يعمل (endpoint + توقيع + ريلود) | **لا يعمل** — تُعرض صفحة الإصدارات بدلاً منه |
| إشعارات ويندوز | تعمل (AUMID + اختصار قائمة ابدأ يُنشآن مع التثبيت) | **تفشل بصمت** ما لم يُنشأ اختصار قائمة ابدأ وتسجيل AUMID يدوياً/برمجياً (انظر B2) |

- القيدان يُعلنان في الواجهة عند اكتشاف تشغيل محمول (رسالة واحدة واضحة في الإعدادات، لا مفاجآت صامتة).

**C3. خط إصدارات CI (GitHub Actions)**
- سير عمل واحد: بناء عبر `tauri-action` → رفع مثبت NSIS + `latest.json` + التوقيع + **النموذج والـ redist كأصول Release** (لتغذية C1) → خطوة رفع للإضافة (Sprint E).

> **معيار القبول:** تثبيت نظيف على جهاز بلا إنترنت يعمل 100%؛ حذف النموذج يدوياً ثم فتح التطبيق يعيد تنزيله بشريط تقدم؛ إصدار تجريبي v0.2.0 يظهر كتحديث داخل نسخة 0.1.x مثبتة؛ النسخة المحمولة تعرض الرسالتين (تحديث + إشعارات) بلا فشل صامت.

---

### Sprint D — الأتمتة المحلية

**D1. طبقة إعدادات موحدة (تمهيد إلزامي)**
- وحدة `settings.rs` في Rust: ملف JSON في `app_data_dir` مع أوامر `get_setting/set_setting` وحدث `settings-changed` — تنقل كل ما في localStorage إليها (وتتزامن معها لغة الواجهة).

**D2. مجلد المراقبة (Watch Folder)**
- وحدة `watcher.rs` بمكتبة `notify` + قناة أحداث.
- في الإعدادات: مفتاح تفعيل + اختيار المجلد (dialog) + اختيار وضع المعالجة الافتراضي.
- عند دخول ملف: يُضاف تلقائياً لطابور المعالجة بنفس الإعدادات المختارة، معالجة خلفية متسلسلة، أحداث تقدم حية في لوحة الطابور، وإشعار انتهاء.
- **حراسة الحلقات:** تجاهل ملفات `_haramlite_work`، والمخرجات (`_vocals`, `_preview`...)، والملفات المخفية/المؤقتة، وانتظار استقرار حجم الملف قبل المعالجة (ملفات قيد التنزيل).

**⚠ إعادة المسح الدوري (تعديل 2):** `notify` على ويندوز يفوّت أحداثاً في بيئات شائعة (OneDrive يرفع/يستعيد الملفات، مضاد فيروسات يقفل الملف لحظياً، مجلدات الشبكة). لذلك لا نعتمد على الأحداث وحدها:
- **إعادة مسح دوري** كل 60 ثانية (قابل للضبط) + مسح فوري عند عودة النافذة للتركيز وعند الإقلاع.
- إلغاء التكرار عبر مفتاح `(المسار + الحجم + mtime)` — الملف المكتشف بالمسح لا يُعالج مرتين إن سبقه حدث.
- الأحداث تبقى للمسار السريع (استجابة شبه فورية)، والمسح الدوري هو شبكة الأمان.

**⚠ حارس القرص (تعديل 3):** أداة رأيية بلا حارس تمتلئ لها مساحة القرص "بأدب" حتى التوقف التام. قبل إدخال أي ملف مراقَب إلى الطابور:
- **حد أقصى لحجم الملف** (افتراضي 2GB، قابل للضبط في الإعدادات) — الأكبر يُرفض برسالة واضحة في الطابور والسجل.
- **فحص المساحة الحرة** على قرص الإخراج قبل بدء الفصل: المطلوب ≥ (حجم الإدخال × 3) أو حد أدنى 1GB أيهما أكبر؛ إن لم تكفِ → يُعلَّم العنصر «تخطّي: مساحة غير كافية» مع إشعار، ولا يبدأ الفصل (لا ملفات عمل نصف مكتملة تتراكم).

> **معيار القبول:** إسقاط 3 ملفات في المجلد → تعالج تلقائياً بالترتيب دون لمس الواجهة، ولا يعيد التطبيق معالجة مخرجاته؛ ملف يُحذف حدث وصوله (محاكاة OneDrive) يلتقطه المسح الدوري خلال 60 ثانية؛ ملف 5GB يُرفض برسالة الحجم، وعند امتلاء القرص يقف الطابور برسائل «تخطّي» بدل فشل غامض.

---

### Sprint E — التكامل مع المتصفح

**E1. نواة الإضافة الجديدة (مجلد `browser-extension/` جديد كلياً)**
- إضافة MV3 بسيطة: قائمة سياق "أرسل إلى HaramLite" على الروابط/الصفحات + Popup بحالة التطبيق، ثنائية اللغة، بلا أي تتبع (PostHog القديم لن يُنقل).
- **MVP صريح — معتمَد من المالك:** تسليم الروابط فقط (التنزيل والمعالجة كاملة في تطبيق سطح المكتب الذي يستفيد من CUDA/Rust) — الإضافة جسر اتصال خفيف وآمن، والمعالجة داخل المتصفح مؤجلة لما بعد التحقق من القناة.

**E2. قناة الاتصال — Native Messaging (وليس منفذ HTTP)**
- التطبيق يسجّل نفسه كـ Native Messaging Host (علم CLI جديد `--native-host`): كتابة مفاتيح التسجيل (HKCU) لتسجيله مع Chrome وFirefox — عبر زر "تفعيل التكامل" في الإعدادات.
- **بروتوكول بسيط ومتين:** نسخة المستضيف تستقبل JSON عبر stdin، تتحقق من وجود نسخة رئيسية عاملة (single-instance)، تكتب ملف طلب `<uuid>.json` في مجلد الطلبات ثم تخرج؛ النسخة الرئيسية تلتقطه بمراقب المجلد نفسه (إعادة استخدام Sprint D بما فيه المسح الدوري) وتنفذه وتشعره بالإنجاز.
- لا منافذ مفتوحة، لا CORS، لا مخاطر شبكة محلية.

> **معيار القبول:** نقرة يمين على رابط YouTube → "أرسل إلى HaramLite" → ينزّل التطبيق الرابط ويعالجه تلقائياً، والنسخة الرئيسية المغلقة ترفض بلطف بدل أن تفتح نسخة ثانية.

---

### Sprint F — التوثيق النهائي (يعمل بالتوازي مع الجميع)

- **README للمستخدم النهائي:** شعار + لقطات (مجلد `IMG/` موجود)، زر "تحميل المثبت" يشير لأحدث Release، متطلبات بسيطة (Windows 10/11 — وVC++ يُثبَّت تلقائياً)، شرح الاستخدام بالمصطلحات العادية، وملاحظات النسخة المحمولة (تحديث + إشعارات).
- نقل تفاصيل البناء للمطورين إلى `CONTRIBUTING.md`.
- تحديث `FUTURE_PLAN.md` بعلامات الإنجاز في نهاية كل Sprint.

---

## ثالثاً: ترتيب التنفيذ واعتمادياته

```
A (بناء Offline) ──► B (معاينة/إشعارات/حول) ──► C (توزيع+تحديث)
        │                    │                        │
        └────────► D (إعدادات ← مراقبة) ◄─────────────┘
                        │
                        └────────► E (إضافة المتصفح) ──► F (توثيق)
```

- **A أولاً** لأنه يلمس `index.html` والبناء، ويحرّر كل ما بعده من القلق الشبكي.
- **D1 قبل D2** (المراقبة تحتاج الإعدادات في Rust).
- **C3 قبل C1 وE** (استضافة الأصول والتوقيع شرط للتحديث الذاتي والإصلاح).
- **F بالتوازي** طوال المدة.
- رفع الإصدار إلى **0.2.0** عند إتمام A–D.

---

## رابعاً: المخاطر والتعامل معها

| الخطر | المعالجة |
|---|---|
| اختلاف بصري بين Tailwind المبني وPlay CDN | تثبيت الإصدار 3.4 + نفس البلجنات + مقارنة لقطات كمعيار قبول |
| التحديث الذاتي يتطلب توقيعاً ونشر `latest.json` بدقة | تقييده بإصدارات المثبت + اختبار قناة تجريبية قبل 0.2.0 |
| إشعارات ويندوز تحتاج AUMID/اختصار قائمة ابدأ — المثبت ينجح والمحمول يفشل بصمت | إنشاء الاختصار برمجياً عند أول تشغيل محمول + تخفيض رشيق معلن في الإعدادات (B2/C2) |
| `notify` يفوّت أحداثاً مع OneDrive/مضاد الفيروسات/الشبكة | إعادة مسح دوري 60 ثانية + عند التركيز/الإقلاع + إلغاء تكرار (D2) |
| امتلاء القرص من معالجة غير منضبطة | حد أقصى لحجم الملف + فحص مساحة قبل الفصل + «تخطّي» معلن (D2) |
| مراقب المجلد يعيد التقاط مخرجاته (حلقة) | قائمة حجب صريحة + فحص استقرار الحجم |
| معاينة الأغنية توحي بأنها تمثل القص النهائي | تسمية «عينة جودة الفصل» في وضع الأغنية + وسم `_preview` (B1) |
| Native Messaging يختلف بين Chrome/Firefox | تجريد طبقة التسجيل، MVP على Chrome أولاً ثم Firefox |

---

## خامساً: القرارات المعتمدة من المالك

| السؤال | القرار المعتمد |
|---|---|
| اسم المنظمة/المستودع | `SMSMy/HaramLite` — رابط المشكلات: `https://github.com/SMSMy/HaramLite/issues/new` |
| نطاق إضافة المتصفح (E) | **MVP = تسليم الروابط فقط**؛ المعالجة كاملة في تطبيق سطح المكتب (الإضافة جسر خفيف بلا عبء حسابي) |
| قرار C1 (التضمين أم التنزيل) | **تضمين كامل في المثبت (Full Offline Installer) + معالج الإصلاح الذاتي** كشبكة أمان (حجم ~120–150MB مقبول ولا يبرر تنزيلاً عند أول تشغيل) |
| حد الحجم الافتراضي لحارس القرص | 2GB قابل للضبط من الواجهة (مقترح — يدخل حيز التنفيذ ما لم يُعترض) |
| فاصل المسح الدوري للمراقبة | 60 ثانية + عند التركيز/الإقلاع (مقترح — يدخل حيز التنفيذ ما لم يُعترض) |

---

## سادساً: سجل التقدم (Progress Log)

| Sprint | الحالة | الملاحظات |
|---|---|---|
| **A** — البناء الكامل دون إنترنت | ✅ **مكتمل** | A1 Tailwind محلي (3.4.17 + forms + container-queries)، A2 خط Material Symbols محلياً (woff2 1.1MB)، A3 تنظيف AdGuard/CDN/Google Fonts + نقل الأنماط المضمّنة إلى styles.css + CSP صارم + توحيد شارة الإصدار. تحقق: `pnpm build` نجح، فحص الأنماط المحسوبة في المتصفح مطابق (ألوان/زجاجية/خطوط/أيقونات)، صفر طلبات خارجية من التطبيق نفسه. |
| **B** — معاينة/إشعارات/حول/إبلاغ | ✅ **مكتمل** | B1 معاينة سريعة (10/15/30 ث) عبر `preview_seconds` → `normalize_for_engine_limited` (ffmpeg `-t`) + وسم `_preview` في أسماء المخرجات + التسمية الصادقة: «عينة جودة الفصل فقط — لا تمثل قصّ الصمت النهائي» في وضع الأغنية و«عينة مطابقة للمخرج النهائي» في المقطع. B2 `tauri-plugin-notification` + أمر `notify_done` + تسجيل AUMID صريح (`SetCurrentProcessExplicitAppUserModelID`) + صوت ding.wav محلي + تخفيض رشيق (toast داخلي) عند فشل الإشعار النظامي. B3 نافذة حول (الإصدار + فريق HaramMute + أهل الفضل: النموذج/ONNX/ffmpeg/yt-dlp/الخط). B4 زر إبلاغ → `github.com/SMSMy/HaramLite/issues/new` عبر opener. تحقق: cargo check ✓، pnpm build ✓ (ding.wav مضمّن)، اختبار تفاعلي في متصفح بمحاكاة Tauri: كل الميزات تعمل وصفر أخطاء. |
| **C** — التوزيع: إصلاح ذاتي + تحديث ذاتي + CI | ✅ **مكتمل** | C1 وحدة `repair.rs` (منفست المكونات الأربعة مع SHA-256 المثبتة من الملفات المحلية + تنزيل من إصدار `assets-v1` + تحقق + تثبيت ذرّي) + أمرا `health_check_cmd`/`repair_component` + نافذة معالج تفتح تلقائياً عند نقص أي مكوّن مع شريط تقدم. C2 `tauri-plugin-updater` (Rust+JS) + زوج مفاتيح توقيع (`updater.key` في gitignore، المفتاح العام في tauri.conf.json) + endpoint من `SMSMy/HaramLite/releases/latest/download/latest.json` + `createUpdaterArtifacts` + فحص صامت عند الإقلاع وزر يدوي مع تنزيل/تثبيت. C3 سير عمل GitHub Actions (`release.yml`) يبني NSIS + latest.json + التوقيع + ينشر أصول الإصلاح. **إصلاح حرج:** إزالة `+crt-static` من `.cargo/config.toml` — كانت تمنع الربط مع مكتبات ONNX Runtime الجاهزة (73 رمزاً ناقصاً)؛ المثبت يثبّت VC++ Redist أصلاً (hooks.nsh) فالـ CRT الديناميكية هي التصميم الصحيح. تحقق: بناء debug كامل نجح، `--check` يجد المكونات الأربعة، واختبار واجهة المعالج في المتصفح (فتح تلقائي + قائمة + إصلاح الكل بالترتيب + صفر أخطاء). |
| **D** — إعدادات موحدة + مجلد مراقبة | ✅ **مكتمل** | D1 وحدة `settings.rs` (JSON في app_data_dir، أوامر get/set + حدث settings-changed، وزرع localStorage منها عند أول تشغيل). D2 وحدة `watch_service.rs` تعمل كاملة في Rust: أحداث notify + **مسح دوري (60 ثانية افتراضياً) كشبكة أمان** + مسح أولي عند الإقلاع + **حارس قرص** (حد حجم أقصى 2GB قابل للضبط + فحص المساحة الحرة قبل الفصل بـ 3× حجم الإدخال أو 1GB) + انتظار استقرار الحجم + حجب المخرجات الذاتية + معالجة خلفية متسلسلة بأحداث watch-progress/done/skip وإشعارات نظام. **تحقق e2e حقيقي:** ملف موجود مسبقاً + ملف أُسقط أثناء التشغيل → كلاهما عولج تلقائياً (mp3)، مخارج بقيت بلا إعادة معالجة عبر دورات مسح، وملف 1.4MB رُفض برسالة الحد في السجل دون مخرج. **إصلاحا اكتشفهما الاختبار:** (1) sweep كان يسجل المفاتيح قبل الحلقة فتتخطى كل ملفات المسح الأولي — أُصلح مع إعادة تسجيل المفتاح المستقر بعد المعالجة لمنع التكرار؛ (2) خطأ عمر مرجعي في setup. |
| **E** — إضافة المتصفح + Native Messaging | ✅ **مكتمل** | E1 إضافة MV3 جديدة `browser-extension/` (مفتاح ثابت ← معرّف `jchaeejligdfbkgkbgneimclkagoopig`، قائمة سياق للروابط/الصفحات/الفيديو، Popup حالة اتصال، صفر تتبع، CSP مقيّدة). E2 وحدة `bridge.rs`: المضيف (`--native-host`) يقرأ بروتوكول stdio (4 بايت طول + JSON) ويكتب طلباً في `requests/` ويشغّل التطبيق إن لم يكن يعمل؛ النسخة العاملة تلتقطه (notify + مسح دوري) وتنزّل وتعالج كاملة وتشعر بإشعار نظام؛ وأمر `register_native_host` يكتب manifest المضيف + مفاتيح تسجيل HKCU (Chrome/Chromium/Edge/Firefox). **تحقق e2e حقيقي:** ping يرد صحيحاً، رابط → ملف طلب → التطبيق التقطه وعالجه (فشل التحميل متوقع لرابط وهمي وسُجّل برشاقة). **إصلاحان من الاختبار:** (1) الابن المولود كان يرث أنبوب stdout فيعلق القارئ — أُصلح بـ `Stdio::null()`؛ (2) استيرادا `Watcher`/`Manager` الناقصان. |
| **F** — التوثيق + الإصدار 0.2.0 | ✅ **مكتمل** | README جديد للمستخدم النهائي (تحميل/متطلبات/مميزات/خصوصية/Issues) + `CONTRIBUTING.md` للمطورين (بناء/بنية/إصدارات/أسرار CI/ملاحظات معمارية) + رفع الإصدار إلى **0.2.0** في package.json و tauri.conf.json و Cargo.toml وشارة الواجهة. |
| **ما بعد الخطة** — الإصلاحات الجراحية | ✅ **مكتمل** | عولجت المشاكل العشرون من `surgical_audit_report.md` (2026-09-01) — الخريطة الكاملة في `NEXT_STEPS.md` (الأولويات 1–3). أبرزها: **R-1** Limiter بـ Rolling-Max O(N) بدل O(N×look_len)+modulo (10.5M عينة: من +15-20ث إلى 0.70ث في debug، مع اختبار انحدار bit-exact مقابل الخوارزمية القديمة)؛ **R-4** إزالة `%` من Pingpong/Comb/Allpass؛ **R-2** `ort::init()` مرة واحدة عند الإقلاع (`separator::init_ort_env`) + شارة CUDA صادقة (`env_cuda`/`restart_required`) + رسالة إعادة تشغيل في الواجهة؛ **R-3** K-weighting على القناة كاملة ثم النوافذ (اختبار انحدار يثبت الاستمرارية)؛ **R-5** نطاقات الصمت تحسب مرة (`cut_silence_with_ranges`)؛ **F-1** دفع السجلات كأحداث `log-line` بدل polling 700ms؛ **E-1** `connectNative` منفذ دائم + مضيف stdio بحلقة مستمرة + timeout أمان؛ **E-3** MutationObserver + `yt-navigate-finish`؛ **F-3/F-4** إلغاء كامل (الدُفعة تلغي الملف الجاري، والمفرد يتحول زر الفصل لزر إلغاء)؛ **F-5** `clearTimeout` لسباقات سطر المرحلة وبطاقة الجسر؛ **E-2** عدّاد فشل (8) يوقف الاستفسار؛ **الأولوية 3** تنظيف شامل (F-2/F-6/F-7/F-8/E-4/E-5/E-6/E-7) + **صفر تحذيرات Rust**. تحقق: 23/23 اختباراً + `tsc` نظيف + مسبار توقيت مضمن (`cargo test --release limiter_timing_probe -- --ignored`). |
| — | ✅ **اكتملت الخطة** | — |

### ملفات Sprint A المتأثرة
- **جديد:** `tailwind.config.cjs`، `src/input.css`، `src/tailwind.css` (مولّد)، `src/assets/fonts/material-symbols-outlined.woff2`، `IMG/sprintA_check.png` (لقطة تحقق)
- **معدّل:** `package.json` (سكربتات build/predev/build:css + تبعيات tailwind)، `index.html` (تنظيف شامل + رابط tailwind.css محلي + شارة v0.1.1)، `src/styles.css` (خط الأيقونات + الأنماط المنقولة)، `src-tauri/tauri.conf.json` (csp + devCsp)، `.gitignore` (`/src/tailwind.css`)

### ملفات Sprint B المتأثرة
- **جديد:** `src/assets/ding.wav` (من Extension)، `src/vite-env.d.ts`
- **معدّل:** `src-tauri/src/media.rs` (normalize_for_engine_limited + إصلاح مرجع معلق)، `pipeline.rs` (معامل preview_seconds + name_tag)، `lib.rs` (أمر notify_done + بلجن الإشعارات + AUMID)، `cli.rs` (None للمعاينة)، `Cargo.toml` (+notification + Win32_UI_Shell)، `capabilities/default.json` (+notification:default +opener:allow-open-url)، `index.html` (صف المعاينة + عناصر الإعدادات + نافذة حول + toast)، `src/main.ts` (كل الأسلاك + i18n)، `src/styles.css`

---

## سجل المراجعات

| الإصدار | التغيير |
|---|---|
| v1.0 | الخطة الأصلية من اعتماد المالك |
| v1.1 | التعديلات الأربعة: (1) قيد AUMID/قائمة ابدأ بجانب قيد التحديث الذاتي، (2) إعادة المسح الدوري للمراقبة مع الأحداث، (3) حارس القرص (حد حجم + فحص مساحة قبل الفصل)، (4) تسمية المعاينة في وضع الأغنية «عينة جودة الفصل» |
| v1.2 | اعتماد المالك الكامل: المستودع `SMSMy/HaramLite`، MVP الإضافة = روابط فقط، C1 = تضمين كامل + معالج إصلاح، والبدء الفعلي بالتنفيذ من Sprint A |
| v1.3 | ما بعد الخطة: تنفيذ إصلاحات التقرير الجراحي العشرين (R-1..R-5, F-1..F-8, E-1..E-7) بالتفاصيل أعلاه |
