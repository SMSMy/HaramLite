# إشعارات الطرف الثالث — HaramLite

> **ما هذا الملف:** توثيق كل مكوّن خارجي **يُوزَّع** مع HaramLite (داخل المثبّت أو في نشرات GitHub)، برخصته ومصدره.
> **ما ليس:** ليس فتوى قانونية — بل سجلّ تقني بالوقائع المقيسة، وكل بند لم أستطع إثباته موسوم **«يحتاج تحقّقاً»** ولم أختلق له رخصة.
>
> **آخر تحديث:** 2026-09-15 · **الإصدار المرجعي:** 0.2.5 (المحرّك: بناء FFmpeg بصيغة **LGPLv3**) · **المدقّق:** جلسة تدقيق A→Z + قياسات إصدار 0.2.5.

---

## ١) مكوّنات محزومة داخل المثبّت (NSIS/MSI)

| المكوّن | المسار | الإصدار | الرخصة | كيف تحقّقنا |
|---|---|---|---|---|
| FFmpeg | `bin/ffmpeg.exe` (126.1 MiB · 132,238,848 بايت) | `n8.1.2-52-g5a03dfa0f6-20260914` (بناء `BtbN/FFmpeg-Builds` · `win64-lgpl`) | **LGPLv3** | ✅ `ffmpeg -version`: سطر التكوين يحمل `--enable-version3` و**لا** يحمل `--enable-gpl` · وبصمة أصلنا المنشور `sha256:799b9ee9…61dd4b` |
| FFprobe | `bin/ffprobe.exe` (125.9 MiB · 132,033,536 بايت) | `n8.1.2-52-g5a03dfa0f6-20260914` (نفس البناء) | **LGPLv3** | ✅ نفس سطر التكوين · وبصمة أصلنا المنشور `sha256:01af86fa…5b94dab` |
| yt-dlp | `bin/yt-dlp.exe` (17.0 MB) | — | **Unlicense** | ✅ واجهة GitHub: `yt-dlp/yt-dlp` |
| نموذج الفصل | `models/UVR-MDX-NET-Voc_FT.onnx` (63.7 MB) | UVR-MDX-NET | **MIT** — مع إسناد إلزامي | ✅ أصله منشور في `TRvlvr/model_repo` (نشرة `all_public_uvr_models`) بنفس الاسم والحجم |
| VC++ Redistributable | `vc_redist.x64.exe` (~24 MB) | مايكروسوفت | رخصة إعادة التوزيع لمايكروسوفت | ✅ الصفحة الرسمية: `learn.microsoft.com/en-us/visualstudio/releases/2022/redistribution` (وجودها مُتحقَّق) — والتزامنا: إدراج الإشارة إليها مع التوزيع |

### FFmpeg — التزامات LGPLv3 (بعد تبديل 0.2.5)

**سطر التكوين المقيس من الثنائي المحزوم** (`ffmpeg -version`, 2026-09-15):
`--enable-version3` **موجود** · `--enable-gpl` **غائب** · `--disable-libx264` · `--disable-libx265` ⇒ **الملف LGPLv3** (وليس GPLv3 كما كان بناء `gyan.dev` السابق).

1. **نصّ الرخصة** يُشحن مع التوزيع: `licenses/LGPL-3.0.txt` (7,652 بايت · `sha256:e3a994d82e644b03a792a930f574002658412f62407f5fee083f2555c5f23118` من `gnu.org`) **و**`licenses/GPL-3.0.txt` (35,149 بايت · `sha256:3972dc97…dfb36986`) — والثاني **لازم** لأن LGPLv3 نصّ «أذونات إضافية» فوق GPLv3 ويُحيل إليه. المجلد كله مُدرَج في موارد الحزمة (`tauri.conf.json` ⇒ `"../licenses": "licenses"`).
2. **المصدر المقابل**: متاح بنظافة أكبر من بناء gyan السابق:
   - **مصدر FFmpeg نفسه**: النسخة upstream معلنة **من الثنائي ذاته** (`n8.1.2-52-g5a03dfa0f6` ⇒ الفرع `n8.1.2` + المراجعة `5a03dfa0f6`) من `ffmpeg.org`.
   - **سكربتات البناء**: ‏`BtbN/FFmpeg-Builds` **ينشرها** (`build.sh` · `scripts.d/` · `variants/` · `patches/` · `makeimage.sh`) ⇒ لا تبقى «رقع غير منشورة» كما كان الحال مع `gyan.dev`.
   - **تثبيت البايتات**: وسم BtbN متحرّك، لكن ما نورّده **مثبَّت ببصمة SHA-256 على نشرة `assets-v1`** (والأصلان المنشوران باسمَي `ffmpeg-lgpl.exe` · `ffprobe-lgpl.exe`)، فما يُشحن لأي مستخدم هو بايتات بعينها لا «أحدث بناء».
3. **لا قيود إضافية** على الملف ولا على من يستلمه، و**حقّ الاستبدال/إعادة الربط** مصان لأن الربط بين تطبيقنا وFFmpeg **ربط عمليات لا ربط مكتبات** (البند ٤).
4. **تطبيق HaramLite نفسه يبقى MIT**: يستدعي `ffmpeg`/`ffprobe` **كعمليات فرعية** عبر `Command::new().args()` (`media.rs`) — أي برنامجان مستقلان (mere aggregation)، لا ربط ولا دمج.
5. **`--enable-libopenh264` موجود في التكوين** (وهو مُرمِّز مفتوح من **سيسكو**، ورخصته ليست LGPL): الثنائي يحمله، **وكودنا لا يستعمله في أي مسار** — مسارانا هما `h264_nvenc` و`h264_mf` (وبديل السقف `h264_mf` كذلك). يُذكر هنا للشفافية: من يشغّل `libopenh264` مباشرةً من هذا الثنائي تلزمه رخصة سيسكو، ونحن لا نفعل. ولا يُضاف كمُرمِّز احتياطي في هذا الإصدار (قرار مُسجَّل في `0.2.5.md`).

> **ملاحظة معمارية (تاريخ مُصحَّح):** سبب اختيار بناء GPL سابقاً كان استعمال `libx264` في **موضعين إنتاجيين** وموضع اختباري. **في 0.2.5 أُزيل `libx264` من الكود**: مسار التصدير العادي يستعمل `h264_nvenc` أولاً (`-preset p4 -cq 20`) و`h264_mf` بديلاً برمجياً (`-rate_control quality -quality 85`)، ومسار سقف الحجم (ميزانية بايت، حدّ تيليجرام) يستعمل `h264_mf` بوضع مقيَّد بالمعدّل. ومولّد عيّنة الاختبار لم يبقَ على `libx264` كذلك.
> **تكافؤ الجودة (مقيس 2026-09-15 · ثلاثة محتويات حقيقية):** `h264_mf -quality 85` مقابل `libx264 crf18` ⇒ **VMAF +0.06…+0.56** (تكافؤ أو أفضل قليلاً)، مع فرق حجم **−3.6٪ / +37.3٪ / +83.5٪**، و**profile المخرج `Constrained Baseline`** (بلا B-frames) وهو ما يفسّر جزءاً من نمو الحجم. **حدّ الأمانة**: المراجع كانت **مضغوطة** ⇒ الأرقام **نسبية لا مطلقة**، والمحتوى ثلاثة أنواع فقط؛ والتحقق النهائي عين المالك وأذنه.

### نموذج الفصل `UVR-MDX-NET-Voc_FT.onnx` — MIT مع إسناد إلزامي

| البند | التفاصيل |
|---|---|
| المصدر الرسمي | `TRvlvr/model_repo` — نشرة `all_public_uvr_models` (الاسم نفسه، 63.7 MB) ✅ تحقّقنا من وجود الأصل بين 86 أصلاً |
| المطوّرون | فريق Ultimate Vocal Remover — **Anjok07** و**aufr33** (الـREADME: «UVR's core developers trained all of the models provided in this package») |
| الرخصة | **MIT** |
| الإسناد المطلوب | نصّ المشروع حرفياً: «For all third-party application developers who wish to use our models, please honor the MIT license by providing credit to UVR and its developers» ✅ مقروء من README المشروع |
| إسناد مستحسن | بنية **MDX-Net** من KUIELab — رخصتها **MIT** ✅ |

**الإسناد الواجب ظهوره في التطبيق والموقع:** «نموذج الفصل من Ultimate Vocal Remover (Anjok07 & aufr33) — بترخيص MIT».

⚠️ **حدّ أمانة:** بصمة الأصل **غير منشورة** في واجهة GitHub لتلك النشرة (حقل `digest` فارغ)، فلم أستطع إثبات تطابق ملفنا بايتاً ببايت مع الأصل. لكن التطبيق يثبّت بصمة الملف في `repair.rs` ويتحقّق منها عند التنزيل، وبصمة ملفنا المحلي: `sha256:534b2070fcc7df514b13ef660dc8cbb328679c2374d04354a5c42bb14ecce111`.
---

## ٢) مكوّنات تُنزَّل عند الطلب (نشرة `assets-v1`، لا تدخل المثبّت)

| المكوّن | الملفات | الإصدار | الرخصة | كيف تحقّقنا |
|---|---|---|---|---|
| CUDA Runtime (‏cudart · cublas · cublasLt · cufft) | 4 DLL | CUDA **12.8.2** | رخصة NVIDIA لتوزيع CUDA القابلة لإعادة التوزيع | ✅ من سكربت الجلب: `developer.download.nvidia.com/compute/cuda/redist/` |
| cuDNN | 10 DLL | cuDNN **9.12.0** | رخصة NVIDIA (النوع نفسه) | ✅ المصدر نفسه |
| ONNX Runtime CUDA provider | `onnxruntime_providers_{shared,cuda}.dll` | **1.22.0** | **MIT** | ✅ واجهة GitHub: `microsoft/onnxruntime` |
| منفست الأصول | `cuda-runtime-manifest.json` | — | من إنتاج المشروع | ✅ |

**الرخصة الحاكمة:** رخصة المستخدم النهائي لـNVIDIA CUDA (‏CUDA EULA) — والبند المعنيّ بالاسم هو **«1.1.2 Distribution Requirements»** ✅ (تحقّقتُ من وجوده في فهرس الرخصة على `docs.nvidia.com/cuda/eula/index.html`).
**إجراء مطلوب:** يجب شحن نصّ الرخصة أو الإشارة الصريحة إليها مع هذه الأصول في النشرة وفي `assets-v1` — **لم يُنفَّذ بعد.**

---

## ٣) مكتبات Rust (مُصرَّفة داخل الثنائية)

مصدر الأرقام: `cargo metadata` على `src-tauri/Cargo.toml` ⇒ **597 حزمة، منها 596 خارجية**. التوزيع بحسب حقل الرخصة:

| الرخصة | عدد الحزم |
|---|---|
| `MIT OR Apache-2.0` | 282 |
| `MIT` | 131 |
| `Apache-2.0 OR MIT` | 57 |
| `MIT/Apache-2.0` | 28 |
| `Zlib OR Apache-2.0 OR MIT` | 18 |
| `Unicode-3.0` | 18 |
| `Unlicense OR MIT` | 9 |
| `MPL-2.0` | 5 |
| `Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT` | 5 |
| `ISC` | 5 |
| `Apache-2.0` | 4 |
| `Apache-2.0 OR ISC OR MIT` | 3 |

**ملاحظات التزام:**
- الأغلبية الساحقة رخص متراخية (MIT/Apache-2.0/ISC/Zlib) لا تفرض على التوزيع الثنائي أكثر من الإسناد والاحتفاظ بالنصّ.
- `MPL-2.0` (5 حزم) تفرض إتاحة مصدر **الملفات المُعدَّلة** فقط — والمشروع لا يعدّل ملفاتها.
- `Unicode-3.0` (18 حزمة) تتطلّب نصّ الرخصة مع التوزيع.
- القائمة الكاملة قابلة للتوليد آلياً من `Cargo.lock` بالأمر: `cargo metadata --format-version 1`.

---

## ٤) واجهة المستخدم والأيقونات

| المكوّن | الرخصة | كيف تحقّقنا |
|---|---|---|
| Tauri v2 + إضافاته (‏dialog · notification · updater · opener) | **MIT OR Apache-2.0** (الواجهة تعرض Apache-2.0) | ✅ واجهة GitHub: `tauri-apps/tauri` · `tauri-apps/plugins-workspace` |
| Material Symbols (الأيقونات) | **Apache-2.0** | ✅ واجهة GitHub: `google/material-design-icons` |
| Thmanyah Sans / Serif Display | **رخصة مخصّصة مقيَّدة** — انظر القسم ٥ | ✅ نصّ الرخصة مستخرج من داخل ملف الخط |

---

## ٥) الخطوط — خط ثمانية (‏Thmanyah)

**المصدر:** `font.thmanyah.com` · **المالك:** شركة ثمانية للنشر والتوزيع · **التواصل:** `ask@thmanyah.com`
**نصّ الرخصة:** «Thmanyah's Font License — Copyright © 2026, Thmanyah Publishing and Distribution, with Reserved Font Name "thmanyah"» (مستخرج من جدول الأسماء داخل ملف الـOTF نفسه).

### ما تسمح به صراحةً ✅
> «**تضمين برنامج الخط في المستندات أو التطبيقات أو المنتجات** التي تنشئها»
> «تضمين برنامج الخط في مواقع الويب أو تطبيقات الويب… **وذلك فقط كجزء من منتج مُجمَّع أو مُعبَّأ أو مُعَمّى**»

### ما تمنعه صراحةً ✗
> «**إعادة توزيع برنامج الخط أو مشاركته أو رفعه أو استضافته أو إتاحته للتنزيل على أي موقع إلكتروني أو خادم أو منصة رقمية** أو خدمة مشاركة ملفات»
> «**ولا يجوز تنزيل برنامج الخط إلا من الموقع الرسمي لثمانية**»
> «إتاحة برنامج الخط بأي طريقة تُمكّن المستخدمين النهائيين… من استخراج برنامج الخط أو تنزيله… **بما في ذلك عبر التضمين على الويب**»

### المواضع الثلاثة في المشروع وحالتها

| الموضع | الملفات | الحكم **بعد الردّ** | الإجراء |
|---|---|---|---|
| داخل تطبيق الويندوز (مثبّت) | `src/assets/fonts/` 5 ملفات | **مسموح صراحةً** («تضمينه في التطبيقات… والمنتجات البرمجية») | **يبقى الخط** — وتُسحب الملفات من التتبّع وتُحقن عند البناء |
| داخل حزمة إضافة المتصفح | `browser-extension/` 3 ملفات | **مسموح صراحةً** (منتج مُعبّأ) | **يبقى الخط** — وتُسحب من التتبّع مثل التطبيق |
| مستودع المشروع العام | **11 ملفاً متتبَّعة** (5 تطبيق · 3 إضافة · 3 موقع) | **مخالف — والردّ يطلبه صراحةً** | **تُزال من المستودع العام** (بانتظار قرار المالك على آلية حقن CI) |
| خطوط الويب على الموقع | `docs/assets/` 3 ملفات WOFF2 | **لا يجوز**: ملفات في مستودع عام **ومتاحة للتنزيل** من `haramlite.com` | **تُستبدل بخط OFL** (‏GitHub Pages يخدم من المستودع) |
| تاريخ المستودع (history) | 26 ملف خط في التزامات سابقة | **يبقى مكشوفاً** بإزالتها من الشجرة وحدها | قرار المالك — إزالته من التاريخ **تخريبية** ولم تُنفَّذ |

### ردّ الجهة المانحة (2026-09-15 · حاتم سعود — `ask@thmanyah.com`)

> **ما أُذن به صراحةً:** «الترخيص الحالي يتيح استخدام الخط في المشاريع الشخصية والتجارية، بما في ذلك **تضمينه في التطبيقات والمواقع والمنتجات البرمجية**، وذلك وفق الشروط والقيود المنصوص عليها في الترخيص.»
> **ما مُنع:** «لا يسمح الترخيص بإعادة توزيع ملفات الخط أو مشاركتها أو **رفعها أو استضافتها على المواقع أو المستودعات أو المنصات الرقمية**، كما لا يسمح بإتاحة ملفات الخط بطريقة تمكّن المستخدمين أو الأطراف الثالثة من **استخراجها أو تنزيلها أو الوصول إليها كملفات خطوط مستقلة**.»
> **الطلب الصريح:** «فإن **ملفات الخط المرفوعة حاليًا على المستودعات العامة في GitHub لا تدخل ضمن الاستخدامات المسموح بها** في الترخيص، ونأمل إزالتها من المستودعات العامة.»

**قراءة الردّ بدقّة (وتصحيح قراءة سابقة):** الردّ **ليس رفضاً للاستخدام** — بل **إجازةٌ للتضمين في المنتج** + **منعٌ للنشر العام**. وعليه فقول «نُبقيه ونُخفي مجلد `font/` فقط» **غير كافٍ**: الـ**11 ملفاً المتتبَّعة** هي بالضبط ما يسمّيه الردّ «ملفات الخط المرفوعة حاليًا على المستودعات العامة»، وملفات الموقع الثلاثة أشدّ لأنها **متاحة للتنزيل مباشرةً** من الموقع.

### البديل الجاهز (مُحدَّث بعد الردّ)
1. **الموقع**: استبدال خطوط الويب بخط برخصة **OFL** (‏IBM Plex Sans Arabic · Noto Kufi Arabic · Cairo · Almarai) — التغيير محصور في `site.input.css` (أسر الخطوط) و`site.tailwind.config.cjs` (أسماء العائلات) وإضافة ملفات OFL إلى `docs/assets/` ثم إعادة توليد `docs/assets/site.css` بـ`pnpm site:css`؛ والحارس `scripts/check-site-css.cjs` يتحقّق تلقائياً من وجود كل `url(*.woff2)` فيمرّ أو يفشل بنفسه.
2. **المستودع**: سحب الـ11 من التتبّع (`git rm --cached` + `.gitignore`) — الملفات تبقى على جهاز المالك و**البناء المحلي لا يتأثّر إطلاقاً** (التطبيق يقرأ `src/styles.css` بمسار نسبي، والإضافة `popup.css`، والموقع CSS مُولَّد) — **مع آلية حقن في CI** لأن `release.yml` يبني الإصدارات على GitHub ولا يملك الملفات حينها: (أ) مستودع خاص للخطوط + سرّ وصول، أو (ب) بناء الإصدار محلياً ورفع المثبّتات. (والمجموع 1.9 MB لثمانية ملفات ⇒ لا تصلح في «أسرار» GitHub منفردةً.)
3. **التطبيق والإضافة**: يبقيان على خط ثمانية ✅ — مسموح صراحةً في الردّ.
4. **لا يُنصح به**: تشفير الملفات ورفعها مشفَّرة — التزامٌ مبنيّ على ثغرة لفظية بعد طلب إزالة صريح، والثقة أهمّ من 1.9 MB.

---

## ٦) المراجع المجمَّدة (لا تُوزَّع — مراجع خوارزمية فقط)

| المرجع | الرخصة | كيف تحقّقنا |
|---|---|---|
| `nomadkaraoke/python-audio-separator` | **MIT** | ✅ واجهة GitHub |
| `Anjok07/ultimatevocalremovergui` (‏UVR5) | **MIT** | ✅ واجهة GitHub |

> هذه ملفات قراءة فقط في تدقيق المحرّك، **لا يُشحن أي منها**. وثوابت المحرّك المستعملة موثّقة في `docs/AUDIT.md`.

---

## ٧) الإضافة (Chrome/Edge)

لا اعتماديات خارجية: `browser-extension/` كود خام بلا مكتبات، ما عدا ملفات الخط (القسم ٥). والصلاحيات ثلاث فقط، وبلا أي طلب شبكة.

---

## ٨) قائمة التحقّق قبل كل إصدار

- [x] **نصّا الرخصة يُشحنان مع التوزيع** — `licenses/LGPL-3.0.txt` (‏7,652 بايت · `sha256:e3a994d82e644b03a792a930f574002658412f62407f5fee083f2555c5f23118`) **و**`licenses/GPL-3.0.txt` (‏35,149 بايت · `sha256:3972DC9744F6499F0F9B2DBF76696F2AE7AD8AF9B23DDE66D6AF86C9DFB36986`)، كلاهما من `gnu.org` — والثاني لازم لأن LGPLv3 يُحيل إلى GPLv3. المجلد مُدرَج في موارد الحزمة (`tauri.conf.json` ⇒ `"../licenses": "licenses"`) فيصل المستلم داخل مجلد التثبيت.
- [x] **مصدر FFmpeg المقابل — مُقضى بنظافة بعد تبديل 0.2.5**: الثنائي المحزوم **LGPLv3** (‏`--enable-version3` بلا `--enable-gpl`)، ومراجعة المصدر **يعلنها الثنائي نفسه** (`n8.1.2-52-g5a03dfa0f6`)، و**الجهة المانحة تنشر سكربتات بناءها** (`BtbN/FFmpeg-Builds`: `build.sh` · `scripts.d/` · `variants/` · `patches/`) ⇒ سقطت مشكلة «رقع المانح غير المنشورة» التي كانت مع `gyan.dev`. والبصمات مثبَّتة على `assets-v1`.
- [ ] **التحقّق أن `bin/` المحزوم فعلاً هو بايتات LGPL** عند كل إصدار: `ffmpeg -version` يجب أن يُظهر **غياب** `--enable-gpl`، وبصمة الملف يجب أن تطابق الأصل المنشور (`ffmpeg-lgpl.exe`) — تُدرَج في قائمة ما قبل النشر.
- [x] رخصة النموذج موثّقة **MIT** مع الإسناد المطلوب لـUVR — انظر القسم ١.
- [x] **رخصة NVIDIA مُشار إليها**: في هذا الملف (البند 1.1.2) · وفي ملاحظات نشرة `v0.2.3` · وفي ملاحظات إصدار `assets-v1` نفسه (حيث تُوزَّع الأصول) وفي نصّ سير `cuda-assets.yml` لما بعده.
- [x] رخص Tauri وإضافاته مؤكَّدة: **MIT OR Apache-2.0** — القسم ٤.
- [x] **إسناد UVR ظاهر للمستخدم** — في «حول البرنامج» (`src/main.ts`: قائمة الإسنادات: اسم النموذج + Ultimate Vocal Remover + Anjok07 و aufr33 + MIT + رابط) وفي تذييل الموقع (`docs/index.html`).
- [x] حالة إذن خط ثمانية محدَّثة: **أُرسل 2026-09-15 · ووصل الردّ 2026-09-15** (‏حاتم سعود) = **إجازة تضمين في المنتج** + **منع نشر عام** + **طلب إزالة الـ11 ملفاً من المستودعات العامة** — التفاصيل في القسم ٥.
- [ ] **إزالة ملفات خط ثمانية الـ11 من المستودع العام** (وما يتبعها: استبدال خطوط الموقع بـOFL · وآلية حقن الخط في بناء CI) — بانتظار قرار المالك، التفاصيل في القسم ٥.
