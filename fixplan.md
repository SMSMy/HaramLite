# fixplan.md — خطة إصلاح جودة الفصل (MDX)

> **الحالة**: قراءة فقط. لم يُعدَّل أي ملف في الكود.
> **القاعدة الحاكمة**: متغيّر واحد لكل خطوة · قياس قبل/بعد · **ولا «إصلاح» بلا سطر من المرجع** ✗

---

## 0. المرجعان — **مثبَّتان برقم commit** ✓✓

الأرقام المستشهد بها في هذا الملف أُخذت من النسخ **المجمَّدة** التالية، وتحقّقت مطابقتها بـSHA-256 ✓:

```bash
# audio-separator — ما نسخه كودنا
curl -sSL https://raw.githubusercontent.com/nomadkaraoke/python-audio-separator/7a1c06f42fb5c0a7dd5dcdf45bead5bcababdbae/audio_separator/separator/architectures/mdx_separator.py -o mdx_ref.py
# SHA-256 = F1A647D5D94F938F…  · 24,823 بايت

# UVR5 — ما يقارن به المستخدم  (لم يتغيّر منذ 2023-10-18)
curl -sSL https://raw.githubusercontent.com/Anjok07/ultimatevocalremovergui/b20ab2508066b0372631a1b3b5d6b0e04b45d7eb/separate.py -o uvr5.py
# SHA-256 = D3D1FD3288491895…  · 74,033 بايت
```
**الأسطر المؤكَّدة في النسختين المجمَّدتين** (تحقّقتُ منها بعد التجميد ✓):
```
UVR5  :499  source = self.demix(mix)                    :514  secondary = … mix.T − source.T
      :615  source = source * self.compensate           :389  spec_utils.normalize(source, …)
AS    :169  mix = spec_utils.normalize(wave=mix, …)     :172  source = self.demix(mix) * peak
      :195  secondary = (−primary * compensate) + mix.T
```
> **مهم**: إن نزّلت من `main`/`master` بدل الرقم أعلاه ✗ فقد تنزاح الأسطر ✗ وتبطل كل الأدلة ✓.

---

## 1. الخلاصة — **انحرافان عن UVR5** (لا «عيبان» مطلقان)

**إطار حاسم**: كودنا **منفّذ أمين لـ`audio-separator`** ✓ — لكن **مرجع المستخدم هو UVR5** ✗ — ومن هنا الانحرافان ✓:

| # | الانحراف | UVR5 | عندنا |
|---|---|---|---|
| **A** | **بوابة المقاطع المشتبهة** | يعالج **الملف كله** ✓ (`:499`) | مصنّف كثافة يحدّد المقاطع ✗ — **حالتان**: لا مقاطع صامتة ⇒ `suspect=[]` ⇒ **لا فصل إطلاقاً** ✗ (`silence.rs:96-97`) · أو تغطية جزئية ⇒ حذف الغناء خارج المقاطع ✗ |
| **B/E** | **مسار المستوى (Level path)** | بلا تطبيع دخل ✓ (`:1290` librosa float32) · `source *= compensate` **على الغناء** ✓ (`:615`) · `inst = mix − source` ✓ (`:514`) · التطبيع **عند الكتابة** ✓ (`:389`) | تطبيع الدخل إلى 0.9 ✗ · `vocals = demix*peak` ✗ · `inst` من المُطبَّع ✗ · بلا تطبيع مخرج ✗ |

### لماذا B/E يفسّر «نزفاً في الآلي» ✓
عند `peak < 0.9` (الشائع في الفيديو/يوتيوب ✓): التطبيع **لا يفعل شيئاً** ✓ ثم **يُضرب الغناء في `peak`** ✗ ⇒ عند 0.5 ⇒ **−6 dB** ✗ ⇒ **يبقى نحو نصف الغناء في الآلي** ✓✓ = «فصل أقل نظافة» ✓.

### ترتيب التنفيذ — **مشروط** ✓
```
الخطوة ١ (A)  ⟶  قياس  ⟶  أُغلقت الفجوة؟  ⟶ نعم: توقّف هنا ✓  ·  لا: الخطوة ٢ (B/E) ✓
```

### ⚖️ قرار يخصّ المالك — يحتاج موافقة صريحة قبل الخطوة ٢
الخطوة ٢ **تترك `audio-separator` عمداً** ✗ لتطابق UVR5 ✓ (حذف تطبيع الدخل ✓ · `*COMPENSATE` على الغناء ✓ · `inst` من الأصل ✓) ⇒ **هذا تغيير مرجع لا إصلاح محايد** ✓ ⇒ **يُنفَّذ فقط إن بقي فرق بعد الخطوة ١، وبموافقة صريحة** ✓✓ — ويُوثَّق في `AUDIT.md` كقرار مقصود ✓.

### ✗ مرفوض (لا تطبّقه)
| الاقتراح | لماذا |
|---|---|
| `(mix[j]*peak) − vocals[j]*COMPENSATE` | معامل التخفيض `g` لا `peak` ⇒ −0.92 dB / −6.02 dB |
| `vocals *= peak/0.9` أو `/g` | UVR5 **لا يطبّع الدخل أصلاً** ✗ ⇒ لا معامل يُعكَس ✓ |
| `silence.rs:96` تُرجع `vec![(0,n)]` | مشتركة (`effects.rs:105` · `pipeline.rs:363`) ⇒ تُفقد مسار `remux` ✓ (`media.rs:527`) + استدلال على صمت كامل ✗ |
| «البوابة وحدها تكفي» | صحيح **لـA** ✓ — ولا يصلح B/E ✗ (يظهر عند `coverage=1`) |

---

## 2. القياس — قبل أي تعديل (٣٠ دقيقة)

**ق١ — سجلّ التطبيق**: ابحث عن `SEPARATE-AB-REPORT` و`mix analysis: … coverage=…`
`coverage` منخفضة/صفر ⇒ **A** ✓ · `coverage≈1` والفرق باقٍ ⇒ **B/E** ✓

**ق٢ — لا تستخدم `vox+inst ≟ mix` ككاشف** ✗ — لا المرجع يطبّقها ✓؛ فرق `0.021·vox` (~2%) **متوقع** ✓.

**ق٣ — مستوى الملف**: اطبع `peak` ⇒ `< 0.9` ⇒ **B/E فعّال** ✓ (الغناء مضروب في `peak` ✓) · `≥ 0.9` ⇒ العيب أضعف ⇒ ركّز على **A**.

**ق٤ — الضبط الموجب** (من مراجع ٢ ✓): أدخل **ثانيتين صمت رقمي** في منتصف نسخة من المقطع ⇒ إن عاد الفصل ⇒ **A مؤكد** ✓✓

**ق٥ — الطريق الفاصل** (من مراجع ٤ ✓ — الأقوى): خذ **نفس WAV** الذي يخرجه `normalize_for_engine` وافتحه **في UVR5** (MDX · Voc_FT · seg 256 · overlap Default) ⇒ اختفى الفرق ⇒ السبب **المدخل** ✓ / بقي ⇒ **المحرك** ✓

**ق٦ — round-trip للـSTFT**: `x̂ = ISTFT(STFT(x))` · `err = x − x̂` ⇒ ≈0 (SNR ≳60 dB) ⇒ STFT سليم ✓ (يستبعد فرضية الـSTFT ✓) · `x̂ ≈ 0.5x` أو تشويش طور ⇒ أصلح ISTFT **قبل أي شيء** ✗

---

## 3. الإصلاح

### ✦ الخطوة ١ — إزالة البوابة (A) — **التزام مستقل**
```rust
// separator.rs داخل separate(): أزل التقييد بالمقاطع
let vocals_src = demix(&mut session, &mix, &|p| progress(p))?;   // الملف كله — كما UVR5 :499
```
يسقط معها: التصفير خارج المقاطع ✗ · `splice_span` والـ`fade` ✗ · والحالة القصوى `is_empty` ✗.
وتبقى `analyze_mix` **للتشخيص فقط** ✓ (تسجيل `dense`/`coverage` ✓) لا كبوابة ✗.
**الحشو**: استعمل نمط UVR5 `pad = gen_size + trim − (L % gen_size)` ✓ (لا `2*TRIM` وحده ✓ — فرق عند الذيل ✓).

**تحقّق**: `coverage=1.00` في السجلّ ✓ + `ق٤` يصير بلا معنى ✓ (الفصل يعمل أصلاً ✓)
**اختبار سلبي**: أعِد البوابة على ملف بلا صمت ⇒ يجب أن يعود `spans=0` وفصل غائب ⇒ استعادة ✓

### ✦ الخطوة ٢ — مسار المستوى (B/E) — **مشروطة بموافقة المالك** ⚖️
```rust
// احذف تطبيع الدخل من مسار الاستدلال ✗
let mut vocals = demix(&mut session, &mix, …)?;                              // المستوى الأصلي ✓
for c in vocals.iter_mut() { for v in c.iter_mut() { *v *= COMPENSATE; } }   // :615 ✓
let instrumental = [ mix[0] − vocals[0], mix[1] − vocals[1] ];                // :514 ✓
```
⚠ **وهذا يخالف `audio-separator`** ✓ (`:169` يطبّع ✓ و`:195` يطرح من المُطبَّع ✓) — **مقصود** ✓: نطابق UVR5 لأن المستخدم يقارن به ✓ ⇒ **وثّقه في `AUDIT.md`** ✓.
⚠ **تطبيع المخرج** (UVR5 يفعله ✓ `:389`): **خارج هذه الخطوة** ✗ (متغيّر واحد ✓) — بند تالٍ ✓.

**تحقّق**: `vox + inst ≈ mix` ✓ (بفارق `0.021·vox` ✓ = سلوك UVR5 ✓) + `peak` لم يعد يؤثر على مستوى الغناء ✓

### ✦ الخطوة ٣ — الاختبار الذهبي (المتبقّي)
شغّل Python **مرة** (`audio-separator` + نفس الملف) واحفظ: `window` · `STFT` · `model_out` · `result` · `divider` · `tar_waves` ⇒ قارن Rust بها بحدّ **< −60 dB**.
**البنود المفتوحة**: **النافذة** (`stft.rs` periodic ✓ مقابل `separator.rs:495` symmetric ✓) · **`overlap`** (عندنا 0.25 ✗ وUVR5 Default = `chunk − n_fft` = 253440 ⇒ ≈2.9% ✓ — تداخلنا أكبر ✓ **يُفترض** أن يساعد ✓ ولا تُسلّم به بلا ذهبي ✗) · **تطبيع المخرج** (`:389` ✓ ولا نفعله ✗).

### ✦ الخطوة ٤ — تحسينات مجانية
1. `media.rs:223` `pcm_s16le` ← `pcm_f32le` ✓ — الأهمّ ليس الكمّ (−96 dBFS ✗) بل **القصّ** بعد الـresample ✗
2. `-af aresample=resampler=soxr` ✓
3. `separator.rs:673-674`: التعليق صحيح **بشرط** أن المقاطع تغطي كل غير-الصمت ✓ (فرضية تصميم A ✓) ⇒ وضّح الشرط ✓ لا تنفي المضمون ✓

---

## 4. قواعد إلزامية للمنفّذ

1. **متغيّر واحد في كل تجربة** ✗ — لا تجمع خطوتين.
2. **قِس قبل/بعد** ✓ — لا تحكم بالسمع وحده.
3. **لا «إصلاح» بلا سطر من المرجع** ✗✗ (ونزّل من الرقم المجمَّد في §0 ✓).
4. **اختبار سلبي لكل تغيير** ✓ — أفسد عمداً ⇒ يجب أن يفشل ⇒ استعادة.
5. **اقرأ الناتج لا نيّة الأمر** ✗ — لا تُعلن نجاحاً قبل قراءة الملف بعد الكتابة ✓.
6. **لا تثق بتعليق** ✗ — اقرأ التنفيذ ✓.
7. **التزام مستقل لكل خطوة** ✓ · وسجّل في `docs/AUDIT.md` ✓ (الموضع · الأثر · الدليل · ما استُثني ولماذا).

---

## 5. ما ثبتت **مطابقته للمرجع** — لا تلمسه ✗✗

| البند | دليل المرجع | كودنا |
|---|---|---|
| `normalize` **على الدخل** | AS `:169` | ✓ مطابق لـ`audio-separator` |
| **استعادة** `* peak` للغناء | AS **`:172`** | ✓ مطابق لـ`audio-separator` |
| الطرح من **المُطبَّع** + `compensate` | AS `:195` | ✓ مطابق لـ`audio-separator` |
| **overlap-add** بنافذة + `divider` | AS `:310` · `:351-353` · `:409` | ✓ مطابق (`separator.rs:496-497`) |
| خطوة `(1−overlap)*chunk_size` | AS `:348-409` | ✓ مطابق (`:492`) |
| `dim_f` وحشو النطاق | AS `:238` | ✓ مطابق (`N_BINS=3841` ✓) |

> **تنبيه**: الخطوة ٢ **تخرج عمداً** عن البنود الثلاثة الأولى ✓ (لتطابق UVR5 ✓) ⇒ إن نُفِّذت **فحدّث هذا الجدول** ✓ واذكر السبب ✓ — ولا تتركه متناقضاً ✗.
> وقد اقترح مراجعون تغيير أربعة من هذه ✗ — **لا تُغيَّر** ✗ بلا قرار موثّق ✓.

---

## 6. سجلّ القياس (يملؤه المنفّذ)

| الخطوة | الملف | coverage | peak | زمن الاستدلال | ملاحظة |
|---|---|---|---|---|---|
| ق١ | | | | | |
| ق٣ | | | | | |
| ق٤ | | | | | |
| ق٥ | | | | | |
| بعد الخطوة ١ | | | | | |
| بعد الخطوة ٢ | | | | | |
