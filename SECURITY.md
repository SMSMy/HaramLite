# سياسة الأمن — HaramLite

## الإبلاغ عن ثغرة (قناة خاصة)

استخدم **الإبلاغ الخاص عن الثغرات** في GitHub — وهو القناة الخاصة الرسمية لهذا
المستودع، ولا يوجد بريد أمني مخصص:

<https://github.com/SMSMy/HaramLite/security/advisories/new>

- **لا تفتح Issue عاماً لثغرة أمنية.** الـIssues العامة مخصّصة للأعطال
  والاقتراحات فقط: <https://github.com/SMSMy/HaramLite/issues/new>.
- إن لم يظهر لك زر الإبلاغ الخاص (الخاصية تُفعَّل من إعدادات المستودع)، فاكتب
  Issue **بلا أي تفاصيل تقنية** تطلب فيه قناة خاصة، وسنفتحها معك.
- لا تُرفق في أي تقرير عام: توكن بوت تيليجرام، `api_hash`، ملف
  `updater.key`، أو أي مفتاح/سرّ — ولا سجلات غير منقّحة (`logs/`).

## ما يهمّنا (نطاق التطبيق)

HaramLite تطبيق سطح مكتب يعمل محلياً على ويندوز: لا حسابات ولا خدمات سحابية.
الثغرات ذات الأولوية عندنا:

- **مسار الإصلاح الذاتي والتنزيل**: التحقق من أصول `assets-v1` ومنفست CUDA
  (بصمات SHA-256، وأسماء ملفات المانيفست، ومسار التثبيت) — أي التفاف على التحقق
  أو كتابة خارج مجلد التثبيت.
- **أسرار المستخدم**: تشفير توكن تيليجرام و`api_hash` بـDPAPI، وعدم ظهورهما في
  السجل أو في رسائل الخطأ.
- **جسر المتصفح (Native Messaging)**: مسارات الملفات، حدود مجلد الطلبات،
  ومنع تنفيذ ما يرسله المتصفح خارج ما هو مقصود.
- **حقن الأوامر** في وسائط `ffmpeg`/`ffprobe`/`yt-dlp` أو في مسارات الملفات.
- **الموقع** في `docs/` (ينشر على haramlite.com) وسكربتات البناء في `scripts/`.

## ما نطلبه في التقرير

- وصف الثغرة وأثرها العملي، وخطوات إعادة الإنتاج.
- إصدار التطبيق (من الإعدادات ← «حول البرنامج») ونظام التشغيل.
- أي دليل (لقطة/سجل منقّح) يساعد على التأكيد.

## ما هو خارج النطاق

- ثغرات في مكوّنات الطرف الثالث نفسها (**FFmpeg** · **yt-dlp** ·
  **ONNX Runtime** · نموذج UVR) — أبلغ الجهة الأصلية، ويكفينا إشعار لنحدّث النسخة.
  وقائمة المكوّنات ورخصها في [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md).
- ما يتطلّب أصلاً صلاحيات مسؤول على الجهاز، أو جهازاً مخترقاً سلفاً.
- تقارير آلية بلا خطوات إعادة إنتاج (ماسحات عامة).

## ما نتعهّد به

نبذل جهداً معقولاً للتأكيد والرد، **ولا نلتزم بجدول زمني تعاقدي** ولا ببرنامج
مكافآت: هذا مشروع صغير. سنذكر فضل المُبلِّغ في ملاحظات الإصدار إن رغب، وسنُعلمك
عند الإصلاح قبل النشر متى أمكن.
