# التطورات المستقبلية (Future Developments)

> **📦 مؤرشفة (Archived):** هذه الوثيقة استُبدلت بـ [`IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md) — الخطة التنفيذية التفصيلية المعتمدة 100% من المالك (Sprints A→F، الإصدار 0.2.0). أُبقي هذا الملف كسجل تاريخي للرؤية الأصلية فقط.

بناءً على التقييمات والمراجعات، هذه قائمة بالميزات والتحسينات المخطط إضافتها لمشروع **HaramLite** في الإصدارات القادمة. جميع هذه الميزات تتوافق مع فلسفة التطبيق (أداة رأي، الخصوصية، العمل محلياً، وعدم تعقيد واجهة المستخدم).

## 1. التوزيع وتجربة أول تشغيل (UX & Distribution)
- **الاستغناء عن Tailwind CDN:** ✅ **تم (Sprint A)** — بناء ملفات CSS محلياً أثناء الـ Build لضمان عمل التطبيق بوضع Offline بنسبة 100% دون الاعتماد على خوادم خارجية.
- **إعداد بيئة العمل تلقائياً (First-run Setup):** ✅ **تم (Sprint C)** — الاستغناء عن النقل اليدوي لملفات النظام. عند أول تشغيل، سيقوم البرنامج بتحميل نموذج الذكاء الاصطناعي (63MB) وأدوات `ffmpeg` تلقائياً مع شريط تقدم، أو سيتم دمجها مسبقاً داخل مثبت NSIS.
- **تحديث README للمستخدم النهائي:** ✅ **تم (Sprint F)** — إعادة تصميم واجهة المستودع (GitHub) لتشمل لقطات شاشة، زر تحميل مباشر للبرنامج (Installer)، وشرح مبسط لمتطلبات التشغيل، بدلاً من التفاصيل البرمجية.

## 2. تحسينات المعالجة وجودة الحياة (Quality of Life)
- **المعاينة السريعة (Quick Preview):** ✅ **تم (Sprint B)** — إضافة خيار لمعالجة أول 10 إلى 15 ثانية فقط من الملف، مما يتيح للمستخدم سماع جودة العزل واتخاذ القرار قبل معالجة ملفات طويلة كالمحاضرات.
- **إشعارات الانتهاء (System Notifications):** ✅ **تم (Sprint B)** — إرسال إشعار نظام (Notification) وإصدار تنبيه صوتي خفيف عند اكتمال معالجة الملفات أو الطابور، لتنبيه المستخدم إذا كان التطبيق يعمل في الخلفية.
- **التحديث الذاتي (Auto-updater):** ✅ **تم (Sprint C)** — تفعيل `tauri-plugin-updater` ليتمكن التطبيق من اكتشاف وتثبيت تحديثاته الخاصة مستقبلاً، تماماً كما يفعل حالياً مع `yt-dlp`.

## 3. تحسينات واجهة المستخدم (UI Additions)
- **زر "حول البرنامج" (About Button):** ✅ **تم (Sprint B)** — إضافة نافذة بسيطة تعرض معلومات الإصدار، المطورين، وحقوق النماذج المستخدمة (أهل الفضل).
- **زر "الإبلاغ عن مشكلة" (Report Issues):** ✅ **تم (Sprint B)** — رابط سريع يوجه المستخدم إلى صفحة الـ Issues في GitHub لرفع المشاكل أو الاقتراحات بسهولة.

## 4. ميزات متقدمة للتكامل (Advanced Integrations)
- **مجلد المراقبة (Watch Folder):** ✅ **تم (Sprint D)** — ميزة يتم تفعيلها واختيار المجلد الخاص بها من قائمة **الإعدادات**. عند التفعيل، يقوم البرنامج تلقائياً بالتقاط أي ملف يوضع في هذا المجلد ومعالجته في الخلفية.
- **التوافق مع المتصفح (Browser Extension Integration):** ✅ **تم (Sprint E)** — بناء إضافة للمتصفح (بناءً على نواة مجلد `Extension` الحالي) تتخاطب مع التطبيق المحلي (Desktop Backend)؛ لتسهيل إرسال الروابط أو معالجة الصوت مباشرة من المتصفح مع الحفاظ على الخصوصية وعدم رفع الملفات لخوادم سحابية.
