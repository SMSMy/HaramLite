# AGENT.md — دليل الوكلاء (كيف نعمل في هذا المشروع)

> **الغرض**: هذا الملف هو العقد بين المالك وأي وكيل (نموذج ذكاء اصطناعي) يعمل على هذا
> المستودع — إشرافاً أو تنفيذاً. مقصوده أن يجعل عمل الوكلاء **قابلاً للتصديق**: لا
> ادّعاء بلا قياس، ولا «تمّ» بلا دليل، ولا صمت عن حدّ لم يُتحقَّق منه.
>
> **قابل للنقل**: الأقسام ٠–٧ عامّة تصلح لأي مشروع. الأقسام المعلَّمة بـ«عدّلها
> لمشروعك» (٨–٩) خاصة بهذا المستودع: أوامر البوابات، وبنية الملفات، والفخاخ البيئية.
> إن نسختَ الملف لمشروع آخر فاستبدل تلك الأقسام فقط.
>
> **اسم متعارف**: صار المتعارف عليه في أدوات الوكلاء ملف `AGENTS.md`؛ هذا الملف
> اسمه `AGENT.md` بحسب قرار المالك. إن أردت أن تكتشفه الأدوات تلقائياً فاجعل
> `AGENTS.md` نسخةً منه أو رابطاً إليه.

---

## ٠) القاعدة الحاكمة — كل ما بعدها تفصيل لها

> **لا ادّعاء بلا قياس، ولا قياس بلا مرجع.**
>
> كل جملة تقول «تمّ» أو «يعمل» أو «أسرع» أو «آمن» يجب أن يقف خلفها **دليل يمكن
> لغيرك إعادة إنتاجه**: أمرٌ شُغِّل، أو رقمٌ مقيس، أو `file:line`، أو مصدر.
> وما لم تقِسه فقُل صراحةً إنك لم تقِسه. الصمت عن حدّ **خطأ**؛ الاعتراف به **قيمة**.

هذه القاعدة ليست شكلاً: هي التي تفرّق بين تقرير يُبنى عليه قرار وتقرير يُجمِّل.

---

## ١) استطلع قبل أن تحكم

**القاعدة**: قبل أي تعديل أو حكم، **اقرأ الملف/الكود/الإعداد الفعلي**. لا تعتمد على
ذاكرتك ولا على ملخّص سابق ولا على اسم ملف يوحي بمحتواه.

**لماذا**: في هذا المشروع ادّعى مراجع خارجي أن `index.html` فيه «منطق واجهة كبير
45.9KB»؛ القياس أظهر **95339 بايت** وكتلة `<style>` بطول 160 حرفاً وJS بطول 8640
حرفاً ⇒ الادّعاء مردود بالأرقام لا بالرأي.

**كيف**:
- افتح الملف واقرأه، واستشهد بالسطر (`src/main.ts:279` لا «في الواجهة»).
- قبل تكرار حكم سابق، تحقّق أنه ما زال صحيحاً (الكود يتحرّك).
- إن كان الملف مذكوراً في ملخّص، **اقرأه من جديد** قبل التعديل.

---

## ٢) قِس قبل/بعد، واكتب الرقم بمرجعه

**القاعدة**: كل تغيير يمسّ السلوك أو الحجم أو السرعة يُقاس قبله وبعده، ويُذكر
الرقم في رسالة الالتزام وتقريرك.

**كيف**:
- الرقم بلا وحدة ولا سياق ليس قياساً: «100050 ⟶ 100736 بايت» نعم، «تحسّن الحجم» لا.
- إن كان القياس يعتمد على بيئتك، قل ذلك: «محلياً على هذا الجهاز»، ولا تنسبه إلى CI.
- الأرقام تتبدّل: إن قِستَ رقماً جديداً فحدِّث الوثيقة واذكر أن القديم مُبطَل.
  (حصل فعلاً: رقم اختبارات مخمَّن «135+» ⟶ مقيس 163 ⟶ 164 ⟶ 170 بعد إضافات.)
- **قياسان أفضل من قياس**: إن أمكن، أكّد الرقم من مصدرين مستقلّين. مثال: بصمة
  الإصدار تأكّدت من ثلاثة: حسابي المحلي، ومحتوى الملف المرفوع، وواجهة GitHub.
- **ولا تُقَس إلا شجرة ساكنة وملتزَمة** (درس 2026-09-21): شغّلتُ `clippy` في شجرة عامل **وهو
  يُحرّرها** فقرأتُ ١٦ تحذيراً بدل ١٤، وكدتُ أعترض على رقمه الصحيح؛ ثم سقط البناء بعشرة أخطاء
  تصريف لأن الملف كان **معدَّلاً وغير ملتزَم**. القاعدة: `git status` فارغ + `git rev-parse HEAD`
  مثبَّت قبل القياس وبعده، وإلا فقِس **التزاماً** (`git worktree add --detach <sha>`) لا شجرةَ عمل.
  وأي رقم يخالف رقم عامل يُحقَّق منه في السكون **قبل** صياغة اعتراض.

---

## ٣) لكل تغيير اختبار سالب (مُفسِد)

**القاعدة**: **حارس لم يسقط قط ليس حارساً.** أي حارس/بوابة/فحص تضيفه أو تعدّله يجب
أن تُثبت أنه **يرى الخطأ**: تُفسد الشيء عمداً فيسقط، ثم تُعيده فينجح.

**لماذا**: في هذا المشروع مرّت نسخة من حارس مزامنة الإضافة (مئات الفحوص) وهي **لا
ترى** حذف الحمولة كاملة (‏`hist`/`keptBefore`)؛ مُفسِد واحد كشف الثغرة، فأُغلقت بأربعة
فحوص سلوكية وأربعة مُفسِدات. وحارس تكافؤ الإعدادات كان يمكن «إسكاته» بتعريف دالة
مكرّرة — حتى صار يرفض صفر تعريف أو تعريفين.

**كيف**:
- اكتب المُفسِد في تقريرك: **ما عبّثته · الأمر · النتيجة الحرفية**.
- الأنواع المفيدة: حذف الحمولة · قلب شرط · تغيير اسم/مسار · تعريف مكرّر · قيمة حدّية.
- **لا تقبل مُفسِداً باطلاً**: إن لم يغيّر شيئاً فسيعطي «نجاحاً» كاذباً. تحقّق أن
  الاستبدال وقع فعلاً (حدث فعلاً: محاولة حقن بعلامات اقتباس مفردة في ملف يُصغَّر
  بعلامات مزدوجة لم تُغيّر شيئاً، وكادت تُثبت «تطابق» كاذباً).
- إن لم يكن الاختبار السالب ممكناً، **قُل ذلك صراحةً** ولا تدّعِه.

---

## ٤) أمانة الحدود — ما لم تتحقّق منه يُقال

**القاعدة**: في كل تقرير قسمٌ صريح: **«ما لم أستطع التحقّق منه»**. لا تدفنه في ثنايا
الكلام ولا تصوغه بصيغة توحي بالإنجاز.

**أمثلة حقيقية من هذا المشروع** (انسخ نمطها لا نصوصها):
- «لا متصفّح في هذه البيئة ⇒ **لم أرَ الصفحة بعيني**؛ التحقّق آلي بتشغيل سكربت
  الصفحة نفسه في Node مقابل واجهة GitHub الحقيقية».
- «لا عتاد GPU ⇒ **لا جلسة CUDA حقيقية**؛ المقيس منطق الكشف/القرار بمجلدات مؤقتة».
- «مصفوفة الاختبار اليدوي **لم تُملأ** لأنها كلها تحتاج ويندوز/عتاداً/متصفّحاً».
- «لا مُشغِّل اختبارات واجهة في المستودع أصلاً ⇒ الدليل **بنيوي/إحصائي** لا سلوكي».

**قاعدة فرعية**: التمييز بين **تحليل ساكن** و**تشغيل حيّ** في كل جملة تحتمل الاثنين.

---

## ٥) أعلن أخطاء أدواتك أنت — لا تخفِها

**القاعدة**: إن أعطت أداتك نتيجة مشكوكاً فيها، أو انهارت، أو كان فحصك باطلاً،
**اذكر ذلك في التقرير** وصحّح الأداة قبل أن تبني على نتيجتها.

**لماذا**: أدواتي أخطأت في هذه الجلسة أكثر من مرة، وكل خطأ أُعلن صار سطراً في السجل:
- فحص «يحوي البصمة؟» أعطى نتيجة باطلة لأن المحتوى يعود **مصفوفة بايتات** لا نصاً.
- `process.exit()` مع `fetch` معلّق أسقط Node على ويندوز (`libuv` assertion) فأعطى
  رمز خروج غريباً — الخلل في الأداة لا في المنتج.
- مقارنة حزمتين أعلنت «فرقاً» سببه **متغيّر مُصغَّر** داخل قالب نصّي (`"${he}"` ⟶ `"${ge}"`).
- اختبار «الشجرة نظيفة» فشل لأن الأداة افترضت شجرة نظيفة وفيها تعديلات مشروعة.

**الفائدة العملية**: الخطأ المُعلَن يوفّر على من بعدك ساعة، والخطأ المخفي يبني قراراً
على رمل.

---

## ٦) لا تُصلح ما لم تُثبت أنه معطوب · ولا تغيّر سلوكاً بلا تصريح

**القاعدة**: إن لم تُثبت العطل بقياس فليس عندك عطل — عندك فرضية. وكل تغيير سلوك
(ولو بدا تحسيناً) يُصرَّح به في التقرير وفي رسالة الالتزام.

**مثال**: وكيل أضاف حالات فشل CUDA وكتب في التعليق «هذا تشخيص لا سلوك». المراجعة
وجدت **فرقاً سلوكياً واحداً**: طلب CUDA مع مكتبات كاملة على جهاز بلا تعريف لم يعد
يُحاوَل (البوابة القديمة كانت تفحص الملفات وحدها). صُحّح التعليق، وأُثبت الفرق
بمُفسِد: إسقاط شرط وجود الكرت ⇒ سقوط اختبار واحد بالاسم. **الادّعاء «لا تغيير سلوك»
لم يُقبل، بل قوبل بقياس.**

---

## ٧) تفويض الوكلاء — كيف نوزّع العمل

**القاعدة**: يُفوَّض العمل المستقلّ الواسع إلى وكلاء في **أشجار عمل معزولة**، بواحد
منفصل لكل مسار، مع **نطاق مكتوب** و**ممنوعات صريحة**، ومراجعة إشرافية لكل فرق.

**بنية التكليف (انسخها)**:
1. **السياق**: المستودع، شجرة العمل (أوامر الإنشاء الحرفية)، ما يجب تثبيته.
2. **الممنوعات**: لا `push` · لا `rebase` · لا `reset` · لا لمس `docs/` أو أي مجلد
   يملكه المشرف (كي لا تتصادم الفرق).
3. **المطلوب بالضبط**: بند بند، بمراجع `file:line`، وبما يعنيه «أُنجز».
4. **قواعد الجودة**: قياس · اختبار سالب · `#[allow(dead_code)]` ممنوع · نمط الكود.
5. **البوابات**: الأوامر كما هي، وتشغيلها قبل/بعد.
6. **قالب التقرير**: جداول محدّدة + قسم «ما لم أتحقّق منه» + **إفصاح عن أي حادثة**
   (سابقة: وكيل نفّذ `git reset --hard` فأفقد شجرته واستعادها من `reflog` — الإفصاح
   جعل الحادثة مُراجَعة؛ وإخفاؤها كان سيجعل قياساته كلها مشكوكاً فيها).

**قاعدة الفحص الأمني — احكم على القيمة لا على النصّ**: أي حارس يفحص **تمثيلاً نصّياً**
(بادئة، شكل محارف، قائمة صيغ مكتوبة) يُخترق بمرادفة واحدة. سابقة مسجَّلة: حارس رفض
المضيفات المحلية كان يقارن `host == "::1"` وبادئات `fc`/`fd`/`fe8`، فمرّت منه **ثلاث
صور متكافئة** (`[0:0:0:0:0:0:0:1]` · `[::]` · `[::ffff:7f00:1]`) — ولم يكشفها إلا
**هجوم المشرف المستقل** بحالات ليست في جدول الوكيل. **فطبّقها على نفسك**: بعد أن
يُسلّم وكيلٌ حارساً أمنياً، **هاجمه بحالات من عندك** (صور متكافئة، ترميزات بديلة،
تفاف حول التطبيع)، لا تُعِد تشغيل جدوله.

**قواعد التفويض**:
- **ملف واحد لكل التزام** — يسهّل المراجعة والعكس.
- **لا تكلّف وكيلاً بما لا يمكنه قياسه** (لا متصفّح لديه ⇒ لا تطلب منه حكماً بصرياً).
- **فرّق الملفات بين الوكلاء**؛ وإن تقاطعت فأعلن أنك ستحلّ التعارض بنفسك.

---

## ٨) مراجعة عمل الوكيل — بنفسك لا بتقريره

**القاعدة**: تقرير الوكيل **مصدر للفحص لا للتصديق**. راجع بنفسك: هل الدمج أمين؟
هل الأرقام صحيحة؟ هل الحارس ما زال يرى؟

**ترسانة المراجعة التي استُعملت هنا** (أعِد استعمالها):
| الفحص | الأمر/الأداة | ما يُثبته |
|---|---|---|
| أمانة الـcherry-pick | `git diff <التزامي> <التزام الوكيل> -- <المسار>` = فارغ | لم يُدسّ شيء |
| أمانة الـrebase | `git diff <البصمة القديمة> HEAD -- <المسارات>` = فارغ | إعادة الترتيب لم تمسّ العمل |
| تكافؤ refactor | مقارنة **مجموعات السلاسل النصية** في الحزمة المبنية قبل/بعد | نقل بلا تغيير نصّي |
| تكافؤ أعمق | عدّ `invoke(` · `listen(` · معرّفات DOM · مفاتيح التخزين · أصناف CSS | لا معرّف ضاع |
| تكافؤ CSS | تطابق **بصمة** ملف CSS المبني | لا انحدار نمط |
| البوابات المعدَّلة | مُفسِدات الحارس المعدَّل (٧ مُفسِدات على الأقل) | الحارس أقوى لا أضعف |
| أرقام الوكيل | أعِد تشغيل الأمر بنفسك | الرقم حقيقي |

**قاعدة ذهبية**: إن عدّل وكيل **حارساً** ليمرّ عمله، فتوقّف. الحارس يُقوّى أو يُعاد
تصميمه، ولا يُرخى. (حدث: وكيل عدّل حارس تكافؤ الإعدادات بعد نقل دالة — رُوجع
بـ12 فحصاً و7 مُفسِدات فأُقرّ لأنه صار **أقوى**: يفشل عند صفر تعريف أو تعريفين.)

---

## ٩) نظافة git — لا force-push أبداً

- **قاعدة مطلقة**: لا `push --force` ولا `--force-with-lease` ولا حذف فرع `main`.
  حماية الفرع مفروضة على الخادم أصلاً؛ لا تحاول تجاوزها.
- **التزام لكل وحدة منطقية** برسالة `type(scope): subject` بالإنجليزية تشرح **لماذا**
  لا «ماذا» فقط:
  - `fix(trace): stop the recursion that made every load row vanish`
  - `ci(gate): build the frontend — the gate checked types but never ran vite build`
- **تحقّق قبل الالتزام**: `git status` · `git diff --check` (مسافات/نهايات أسطر) ·
  وأن الملف الذي عدّلته **نهايات أسطره كما كانت** (LF مقابل CRLF فخّ حقيقي هنا).
- **وافحص `git status` *بعد* كل `git add` أيضاً، لا قبله فقط** (حادثة مسجَّلة: جهّزتُ
  ثلاثة ملفات ثم جهّزتُ ملفاً رابعاً والتزمتُه — فاجتاح الالتزام **الأربعة** ورسالته
  تذكر واحداً. والتصحيح بـ`--amend` **ممنوع** بعد الدفع ⇒ بقي الالتباس في التاريخ
  وسُجِّل في سجل التدقيق. القاعدة: **ما هو مُجهَّز الآن؟** قبل كل التزام).
- **وراجع أن كل رقم في رسالة الالتزام مقيس**: رسالة فيها رقم مخترع أسوأ من رسالة بلا أرقام.
- **وإن بدأت عملية git متعدّدة الخطوات** (`cherry-pick`/`rebase`/`merge`) **فأنهِها أو
  نظّفها في الجلسة نفسها**: حالة `sequencer` تُترك خلفها فترفض git التزامات لاحقة.
  **ولا تستعمل `--abort`** قبل قراءة `abort-safety`: قد يعيد الشجرة إلى نقطة قديمة
  ويهدم عملاً مُلتزَماً — `--quit` يُنظّف الحالة **بلا لمس الشجرة ولا `HEAD`**.
- **لا تلتزم ملفات عمل محلية**: أرشيف الخطط محلي (انظر `ARCHIVE/` وقسم ١٠).

**٩.أ) الإصدار المنشور لا يُمَس (سياسة ب-١)**

> **إذا تغيّر بايت واحد في `EXE`/`MSI` تغيّر رقم الإصدار.** ولا استبدال لأصول إصدار
> منشور. والعلاج عند الخطأ **إصدارٌ جديد** ووسم السابق `deprecated`. والاستثناء الوحيد
> قناة `assets-v1` (الإصلاح الذاتي) فهي **مُتغيّرة بطبعها**. **ولا `tag -f` ولا
> `push --force`.**

**لماذا**: البصمة المنشورة **عقد عام** — من نزّل 0.2.6 وحفظ بصمته يجب أن يجد نفس
البايتات بعد شهر. واستبدال أصل تحت الرقم نفسه يجعل البصمة **تكذب**، ويُبطل كل
«تطابقت ثلاثاً» في `docs/RELEASE-*.md`. **وسابقة مسجَّلة**: أُعيد بناء أصول 0.2.6
**تحت الرقم نفسه** بعد إصلاح اتجاه لوحة الإعدادات (قرار المالك) ⇒ صار للإصدار
**بناءان**، واحتُفظ ببصمات البناء الأول نصّاً للتمييز. **والقاعدة التي وُلدت من ذلك**:
البايت يتغيّر ⇒ الرقم يتغيّر؛ فإن اضطررت للبناء ثانيةً فاذكر البناءين صراحةً ولا
تُخفِ الأول.

**والوسم المنشور لا يُحرَّك**: كان مقترحاً تحريك `v0.2.6` ليطابق مصدر الأصول، ثم
شُطب — لأن تحريك وسم منشور **إعادةُ كتابة مرجع عام**. والبديل المسجَّل: **الالتزام
الذي بُنيت منه الأصول يُكتب نصّاً** في `docs/RELEASE-X.Y.Z.md` (‏`80e5436` لـ0.2.6)
⇒ فالغموض **موثَّق** لا مُخفى.

**٩.ب) كل عطل ⇒ اختبار دائم (سياسة ج-٢)**

> **كل عطل ميداني يمرّ**: اختبار يفشل على الكود القديم ← إصلاح ← نجاح ← **يبقى
> للأبد**، ويُربط بالبلاغ في تعليق. وإن كان الاختبار يدوياً فيُدرج صفّه في
> `qa/TEST-MATRIX.md`.

**لماذا**: عطلٌ بلا اختبار يعود. والاختبار الذي يبقى هو الفرق بين «أصلحنا» و«لن يتكرّر».
**والترتيب مُلزِم**: اكتب الاختبار على الكود **قبل** الإصلاح — فإن نجح على الكود القديم
فأنت لم تفهم العطل بعد (وهذا مُفسِد باطل، انظر §٣).

**وحدّ الإدراج**: الاختبار الدائم يُوصل بـCI في نفس العمل، وإلا فهو نصيحة (§١٠).
وإن تعذّر آليّاً (يحتاج ويندوز/عتاداً/متصفّحاً) فالتصريح بذلك **إلزامي** والصفّ في
المصفوفة **بديل مُعلَن لا مكافئ**.

---

## ١٠) البوابة التي ليست في CI ليست بوابة

**القاعدة**: كل حارس/فحص تضيفه يُوصَل بـ CI **في نفس العمل**، وإلا فهو نصيحة.

**الدليل من هنا**: حارس الإضافة (473 فحصاً) كان يعمل **يدوياً فقط** حتى وُصل بـ
`ci.yml`. وأخطر منه: **CI لم يكن يبني الواجهة إطلاقاً** (`tsc` يفحص الأنواع ولا ينتج
حزمة) ⇒ انكسار البناء كان يمرّ أخضر، حتى كشفه refactor كبير. القاعدة العملية:
**اسأل دائماً: ما نوع العطل الذي يمرّ من بواباتي اليوم بصمت؟**

**وإن تعدّلت بوابة**: أعِد ترقيمها/تسميتها في الوثائق، **وصحّح مراجع الأسطر**
(`ci.yml:94-95`) لأنها تتقادم بصمت (وجدنا **تسعة** مراجع قديمة في جدول واحد).

---

## ١١) تعريف «منجز» (Definition of Done)

لا تقل «تمّ» حتى تجتمع هذه:

- [ ] الكود مكتوب ومقروء بعد الكتابة (لا تعتمد على ما نويت كتابته).
- [ ] **قياس قبل/بعد** بالأرقام والوحدات.
- [ ] **اختبار سلبي** أثبت أن الحارس يرى الخطأ (أو تصريح صريح بتعذّره).
- [ ] البوابات كلها خضراء **بأمرها الحرفي**، ومخرجاتها في التقرير.
- [ ] البوابة الجديدة **موصولة بـ CI** إن كانت جديدة.
- [ ] الوثائق المتأثّرة محدَّثة (`docs/` · `README` · `CHANGELOG` إن لزم).
- [ ] **بند في سجل التدقيق** (`docs/AUDIT.md`) بتاريخه ومقياسه.
- [ ] قسم **«ما لم أتحقّق منه»** مكتوب بصراحة.
- [ ] التزامات صغيرة برسائل تشرح السبب، ولا force-push.

---

## ١٢) قوالب جاهزة

**قالب تقرير وكيل** (يُعطى للوكيل حرفياً):
```
1) جدول الالتزامات: البصمة · الرسالة · الملفات
2) ما تغيّر في كل ملف (بمرجع سطر)
3) القياس قبل/بعد لكل بوابة (أرقام حقيقية من تشغيلك)
4) الاختبار السلبي: ما عبّثته · الأمر · النتيجة الحرفية
5) قرارات اتخذتها من عندك ولماذا
6) ما لم تستطع التحقّق منه بصراحة
```

**قالب بند سجل التدقيق**:
```
- **<البند> — <العنوان>** ✅ (`<البصمة>`): <ما نُفِّذ بدقّة>
  - **القياس**: <قبل> ⟶ <بعد> (الأمر/المصدر)
  - **الاختبار السلبي**: <ما عبّثته> ⇒ <النتيجة الحرفية>
  - **حدّ أمانة**: <ما لم يُتَحقَّق منه>
```

**قالب سؤال المالك عند قرار**: **ما هو · المصلحة · الضرر/الكلفة · توصيتي · ما أحتاجه
منك**. لا تعرض قراراً بلا هذه الخمسة (المالك طلب هذا النمط صراحةً).

---

## ١٣) التواصل مع المالك

- **اللغة**: العربية، والمصطلح التقني بالإنجليزية كما هو (`clippy` · `SHA-256`).
- **الأرقام أولاً**: جدول أو قائمة، لا سرد.
- **لا مجاملة ولا تهويل**: «نجح» تعني نجح بدليل، و«أخفق» تُقال بلا تجميل.
- **الإذن قبل الخطر**: النشر، الرفع إلى متجر، تغيير اعتماديات، أي عمل لا رجعة فيه
  ⇒ اطلب الإذن صراحةً. أما الإصلاحات العادية داخل النطاق فلا تنتظرها.
- **لا تُخفِ عيباً وجدتَه في عملك السابق**: صحّحه، وسجّله، واذكر أنه كان خطأك.
- **قبل أي تقرير حالة: ثبّت الموقع صراحةً** (بند م٨/ن-٤، اكتُشف أثناء نشر 0.2.8): `git rev-parse HEAD`
  و`git rev-parse origin/main` — فإن **انزاح `main` المحلّي** عن البعيد فقُل «محلّي وغير مدفوع»
  ولا تُبلِغ عنه كحالة المنشور. الرقم بلا هذا التثبيت يوهم بأن ما تُبلغ عنه هو ما يراه الناس.

---

## ١٤) عدّلها لمشروعك — بوابات هذا المستودع

> أوامر حرفية. `cargo` يحتاج `$env:PATH="$env:USERPROFILE\.cargo\bin;$env:PATH"`
> و`cargo` يُشغَّل من `src-tauri` (لا `Cargo.toml` في الجذر).

| البوابة | الأمر | ما تحكمه |
|---|---|---|
| اختبارات Rust | `cargo test --quiet` (من `src-tauri`) | المحرّك والإعدادات والجسر (٣١٨ ناجح · ٠ فاشل · ٤ مُهمَل بعد م٣) |
| clippy | `cargo clippy --all-targets` | خطأ = فشل (١٤ تحذيراً موثَّقاً) |
| TypeScript | `pnpm exec tsc --noEmit` | أنواع الواجهة |
| **التنسيق** | `cargo fmt --check` (من `src-tauri`) | الشجرة منسَّقة (كانت 410 كتل فرق) |
| **بناء الواجهة** | `pnpm build:web` | أن الحزمة **تُبنى فعلاً** (tailwind + vite) |
| **اختبارات الواجهة** | `pnpm test:web` | ٢٢٠ حالة في ١٣ ملفاً: عقد مصارف HTML · وحدات نقية · تطبيع السقف · تكافؤ مفاتيح الترجمة |
| **حارس الهندسة** | `node scripts/check-layout.cjs` | 18 صندوقاً عائماً في 6 حالات داخل حدود النافذة (**بعد البناء**) |
| تكافؤ الإعدادات | `pnpm settings:parity` | كل حقل في `Settings` له مقابل في `collectSettings()` (‏24=24) |
| حراسة الإضافة | `pnpm ext:guard` | 513 فحصاً على `content.js` |
| مُفسَدات الإضافة | `pnpm ext:mutants` | بوابة **سلبية**: كل مُفسَد يجب أن يُسقط الحارس |
| اتساق الإصدار | `pnpm versions:check` | الإصدار في أربعة ملفات |
| حرّاس الموقع | `pnpm site:check` | CSS · صفحات · وسوم · عربي · روابط · التذييل |
| سجلّ الاختبار اليدوي | `node scripts/matrix-check.cjs` | 20 صفاً إلزامياً في `qa/TEST-MATRIX.md` كلٌّ بتاريخه وجهازه ونتيجته |
| **حارس مدخل الفصل** | `pnpm separation:entry` | كل مدخل حيّ إلى `process_file` يمرّ عبر الغلاف الواحد (٣ مواضع مسموحة بعده) |
| **حارس الحرّاس** | `pnpm guards:selfcheck` | لكل حارس: مُفسَد يُسقطه + ضابط يمرّ + صفر مدخل يفشل بصوت عالٍ (12/12 · 44/44 · 12/12) |
| **نسب نسخة الإصدار** | `pnpm release:verify` | الثنائي المُسلَّم مُثبَت النسب **بالقياس الحيّ** (يُشغَّل على مسار التنزيل مع `yt-dlp` مزيّف، ويُفحَص ما مرّره فعلاً) + ليس أقدم من أحدث مصدر · و`--self-test` يشغّل ضابطاً ومُفسَداً · بوّابة تسليم قبل كل نسخة تُسلَّم للمالك |
| الخط الكامل (خارج الست عشرة) | `pnpm e2e:release` | E2E بالنموذج وffmpeg (بوابة إصدار، مُهمَل في CI العادي) |

**بنية سريعة**: `src-tauri/src/` (Rust: `separator` · `pipeline` · `bridge` ·
`settings` · `cuda_runtime` · `yt_dlp` · `telegram` · `repair`) · `src/` (واجهة
TypeScript مقسَّمة إلى وحدات) · `index.html` (واجهة واحدة، **لا تُفصل** — البند
مردود بالقياس) · `docs/` (جذر النشر على الموقع + كل الوثائق) · `browser-extension/`
(إضافة MV3، دورة إصدار مستقلة) · `ARCHIVE/` (ملفات عمل محلية غير مُتتبَّعة).

---

## ١٥) الفخاخ المسجَّلة (توفّر ساعات)

**بيئة ويندوز / PowerShell**
- `$env:TEMP` في هذه الجلسة = `C:\windows\TEMP` **لا** مجلد المستخدم ⇒ ملف كتبتَه
  بـ`$env:TEMP` لن تجده في `%LOCALAPPDATA%\Temp`.
- **لا تستعمل نصّاً مزدوج الاقتباس فيه backtick** لكتابة ملف (أفسد ملفات فعلاً).
  استعمل أدوات التحرير المباشرة.
- `Select-Object -First N` على مخرجات أمر أصلي قد **تقطع الأنبوب قبل أن ينتهي** ⇒
  نتيجة ناقصة تُقرأ كأنها كاملة.
- `[System.IO.File]::ReadAllText` بمسار **نسبي** يقرأ من المجلد الأصل لا من مجلدك.
- الملفات: بعضها LF وبعضها CRLF؛ أدوات ويندوز تكتب CRLF وتُفسد التزاماً نظيفاً.
  تحقّق بـ`git diff --check` وأن `core.autocrlf` لا يقلب لك الملف.

**Node/JS**
- `process.exit()` ومقبض `fetch` ما زال يُغلق ⇒ انهيار `libuv` على ويندوز ورمز خروج
  غريب. استعمل `process.exitCode` واترك الخروج طبيعياً.
- `Invoke-WebRequest.Content` لملف نصّي قد يعود **مصفوفة بايتات** لا نصاً ⇒ افحص
  النوع قبل `-match` (وإلا فحصك باطل ويبدو ناجحاً).
- تعابير «واعية بالاقتباس»: السلسلة الملتقطة تحمل علامتَي الاقتباس — جرّدهما قبل
  مطابقة النمط.
- الحزمة المُصغَّرة تُعيد تسمية المتغيّرات المحلية ⇒ فرق في القوالب النصّية **متوقَّع**
  ولا يعني تغيّر سلوك. ميّزه عن الفرق الحقيقي قبل أن تُعلن انحداراً.

**Rust/CI**
- `cargo test` يحتاج موارد الحزمة (`bin/` · `models/`) وإلا فشل `tauri_build` قبل أي
  اختبار؛ نموذج المحرّك (63.7MB) غير متتبَّع ⇒ اختبارات المحرّك **تُتخطّى صامتة** بدونه.
- اختبارٌ يلمس `apply_settings` يسحب الـpipeline إلى ثنائي الاختبار ⇒ فشل إقلاع
  بصيغة `0xc0000139`. لا تلمس مسارات الإعدادات في اختبارات الوحدة بلا داع.
- عمل `gh run watch` قد يبقى معلّقاً بعد انتهاء التشغيل ⇒ اقرأ الحالة بـ
  `gh run list --json status,conclusion` بدل انتظاره.

---

## ١٦) خارج النطاق / ممنوع

- **ممنوع**: `#[allow(dead_code)]` جديد · force-push · تعديل ملفات وكلاء آخرين أثناء
  عملهم · نشر أو رفع بلا إذن · تعديل `docs/` من وكيل تنفيذ (يملكه المشرف) ·
  كتابة «تمّ» بلا دليل · حذف سطر من حارس ليمرّ العمل.
- **خارج النطاق بلا تكليف صريح**: ترقية اعتماديات · إعادة تصميم واجهة · تغيير سلوك
  مستخدم لم يُطلب · أي شيء يحتاج عتاداً أو متصفّحاً وتزعم أنك اختبرته.

---

## ١٧) قائمة تحقّق نهائية قبل الدفع

```
[ ] git status نظيف إلا ما أردتُ التزامه
[ ] git diff --check نظيف (لا مسافات زائدة/تعارض أطراف أسطر)
[ ] البوابات التسع خضراء — بالأمر الحرفي — ومخرجاتها محفوظة
[ ] اختبار سلبي مُنفَّذ ومُسجَّل لكل تغيير سلوكي
[ ] كل رقم في رسائل الالتزام مقيس فعلاً
[ ] الوثائق المتأثّرة محدَّثة (ومنها سجل التدقيق بتاريخ اليوم)
[ ] لا force-push · لا التزام لملفات محلية/أرشيف
[ ] بعد الدفع: اقرأ نتيجة CI كاملة (لا تفترض الخضرة) وأصلح قبل أن تنتقل
```
