# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project specifics
/bin/
/models/
HaramLite Downloads/
.env
/src-tauri/target/
/font/تعليمات استخدام الخط.md
src-tauri/vc_redist.x64.exe

# Generated by tailwindcss CLI (pnpm predev / pnpm build)
/src/tailwind.css

# Updater signing private key — NEVER commit (public key lives in tauri.conf.json)
/updater.key

# Third-party reference only — ideas, never code (AGPL). See docs/AUDIT.md.
/inspiration/1.32.2_0/

# Untracked owner scratch — never commit its content (auto-protection).
docs/rebuild/


# Store listing artwork (contains YouTube screenshots — local only)
/store-assets/
# ── أُضيفت بعد مراجعة خبير (2026-09-12) ──────────────────────────────────────
# أرشيفات تصدير التصميم (ستيتش) — ليست مصدراً
/site/*.zip
# حِزم الإضافة وتوقيعها
*.crx
*.xpi
# مفتاح تحزيم إضافة كروم (.pem سرّ — الحقل key في المانيفست عام ويبقى)
*.pem
*.p12
# مثبتات مبنية
HaramLite_*.exe
HaramLite_*.msi
# ثنائيات وأوزان بأي مسار (لا في /bin فقط)
**/ffmpeg.exe
**/ffprobe.exe
**/yt-dlp.exe
**/*.onnx
*.pdb
# كاش وأدوات
.vite/
.pnpm-store/
*.tsbuildinfo
coverage/
*.lcov
# بيئة
.env.*
!.env.example
# ويندوز
Thumbs.db
Desktop.ini
# بيانات تشغيل محلية (yt-dlp / التطبيق)
cookies.txt
*.part
*.ytdl
# فيديو عرض الإضافة — مكانه المناسب الإصدارات لا المستودع
/browser-extension/*.mp4
# نسخ لقطات مرقّمة (الاسم القانوني هو IMG/Screenshot.png)
/IMG/Screenshot v*.png

# ملاحظات جلسة وخطط عمل وتعاريف أدوات محلية — لا تُنشر (أُضيفت 2026-09-14)
/fixplan.md
/HANDOFF.md
/0.2.3.md
/0.2.3-EXEC.md
/local_code_auditor.md
