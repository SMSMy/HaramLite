# 0.2.3-EXEC.md — تعليمات التنفيذ للوكيل (محرّك فقط)

> **اقرأ هذا الملف كاملاً قبل أي عمل.** المرجعان المجمَّدان في `fixplan.md §0` ✓. المبدأ: **متغيّر واحد لكل التزام** ✓.
> **ما تفعله هنا**: التزامان فقط ✓ (سطر `peak` ✓ + تنظيف التحذيرات ✓). **ثم توقف** ✗ — لا إصدار ✗ ولا رفع ✗.

---

## 0. هوية المشروع — **استعمل هذه الأسماء حرفياً** ✗ (أي غيرها يفشل)
```
[package] name = "haramlite-rs"        ← لا "haramlite" ✗
[lib]     name = "haramlite_rs_lib"
[[bin]]   name = "HaramLite"

Cargo.toml في الجذر: **غير موجود** ✗ ⇒ نفّذ من `src-tauri/` ✓ أو أضف `--manifest-path src-tauri/Cargo.toml` ✓
أوامر مسموحة:  cargo test --lib                 (من src-tauri/)
               cargo build --release -p haramlite-rs
```

## 1. قبل البدء — تحقّق، وإلا **توقّف وأبلغ**
```
HEAD = cb009bb أو ما بعده على نفس الخط (f08d84a · ef6a9ff · cb009bb) ✓
الشجرة: **نظيفة في الملفات المتعقّبة** ✓ — وغير المتعقّبة (0.2.3.md · HANDOFF.md · fixplan.md) **لا تُعدّ اتساخاً** ✓
        ولا تُدرِجها في أي التزام ✗ ولا تحذفها ✗
إن كانت ملفات متعقّبة معدّلة ✗ أو HEAD خارج هذا الخط ✗ ⇒ توقّف وأبلغ ✓
اقرأ أي ملف قبل تعديله ✓ · وبعد كل كتابة **أعد القراءة وتأكّد من البايتات** ✓ — لا تُعلن نجاحاً بنيّة الأمر ✗
```
**ومواضع الملفات تُؤخذ من سطر التحذير في ناتج البناء** ✓ — لا يُفترض `bridge.rs` ✗.

---

## 2. الالتزام ١ — سطر `peak` (سطر واحد ✓ صفر سلوك ✓)

**الملف**: `src-tauri/src/separator.rs`
**ابحث عنه** (لا تعتمد على رقم سطر قديم ✗):
```rust
    let peak = normalize(&mut mix);
```
**وأدرج بعده مباشرةً**:
```rust
    tracing::info!(
        target: "sep",
        "level: peak={peak:.4} ({:.2} dBFS) attenuated={}",
        if peak > 0.0 { 20.0 * peak.log10() } else { -120.0 },
        peak > 0.9
    );
```
**قواعد**: لا تُضِف سطر تغطية ✗ (‏`coverage` مُسجَّلة أصلاً في `:670` و`:689` ✓) · ولا تُغيّر `normalize()` ولا `demix()` ولا أي DSP ✗.

### التحقق قبل الالتزام
1. أعد قراءة الدالة: السطر بعد `normalize` وقبل `analyze_mix`/`demix` ✓
2. `rg 'level: peak=' src-tauri/src/separator.rs` ⇒ **مطابقة واحدة** ✓
3. `cargo test --lib` (من `src-tauri/`) ⇒ **0 فاشل** ✓ — سجّل الأمر ورمز الخروج ✓
4. **اختبار سلبي سلوكي** (لا `rg` ✗ — فالـ`rg` لا يُثبت شيئاً عن السلوك ✓):
   ```
   أ) .\src-tauri\target\release\HaramLite.exe "C:\Temp\tiny.wav" --out "C:\Temp\o"     ← CLI، بلا واجهة ✓
      (الـCLI موثّق في cli.rs:4 ⇒ haramlite <files...> [--out DIR] [-m song|clip])
   ب) اقرأ ملف السجلّ:   %LOCALAPPDATA%\com.harammute.haramlite\logs\haramlite.log.<YYYY-MM-DD>
      ⇒ يجب أن يظهر  level: peak=…        ← ★ السجلّ ملف ✗ لا طرفية ✓ (logging.rs:115)
   ج) علّق سطر tracing ⇒ أعد (أ) ⇒ يجب أن يغيب level: peak= ⇒ **استعادة السطر** ✓
   ```

**رسالة الالتزام**:
```
log input peak after normalize for 0.2.3 gate

Emits peak (linear + dBFS) and whether the 0.9 attenuator ran.
Diagnostic only; no DSP change. Needed to decide UVR5 level-path (step 2).
```
ثم `git show --stat` ⇒ **`separator.rs` وحده** ✓.

---

## 3. الالتزام ٢ — حذف الكود الميت + تصحيح `AUDIT` (لا سلوك ✓)

### ٢أ — `src-tauri/src/separator.rs`
- **احذف الدالة** `splice_span` بالكامل ✓
- **واحذف اختباريها** (ابحث عن `splice_is_sample_exact` و`splice_seams_have_no_clicks` ✓ — لا تفترض أرقام أسطر ✓) — وأي مساعد اختبار يصبح غير مستعمل **بسبب هذا الحذف وحده** ✓
- **لا `#[allow(dead_code)]`** ✗ — السبب: `lib.rs:20` يعلن `mod separator;` **خاصة** ✗ ⇒ `pub` لم يُسكت التحذير قط ✓
- **لا تُعِد بوابة المقاطع** ✗

### ٢ب — التحذيرات الأخرى (**المسار من ناتج البناء** ✗ لا من هنا)
**المتوقّع (تحقّق ✓ لا تفترض ✗)**:
- `job_url_of` في `bridge.rs:373` ✓ — **نداءات إنتاجية: 0** ✓ — **نداءات اختبار: 5** في `bridge.rs:1611-1617` ✓ ⇒ **احذف الدالة واختبارها معاً** ✓ (مثل `splice_span` ✓) — وإن لزم تعديل **منطق مسار إنتاجي** ⇒ توقّف وأبلغ ✗
- `KEY_WRITE` و`std::path::Path` غير المستخدمين: **ليسا في `bridge.rs`** ✗ — آخر ما رُئيا: `autostart.rs` (`KEY_WRITE` في استيراد `winreg` ✓) و`Path` في `autostart.rs` و`cli.rs` و`effects.rs` ✓ ⇒ **احذفهما فقط من الملف الذي سمّاه `rustc`** ✓

### ٢ج — `docs/AUDIT.md` (**عربي** ✗ — لا فقرة إنجليزية ✗)
ابحث عن العبارة الخاطئة («`pub` فلا تحذير» ✓) واستبدلها بالعربية:
```
بعد الخطوة ١ لم يعد لـ `splice_span` أي نداء، فيُحذّر المحرّك:
`warning: function splice_span is never used`.
`lib.rs` يعلن `mod separator;` وحدةً خاصة، لذلك يظهر التحذير في كل بناء
ويغطّي التحذيرات الحقيقية. الحل: حذف الدالة واختباريها (نُفِّذ في 0.2.3).
لا تُستخدم `#[allow(dead_code)]`.
```

### التحقق قبل الالتزام
1. `rg splice_span src-tauri --glob '*.rs'` ⇒ **0** ✓
2. `rg job_url_of src-tauri --glob '*.rs'` ⇒ **0** ✓
3. `cargo test --lib` ⇒ **0 فاشل** ✓ (اختبارا `splice` واختبار `job_url_of` زالت ✓ والباقي يعمل ✓)
4. `cargo build --release -p haramlite-rs` ⇒ **صفر تحذيرات** ✓ — وإن بقي تحذير **خارج** الملفات التي لمستها ✗ ⇒ لا تُوسّع النطاق ✗ **وأبلغ بأسطره حرفياً** ✓
5. **اختبار سلبي**: استعد `splice_span` (الدالة + اختباراها) من `git show HEAD:src-tauri/src/separator.rs` **بلا التزام** ✓ ⇒ `cargo build --release -p haramlite-rs` ⇒ **يجب أن يعود التحذير** ✓ ⇒ ثم `git checkout -- src-tauri/src/separator.rs` ✓

**رسالة الالتزام**:
```
remove dead splice_span and unused autostart/bridge symbols

After whole-file demix, splice_span had no runtime callers; tests
only. lib.rs uses private `mod separator`, so the unused-fn warning
fired every release build. Drop job_url_of with its test (prod
callers: 0). Drop unused KEY_WRITE/Path from the file rustc named
(autostart.rs, not bridge.rs). Correct AUDIT.md: pub never
suppressed that warning.
```
ثم `git show --stat` ⇒ المتوقّع: `separator.rs` ✓ · `bridge.rs` ✓ · `autostart.rs` (إن أشار إليه `rustc` ✓) · `AUDIT.md` ✓ — **ولا شيء غيرها** ✗ (ولا تُدرج `0.2.3.md`/`HANDOFF.md`/`fixplan.md` ✗).

---

## 4. توقّف هنا ✗
**لا** رفع أرقام ✗ · **لا** `RELEASE-0.2.3.md` ✗ · **لا** `tauri build` ✗ · **لا** `git push` ✗ · **لا** B/E ✗ · **لا** `silence.rs` ✗ · **لا** overlap/النافذة/‏`pcm_s16le` ✗ · **لا** الإضافة ✗ · **لا** HTML الموقع ✗.

## 5. شكل الردّ على المالك (هذا الجدول حرفياً ✓)
| step | command | exit | evidence |
|---|---|---|---|
| commit 1 | … | … | عدد `rg 'level: peak='` · ملخّص الاختبارات · السلبي: غاب من السجلّ ثم استُعيد |
| commit 2 | … | … | `rg splice_span`=0 · `rg job_url_of`=0 · عدد التحذيرات في `cargo build --release -p haramlite-rs` |
| cargo test --lib | … | … | N ناجح / 0 فاشل |
| HEAD hashes | commit1=… commit2=… | | |

وإن خالف الواقع شيئاً (سطر انتقل ✓ · `job_url_of` لها نداء إنتاجي ✓ · `rustc` سمّى ملفاً غير `autostart.rs` ✓ · مسار `AUDIT` مختلف ✓) ⇒ **توقّف عند تلك الخطوة الفرعية وأبلغ** ✓ — **ولا تختلق بديلاً** ✗.

---

## 6. `ق٥` — القياس الفاصل (للمالك، لا للوكيل ✗)

### ✅ وقد تأكّد نصفه من سجلّات جهازك فعلاً ✓✓
```
09-13 (ملف «الصابون»):  suspect_spans=0 · coverage=0.00 · inference_ms=43    ✗ لا استدلال إطلاقاً (الاستدلال الحقيقي 8–16s)
09-14 (تشغيل اليوم):    spans=1 · coverage=1.00 · inference_ms=8805          ✓ الخطوة ١ تعمل
06/11/12 (مواد أخرى):   coverage=0.97–1.00                                    ✓ البوابة لم تكن تضرّ هناك
```
⇒ **`ق١` مؤكّد على ملفك** ✓✓ — **و`A` كانت قاتلة على مادة واحدة فقط** ✓ (وهذا يفسّر أن التجارب السابقة بدت ممتازة ✓).

### والخطوة المتبقية (١٠ دقائق)
1. **أنشئ ملف المحرّك** بنفس أمر التطبيق ✓ (`media.rs:223` ✓):
   ```powershell
   $ff = "…\haramlite-rs\src-tauri\target\release\bin\ffmpeg.exe"
   & $ff -y -i "الصابون….mp4" -vn -ac 2 -ar 44100 -c:a pcm_s16le "C:\Temp\engine_in.wav"
   & $ff -i "C:\Temp\engine_in.wav" -af volumedetect -f null NUL      # ⇒ max_volume = peak
   ```
2. **افتح `engine_in.wav` في UVR5** ✓ (‏MDX-Net · `UVR-MDX-NET-Voc_FT` · segment 256 · overlap **Default** · WAV ✓)
3. **وعالج نفس المقطع في تطبيقنا** ✓ بوضع **«مقطع عادي» (Clip)** ✓ (ليحفظ المقياس الزمني ⇒ تصير المقارنة مباشرة ✓✓) — وأخرج **صوتاً** (‏MP3 ✓) ليبقى مسار الأصوات ✓
4. **قارن مسار الأصوات** سماعياً ✓ + والصق لنا: `max_volume` ✓ · `coverage` من السجلّ ✓ · وبقي الفرق أم اختفى ✓

### والقراءة
```
تساويا      ⇒ الفجوة في المدخل (s16/resample) ⇒ 0.2.4
UVR5 أفضل   ⇒ الفجوة في المحرّك ⇒ الخطوة ٢ في 0.2.4 (بموافقة صريحة)
```
