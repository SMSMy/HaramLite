# @  تُهمَل آثار التشغيل: هي دليل تشغيل لا يُلتزم، وتتجدد بتشغيل المُقيِّم.
#    سبب وجود هذا الملف **داخل** المجلد لا في `.gitignore` الجذر: نطاق التعديل
#    محصور بـ`qa/**`، والأثر واحد سواء كُتب السطر هنا أو هناك.
#    (ملف `.gitignore` نفسه متتبَّع، فلا يبتلع القاعدة نفسها.)
*
!.gitignore
