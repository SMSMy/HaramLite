# 0.2.3.md — خطة إصدار 0.2.3 (المحرّك)

> **النطاق**: **محرّك الفصل فقط** ✓ — لا الإضافة ✗ ولا الموقع ✗ (لكلٍّ قناته ✓).
> **الحالة**: 3 التزامات محلية غير مدفوعة ✓ · الشجرة نظيفة ✓ · بناء كامل ناجح ✓ (ينقصه الرفع ✗).
> **القاعدة**: متغيّر واحد لكل التزام ✓ · قياس قبل/بعد ✓ · لا تعديل بلا سطر من المرجع ✗ (`fixplan.md §0` ✓).

---

## 0. ما هو **داخل** 0.2.3

| البند | الحالة | ملاحظة |
|---|---|---|
| **الخطوة ١** — إزالة بوابة المقاطع (استدلال على الملف كله ✓) | **مُنجزة** ✓ (`ef6a9ff` ✓) | مطابقة UVR5 `:499` ✓ — ومُختبَرة ✓ (12/0 ✓) |
| **بند ١** — سطر تسجيل `peak` | **مطلوب** ✗ | يفتح قرار الخطوة ٢ ✓ |
| **بند ٢** — التحذيرات الأربعة + تصحيح سطر `AUDIT` ✗ | **مطلوب** ✗ | بناء نظيف ✓ |

### بند ١ — سطر `peak` (سطر واحد ✓ صفر خطر ✓)
**الموضع**: `src-tauri/src/separator.rs` بعد `:661` (`let peak = normalize(&mut mix);`) ✓:
```rust
tracing::info!(target: "sep", "level: peak={peak:.4} ({:.2} dBFS) attenuated={}",
    if peak > 0.0 { 20.0 * peak.log10() } else { -120.0 },
    peak > 0.9);
```
> ⚠ **لا تُضِف سطر تغطية** ✗ — **موجود مرتين** ✓: `:670` («`mix analysis: … coverage={:.2}`» ✓) و`:689` («`SEPARATE-AB-REPORT … coverage={:.2}`» ✓).
> **التحقق**: شغّل معالجة وابحث عن `level: peak=` في السجلّ ✓ — **اختبار سلبي**: علّق السطر ⇒ يجب أن يغيب ⇒ استعادة ✓.

### بند ٢ — التحذيرات الأربعة (صفر خطر وظيفي ✓)
```
src-tauri/src/separator.rs:623  warning: function `splice_span` is never used     ← ★ من تعديلنا ✓ (نداءاته الوحيدة اختباراه :994 و:1011)
src-tauri/src/bridge.rs:373     warning: function `job_url_of` is never used      ← قديم ✓
                                warning: unused import: `KEY_WRITE`                ← قديم ✓
                                warning: unused import: `std::path::Path`          ← قديم ✓
```
**القرار**: **احذف** `splice_span` + اختباريه ✓ (‏`git` يحفظه ✓ — وإن عاد نهج المقاطع يُستعاد ✓) — **ولا `#[allow(dead_code)]`** ✗ (الدالة **بلا نداء** بعد الخطوة ١ ✓ فلا مبرّر لإبقائها صامتة ✓).
**والسبب أن `pub` لم يمنع التحذير** ✗: `lib.rs:20` يعلن **`mod separator;`** (خاصة ✗ لا `pub mod`) ⇒ غير قابل للوصول من خارج الحزمة ✓.

**وتصحيح سطر `AUDIT`** ✗ (الجملة «`pub` فلا تحذير» **غير صحيحة** ✗) — استبدلها بـ:
> «`splice_span` بلا نداء بعد الخطوة ١ ⇒ **المحرّك يحذّر**: `warning: function splice_span is never used` (`separator.rs:623`) — لأن `lib.rs:20` يعلن الوحدة `mod separator;` **خاصة** ⇒ التحذير **ظاهر في كل بناء** ويشوّش على التحذيرات الحقيقية. الخياران: حذف الدالة واختباريها ✓ · أو `#[allow(dead_code)]` مع تعليق ✓ — **مؤجَّل** ✓.»

**التحقق**: `cargo build --release` ⇒ **صفر تحذيرات** ✓.

---

## 1. ما هو **خارج** 0.2.3 ✗ (لا تُدمجه ✗)

| البند | المكوّن | قناته |
|---|---|---|
| **بند ٣** — روابط أقسام صفحة الجسر (`#how-it-works` 0 ✓ · `#video` 0 ✓ · `#install` 0 ✓) | **الموقع** (`docs/bridge.html`) | **بلا إصدار** ✓ — يُنشر بالدفع ✓ |
| **بند ٤** — حذف `mode:'watch'` المضلّل (`content.js:275` ✓) | **الإضافة** | **1.1.2** ✓ (لم تُرفع بعد ✓ ⇒ يُضمّ بلا جولة إضافية ✓) |

> **سبب الفصل** ✓: خلط المحرّك مع الإضافة/الموقع في تغييرة واحدة يخلط **سجلّ التغييرات** ✗ ويعقّد أي **تراجع** ✗✓.

---

## 2. الشرط المسبق — `ق٥` (لا إصدار قبلها ✗)

**الطريقة** (١٠ دقائق ✓): خذ **نفس WAV** الذي يخرجه `normalize_for_engine` ✓ — وافتحه **في UVR5** ✓ (‏MDX · `Voc-FT` · seg 256 · overlap Default ✓) — وقارن بمخرجنا لنفس المقطع ✓.

| النتيجة | المعنى | القرار |
|---|---|---|
| **اختفى الفرق** | المشكلة في **المدخل** (FFmpeg/16-bit/resample ✓) | أطلق 0.2.3 كما هي ✓ — وعالج المدخل في **0.2.4** ✓ |
| **بقي الفرق** | المشكلة في **المحرّك** ⇒ **الخطوة ٢** (مسار UVR5 ✓) | **لا تُدخلها في 0.2.3** ✗✓ — أطلق 0.2.3 كما هي (**A فقط** ✓) وافتح **0.2.4** لـB/E **بموافقة صريحة** ✓ |

> **مبدأ حاكم — «ضيّق 0.2.3 ولا تُوسّعه»** ✓✓: الخطوة ١ **وحدها** هي قيمة هذا الإصدار ✓. وإدخال B/E معها يعني **تغييرين سلوكيين في إصدار واحد** ✗ ⇒ إن تغيّرت الجودة بعد أسبوع (أفضل أو **أسوأ** ✗) **لن تعرف أيّهما السبب** ✗✓ — وهو **نقض مباشر** لقاعدة «متغيّر واحد» ✓ ولـ`fixplan.md §5` («ما يطابق AS يُترك حتى يُقرَّر كسره» ✓).
> **ولا تؤجّل النشر بانتظار ٢** ✗ — **ولا تبتلع ٢ داخل ١** ✗.

> ⚠ **و`cargo test --lib` لا يقيس مطابقة UVR5** ✗ — يقيس **البنية** (الهوية ✓ · الحواف ✓ · الحشو ✓) لا **النزف** ✗ ⇒ **الحكم لـ`ق٥` وحدها** ✓✓ (فلا تستشهد بـ12/0 كدليل جودة ✗).

**وتحقّق الخطة قبل أي تنفيذ للخطوة ٢** ✓: `fixplan.md` **مُحدَّثة فعلاً** ✓✓ — تحقّقتُ بالبحث: «trim-discard» = **0** ✓ · «التطبيع على المخرج» = **0** ✓ — و`§5` ينصّ على: `normalize` **على الدخل** ✓ (‏AS `:169` ✓) · **استعادة** `* peak` ✓ (‏AS `:172` ✓) · الطرح من **المُطبَّع** ✓ (‏AS `:195` ✓) · **overlap-add** + `divider` ✓ (‏AS `:310`/`:351-353`/`:409` ✓).

---

## 3. خطوات الإصدار (بالترتيب)

1. **بند ١** ⇒ التزام مستقل ✓ · **بند ٢** ⇒ التزام مستقل ✓ *(لا تجمعهما ✗)*
2. **الاختبارات**: `cargo test --lib` ⇒ **0 فاشل** ✓ · والحراس الخمسة: `pnpm site:check` ⇒ **exit 0** ✓
3. **`ق٥`** ⇒ والقرار (§2 ✓)
4. **رفع الأرقام الثلاثة** ✗✓ — **في التزام منفصل بعد ١ و٢** ✗✓ (**لا معهما** ✗) — كلها الآن `0.2.2` ✓ ⇒ `0.2.3`:
   ```
   package.json                   0.2.2 → 0.2.3
   src-tauri/tauri.conf.json      0.2.2 → 0.2.3
   src-tauri/Cargo.toml           0.2.2 → 0.2.3     ← الثالث يُنسى عادةً ✗
   ```
5. **`docs/RELEASE-0.2.3.md`** ✓ — **وأضِفه إلى `_config.yml`** ✗✓ في `exclude:` (بعد `RELEASE-0.2.2.md` ✓) — **وإلا نُشرت الصفحة مشوّهة** ✗ (العلّة نفسها في `RELEASE-0.2.2.md` ✓).
6. **حدّث `0.2.2` الثابت في الموقع** ✗ — **30 موضعاً**:
   ```
   docs/index.html 16 · remove-music-from-video 3 · remove-music-from-lectures 2
   + 9 صفحات بموضع لكل منها  ⇒ المجموع 30
   ```
   *(والحراس تكشف «شارة إصدار ثابتة» ✓ — شغّل `pnpm site:check` بعدها ✓)*
7. **البناء الكامل** ✓: `pnpm tauri build` (‏~9 دقائق ✓ — انتهى بنجاح مرة ✓) ⇒ `HaramLite.exe` + MSI + NSIS ✓
8. **التحقق قبل النشر** ✓: شغّل الـEXE من **جذر المستودع** ✓ ⇒ عالج مقطعاً ⇒ تأكّد من ظهور `level: peak=` ✓ و`coverage=` ✓
9. **الإصدار** ✓: `release.yml` يعمل بـ **`workflow_dispatch`** ✗✓ (**تشغيل يدوي من تبويب Actions** ✓ — **لا** بوسم ✗ كما كان يُظنّ ✓)
10. **بعد النشر** ✓: `/RELEASE-0.2.3.html` يجب أن يعطي **404** ✓ (إن ظهر ⇒ خطأ في `exclude` ✗) · وتحقّق من صفحة الإصدارات على GitHub ✓

---

## 4. ممنوع ✗ (في هذه الجولة)

- **B/E** (‏`normalize` الدخل · `*peak` · الطرح من المُطبَّع ✓) — **لا يُلمس** ✗ إلا **بموافقة صريحة** وبعد `ق٥` ✓.
- `silence.rs` ✗ (مشتركة مع `effects.rs:105` و`pipeline.rs:363` ⇒ تُفقد مسار `remux` ✓).
- `overlap` (0.25) ✗ · النافذة (periodic/symmetric) ✗ · تطبيع المخرج ✗ — **كلها للذهبي** ✓.
- خلط الإضافة/الموقع في التزامات 0.2.3 ✗.
- `git push` قبل: الاختبارات ✓ + الحراس ✓ + `ق٥` ✓.

---

## 5. الحالة الحالية (تحقّق منها في بداية الجلسة)

```
HEAD = cb009bb (3 التزامات محلية غير مدفوعة: f08d84a التوثيق · ef6a9ff الخطوة ١ · cb009bb ملاحظة التنظيف)
الشجرة نظيفة ✓ · الحراس الخمسة exit=0 ✓ · cargo test --lib separator:: ⇒ 12 ناجحاً / 0 فاشل ✓
البناء الكامل: نجح ✓ (HaramLite.exe 37MB · MSI 431MB · NSIS 220MB · 8m49s ✓)
النموذج: models/UVR-MDX-NET-Voc_FT.onnx ✓ (63.7MB) · المساعدون: target\release\bin\{ffprobe,yt-dlp}.exe ✓
الإضافة: 1.1.2 جاهزة محلية ✓ — **لم تُرفع** ✗ (المتجر على 1.1.0 بانتظار اعتماد Google ✗)
```

## 6. سجلّ (يملؤه المنفّذ)

| الخطوة | الأمر/الأداة | الرمز | ملاحظة |
|---|---|---|---|
| بند ١ | | | |
| بند ٢ | | | |
| `cargo test --lib` | | | |
| `pnpm site:check` | | | |
| `ق٥` | | | |
| رفع الأرقام الثلاثة | | | |
| `pnpm tauri build` | | | |
| `release.yml` | | | |
